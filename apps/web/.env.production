# Production build (served by chisha.debug_server at 127.0.0.1:8765) talks to
# the real FastAPI backend. Dev (vite dev server) keeps default mock unless
# user overrides — see vite.config.ts proxy + lib/api.ts dispatcher.
VITE_USE_MOCK=0
