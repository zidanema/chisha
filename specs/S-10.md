# S-10 · 文档收尾 + neat-freak

**依赖 S-09。最后一站。**

参考: `docs/CONTRIBUTING_DOCS.md` 文档纪律 ≤ 15 行/决策,4 桶分流。

## What

### 1. docs/decisions.md 加 D-093 (≤ 15 行,产品决策视角)

模板:
```markdown
## D-093 (2026-05-19) Sandbox Lab 白盒时光机落地

- **是什么**: apps/sandbox-lab/ 独立 SPA :5175, 7~14 天可回放/分支推荐沙箱
- **scope 拍板**:
  - 多 session + branch/rollback **完整版** (不做单 session 简化)
  - Refine **单 round**, B panel 改文案为"当前 round 已应用"; 跨顿 TTL 持久化留 v2 (待 D-XXX)
  - 独立 SPA (不并入 apps/debug-ui 保留 read-only invariant)
- **为何不并入 debug-ui**: 后者 read-only Workflow A (D-087), 加可写交互会污染契约
- **brief**: docs/design_briefs/2026-05-19-sandbox-lab.md
```

≤ 15 行严格执行,讲不完就丢弃。

### 2. docs/CONTRACTS.md 加 sandbox 新约束

每条 ≤ 10 行,全文 ≤ 200 行:

- **sandbox_context (ContextVar)**: backend 永远显式 sid 注入,绝不靠全局 active。多 tab 并发 eat 不串
- **meal_to_trace.json 是唯一索引**: rollback/branch 按此查 trace,绝不扫 recommend_trace 目录枚举
- **D-077 兼容**: sandbox.{init,advance,reset,state,...} 旧签名 sid=None → "_default" session
- **sessions/{sid} 整目录拷贝/裁剪**: branch 必须重置 long_term_prefs/decisions/meal_to_trace/recommend_log/recommend_trace,否则未来记忆泄漏
- **Refine 单 round**: backend 不维护跨顿 refine,advance_meal 后 last_recs.applied_refine 清空 (跨顿 TTL 留 v2)

### 3. CLAUDE.md 常用命令补一行

```bash
# 新 sandbox-lab (D-093, 端口 5175, proxy /api → :8765)
cd apps/sandbox-lab && npm install && npm run dev  # http://127.0.0.1:5175
```

### 4. apps/sandbox-lab/README.md 完整化

仿 apps/debug-ui/README.md 写:
- 项目定位 (白盒时光机, 内部 debug)
- 本地拉起 (后端 + 前端两个 server)
- chrome-devtools-mcp 自测约定
- 主题切换 (5 套 or 4 套 accent + 2 套 theme)
- 数据流 (Backend → useSandbox → Components)
- ?dev=1 / ?mock_recommend=1 query 用法

### 5. HANDOFF_PROMPT.md 验收清单勾完

- 在 `chidiansha-sandbox/project/sandbox-lab/HANDOFF_PROMPT.md` 末尾加一节 "## 落地状态 (2026-05-19)"
  - 11 条验收清单逐条 ✓ + 简短验证证据 (S-08 chrome-devtools-mcp 报告链)
  - design brief 链 + 主要 commits
  - 已知 deviation: refine 单 round (B panel 文案不同),跨顿 TTL 留 v2

### 6. design brief 转正

`docs/design_briefs/2026-05-19-sandbox-lab.md` 顶部 status 改:
- 旧: "草稿 / 待 codex review"
- 新: "落地完成 / D-093 已落 decisions.md"

### 7. README.md (项目根) 加 apps/sandbox-lab 索引

如果 README.md 有"应用列表"/"工程目录"一节,加一行:
- apps/sandbox-lab/ — Sandbox Lab 白盒时光机 (D-093)

如果没有就不加,避免散文式列表。

### 8. memory 更新

`~/.claude/projects/-Users-chisha/memory/`:
- 新建 `chisha_sandbox_lab_d088.md`:
  ```
  ---
  name: chisha-sandbox-lab-d088
  description: Sandbox Lab 白盒时光机 - apps/sandbox-lab :5175 独立 SPA, 多 session + branch/rollback, refine 单 round
  metadata:
    type: project
  ---
  D-093 (2026-05-19): apps/sandbox-lab/ 独立 SPA :5175. 多 session + branch/rollback 完整版; Refine 单 round (B panel 改文案), 跨顿 TTL 留 v2.
  
  **Why**: 推荐链路调试沙箱, 7~14 天可回放可分支.
  **How to apply**: 改 sandbox-lab 前端走 [[chisha-chrome-devtools-mcp]] 自测; 改 backend 用 ?mock_recommend=1 避免烧 LLM.
  brief: docs/design_briefs/2026-05-19-sandbox-lab.md
  ```
- 改 `MEMORY.md` 加索引一行:
  - `[D-093 Sandbox Lab](chisha_sandbox_lab_d088.md) — apps/sandbox-lab :5175 独立 SPA, 多 session + branch/rollback`

### 9. (可选) neat-freak skill

如果时间充裕,调 /neat 整理:
- 检查 docs/decisions.md 是否漏 D-093
- 检查 README.md 是否过期 (Phase 0 状态描述要不要更新)
- 检查 archive/ 有没有需要挪的旧文档

## Why

收口落决策 + 同步项目文档 + 防止记忆遗忘。

## Done When

- D-093 在 docs/decisions.md ≤ 15 行
- CONTRACTS.md 新增 5 条约束,全文仍 ≤ 200 行
- CLAUDE.md 常用命令加 sandbox-lab dev 启动
- apps/sandbox-lab/README.md 完整
- HANDOFF_PROMPT.md 验收清单全勾 + 落地状态节
- design brief status 转正
- memory 文件 + MEMORY.md 索引同步
- 所有内链 (file path / brief link) 可达

## Affected files

- 改: `docs/decisions.md`, `docs/CONTRACTS.md`, `CLAUDE.md`, `apps/sandbox-lab/README.md`
- 改: `chidiansha-sandbox/project/sandbox-lab/HANDOFF_PROMPT.md`
- 改: `docs/design_briefs/2026-05-19-sandbox-lab.md` (status)
- 新建: `~/.claude/projects/-Users-chisha/memory/chisha_sandbox_lab_d088.md`
- 改: `~/.claude/projects/-Users-chisha/memory/MEMORY.md`

## 不做

- 不动代码
- 不写新 design brief (本 brief 自带)
- 不挪 archive (除非 neat-freak 提示)