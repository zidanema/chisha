# S-02 · 视觉 + 静态布局 (mock data)

**依赖 S-01。S-03 交互骨架基于本任务。**

参考: `docs/design_briefs/2026-05-19-sandbox-lab.md`,1:1 视觉源 `chidiansha-sandbox/project/sandbox-lab/{styles.css, app.jsx, panels.jsx, timeline.jsx, tweaks-panel.jsx, data.js}`。

## What

把 HTML 原型 1:1 拆成 TS 组件,全部走 mock 数据渲染。**不实现任何交互逻辑** (那是 S-03)。

### Types (apps/sandbox-lab/src/types/sandbox.ts)
- `Session, Meal, Rec, Decision` 与 HANDOFF_PROMPT.md §数据契约 一致。
- 新增 `SessionMeta, FullSnapshot, ActiveRule, TasteEntry, KeywordEntry, FatigueEntry` (设计 brief §数据契约)。

### Mock (apps/sandbox-lab/src/mocks/sbxMocks.ts)
- 从 `chidiansha-sandbox/project/sandbox-lab/data.js` 改 TS,全部 export const。

### Styles (apps/sandbox-lab/src/styles.css)
- 从原型 styles.css 1:1 搬。整个文件包括 :root vars / body.theme-dark / body.density-compact / 所有组件 class。
- 检查:无 inline color,所有色值走 CSS variable。

### Components (apps/sandbox-lab/src/components/)
- `TopBar.tsx` — brand + Session 下拉 + meta chips + 起点 tag + 时钟 pill + 摘要按钮
- `Banners.tsx` — banner stack 渲染 (refine_conflict / swap / refine_applied / review-mode)
- `Timeline.tsx` — 14 格横向条 + variant=calendar 日历卡,带 op-bar 区域 (按钮组样式)
- `DecisionArea.tsx` — 5 个 RecCard + refine 输入行 + 6 个 preset chips + skip footer
- `ReviewCard.tsx` — 只读卡片 + ReviewHeader + altScores 列表 + 跳 trace 按钮
- `RecCard.tsx` — 推荐卡 (rank/tags/price/title/why/meta/双按钮 + 可叠加调试层 L3 chip / L1/L2/L3 数字 / conflict pill)
- `TweaksPanel.tsx` — 浮层,timelineVariant / rightDensity / showDebugLayer / accent (4 swatch) / theme (light/dark)
- `panels/DPanel.tsx` — 紧凑/标准/详细 3 档密度,三段 (动作摘要 / 状态 diff / 下顿影响)
- `panels/APanel.tsx` — 长期画像 taste + L1 关键词
- `panels/BPanel.tsx` — 活跃规则 (refine + blacklist)。**B panel 文案: refine 块改成"当前 round 已应用: {label}",去掉 TTL 倒计时** (设计 brief scope 锁)。
- `panels/CPanel.tsx` — 近期窗口 (recent 4 顿 + fatigue 计数)
- `modals/SummaryDrawer.tsx` — 右抽屉 + history 列表 + 跳 trace
- `modals/ConfirmModal.tsx` — rollback/branch 二次确认,带文案预览 + name 输入 (branch)
- `modals/RefineModal.tsx` — handoff 保留,本任务先建空骨架

### App (apps/sandbox-lab/src/App.tsx)
- 拼装上述组件,全部喂 sbxMocks。
- 初始 selectedCell = null (决策模式), 不需要状态切换 (那是 S-03)。

### Hooks
- `useTweaks.ts` (S-01 已起,本任务扩成完整 5 字段: timelineVariant/rightDensity/showDebugLayer/accent/theme,localStorage 持久)
- 不实现 useSandbox / useKeyboard / useSandboxApi (S-03)

## Why

把原型的视觉/布局/CSS token/字体/暗色/紧凑模式完整搬到 TS 项目。下个任务 S-03 在这个静态视觉上接交互。

## Done When

- `npm run dev` 起 :5175,渲染:
  - TopBar + 14 格 timeline + 左 5 推荐卡 + 右 4 panel
  - 5 个 preset chips (6 个) + skip footer
  - mock decision 喂 DPanel 三段都渲出来 (含 diff 几行 + implications)
- chrome-devtools-mcp 验证:
  - 切 dark 主题:无遗漏硬编码色 (背景 / 文字 / 边框 / shadow 都跟着变)
  - 切 accent 4 套:按钮 / 选中态 / chip / ring / soft 背景全套同时变 (不只是 --accent)
  - 切紧凑密度:右栏 panel 真的更紧 (B/A/C compact summary, D panel 选 "紧凑" 信息量真的少)
  - 切 timelineVariant=calendar:timeline 显示日历卡视图
  - showDebugLayer=true:RecCard 上 L3 chip + reasoning 行展开 L1/L2/L3 数字 + conflict pill 可见
- mono 字体应用到: trace_id / L3 score / 数字 (`<span className="mono">`)
- console 无 error/warn (允许 React dev warning), network 无 4xx/5xx
- `npm run typecheck` 全绿

## Affected files

- 新建: `apps/sandbox-lab/src/{types/sandbox.ts, mocks/sbxMocks.ts, styles.css (扩), App.tsx (改), components/...}` (~15 个新 tsx 文件)
- 改: `src/main.tsx` (引入新 App), `src/hooks/useTweaks.ts` (扩 5 字段)
- 改: 现有 apps/* 任何文件 → 否

## 不做

- 不接交互逻辑 (eat/skip/refine/timeline 选格 等都没用,但按钮要在视觉上存在)
- 不接后端
- 不写 useKeyboard / useSandbox 等业务 hook