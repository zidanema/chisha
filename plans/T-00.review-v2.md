**Check 1: write_trace() bottom-fill order**

Relevant diff lines:

```diff
+        trace["__version"] = TRACE_SCHEMA_VERSION
+        # T-00: v2 兜底注入 l1.hard_filter_events = [] (Codex review blocker #1).
+        # api._build_trace / debug_recommend._build_l1_trace 上游不主动 init 时,
+        # 在写盘前保证字段存在; 上游已 append 的事件保留.
+        l1_block = trace.setdefault("l1", {})
+        if isinstance(l1_block, dict) and "hard_filter_events" not in l1_block:
+            l1_block["hard_filter_events"] = []
+        # session_id 反注入: 不允许 / 或 ..
```

Verdict: APPROVED.

Reason: The bottom-fill block is after `trace["__version"] = TRACE_SCHEMA_VERSION` and before the later write path. The assignment is guarded by `"hard_filter_events" not in l1_block`, so it only initializes the field when absent and preserves upstream appended events.

**Check 2: bottom-fill safety**

Relevant diff lines:

```diff
+        l1_block = trace.setdefault("l1", {})
+        if isinstance(l1_block, dict) and "hard_filter_events" not in l1_block:
+            l1_block["hard_filter_events"] = []
```

Verdict: APPROVED.

Reason: The previous BLOCKER was a misread. This guard is logically equivalent to `l1_block.setdefault("hard_filter_events", [])` for dict `l1_block` values: an existing `hard_filter_events` key is not overwritten. The extra `isinstance(l1_block, dict)` check avoids calling dict-only methods on non-dict `l1` values; it does not create an overwrite risk.

**Check 3: bool rejection coverage**

Relevant diff lines:

```diff
+    # bool 是 int 的子类, 显式排除 (Codex review NOTE).
+    if isinstance(dropped_count, bool) or not isinstance(dropped_count, int) or dropped_count < 0:
+        logger.warning("append_hard_filter_event: bad dropped_count=%r, skip", dropped_count)
+        return False
+    if isinstance(kept_count, bool) or not isinstance(kept_count, int) or kept_count < 0:
+        logger.warning("append_hard_filter_event: bad kept_count=%r, skip", kept_count)
+        return False
```

Verdict: PASS.

Reason: `append_hard_filter_event()` explicitly rejects bool values for both `dropped_count` and `kept_count` using `isinstance(..., bool)`.

Overall VERDICT: APPROVED
