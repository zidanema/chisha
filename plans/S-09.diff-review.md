## Iter 1

[FALLBACK: Claude Opus adversarial diff review — Codex unavailable (quota)]

Diff scope:
- `chisha/web_api.py` — `FullSnapshotResp` 加 `mealToTrace: dict / currentTraceId: str | None`; `_build_full_snapshot` 末尾派生填充.
- `apps/sandbox-lab/src/api/backend-types.ts` — `BackendFullSnapshot` 加两个字段.
- `apps/sandbox-lab/src/api/adapter.ts` — `SnapshotStorePiece` + `backendFullSnapshotToStore` 透传 (用 `|| null` 防空串).
- `apps/sandbox-lab/src/hooks/useSandbox.ts` — `SessionInternalState` 加字段; `initialSessionStates` mock 给 default; `applySnapshot` capture `recsResp.recommend_session_id`; `handleSwap`/`handleRefine`/`setActiveSessionIdWithEnsure` 同样 capture; return 加 `mealToTrace`/`currentTraceId`.
- `apps/sandbox-lab/src/App.tsx` — `openTrace(meal: Meal | null)` 实现 `window.open(:5174/?trace=&round=R1)`; 4 处调用点改造.
- `apps/debug-ui/src/hooks/useWaTrace.ts` — `readTraceFromUrl()` helper; activeTraceId 初值用 URL; bootstrap 在 urlTrace 存在时不 override.
- `apps/debug-ui/src/App.tsx` — `activeRound` 初值用 URL `?round=`.
- `tests/test_sandbox_rollback_branch.py` — 新加 `test_full_snapshot_includes_meal_to_trace_and_current_trace`.

Audit:

1. **Correctness (eat → polling → applySnapshot 链)**:
   - 后端 eat 完毕删 `last_recs.json` (web_api.py:1525), 异步 BG task 后. polling done → `getFullSnapshot` → `snap.currentTraceId=null` → `applySnapshot` 内 `recs=[]` 因为 last_recs 不存在 (snapshot 也读不到 currentRecs) → auto-postRecs 拉新 recs + capture `recsResp.recommend_session_id`. **正确**.
   - skip / rollback / branch 同链路.

2. **Correctness (swap/refine)**:
   - 后端 swap/refine 写新 `last_recs.json` + 返 `recommend_session_id`. 前端 handleSwap/handleRefine 把 `resp.recommend_session_id` 写进 currentTraceId. **正确**.

3. **Race (debug-ui URL deep-link)**:
   - `useWaTrace` 初值用 `readTraceFromUrl()`. bootstrap effect 内 urlTrace 一律 pin 住, 不 fallback list[0]. detail fetch 走 `/api/trace/{tsid}` authoritative. 404 落 mock fallback toast. **正确**.

4. **Regression surface**:
   - `FullSnapshotResp` 加字段, Pydantic default_factory + None, 现有 caller (S-07 rollback/branch endpoint + S-09 GET) 都通过 `_build_full_snapshot` 共享, 不破契约. typecheck pass + 933 tests pass.
   - debug-ui 已有的 `useEffect(() => { ... rounds.find(prev) ? prev : latest }, [activeTrace, roundsSignature, rounds])` 在 rounds 到达后会 keep URL round 若存在, 否则 fallback latest. spec 符合.

5. **Cross-file invariants (CONTRACTS)**:
   - line 110 "URL state 用 replaceState 不 push" → 仅读 URL, 不写. ✅
   - line 117 "debug-ui 独立 SPA :5174" → deep-link 跳 :5174. ✅
   - line 119 "D-087 SPA 100% read-only" → 加的只是 URL read. ✅

6. **Tests**:
   - 后端 1 新 test 覆盖 mealToTrace 空+currentTraceId, eat 后 mealToTrace 写 + currentTraceId 清空两种状态.
   - 全量 pytest 933 passed (含新 test).
   - frontend typecheck: 两个 SPA 全绿.
   - chrome-devtools-mcp e2e: **browser profile 锁住 (同 S-08), 没验**. 直说.

7. **Prod failure modes**:
   - 后端 `last_recs_data.get("recommend_session_id") or None` — 防 key 不存在 (返 None) + 防空串 (or None). ✅
   - 前端 `snap.currentTraceId || null` — adapter 防空串. ✅
   - mock 路径 (offline) currentTraceId=null + mealToTrace={}, `openTrace` 走 alert("该顿没有 trace"). UX 兜底 ✅.

Non-blocking observations (not counting against verdict):
- `if (urlTrace) { /* 不 override */ } else if (...)` 空 branch 风格丑但功能正确; ESLint 没报.
- `useState<string>(() => ...)` activeRound 初值闭包仅 mount 时跑, 后续 URL push 不响应 — spec 明确 "仅启动一次", by-design.

After adversarial pass I cannot find a blocking issue. Non-blocking: 空 if branch 风格, by-design URL re-read 缺失.

VERDICT: APPROVED
