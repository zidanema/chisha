# S-10 Plan · 文档收尾 + D-088 + memory 同步

risk: low (纯文档, 不改代码)

## Affected files (7 + 2 memory)

**注**: spec affected files 数 = 7, 超过 run-task.md ≤ 5 软上限. 但 S-10 本质是单一收口任务 —
所有改动都是 D-088 的下游同步 (decisions / contract / 红线 / app readme / handoff / brief
status / 项目根 README / memory). 每个文件改动极小 (1~30 行追加), 不动代码, 不动测试,
regression_risk=low. 拆 plan 反而割裂 D-088 收口完整性. 在此显式声明并请 codex 审.

1. `docs/decisions.md` — 尾部追加 D-088 (≤ 15 行硬约束)
2. `docs/CONTRACTS.md` — 新增 "## Sandbox Lab (D-088)" 段, 5 条约束, 每条 ≤ 10 行, 全文 < 200 行
3. `CLAUDE.md` — "常用命令" 加 sandbox-lab dev 启动 4 行 (总文件 ≤ 100 行)
4. `apps/sandbox-lab/README.md` — 替换 "当前状态" 段为完整版 (仿 debug-ui 结构, 不发明新 section)
5. `chidiansha-sandbox/project/sandbox-lab/HANDOFF_PROMPT.md` — 末尾追加 "## 落地状态 (2026-05-20)" 节
6. `docs/design_briefs/2026-05-19-sandbox-lab.md` — 顶部 status 改 "落地完成 / D-088 已落 decisions.md"
7. `README.md` — 项目根 §项目结构 加 `apps/sandbox-lab/` 一行
8. `~/.claude/projects/-Users-chisha/memory/chisha_sandbox_lab_d088.md` — 新建
9. `~/.claude/projects/-Users-chisha/memory/MEMORY.md` — 追加一行索引

## Step-by-step

### Step 1 · docs/decisions.md 追加 D-088

末尾追加 (按现有 D-087 格式), 严格 ≤ 15 行:

```
## D-088
**Sandbox Lab 白盒时光机落地 — apps/sandbox-lab 独立 SPA :5175。** (2026-05-20) · debug 沙箱
- 是什么: 7~14 天可回放/分支推荐沙箱; 多 session + branch/rollback 完整版
- scope 拍板: Refine 单 round (B panel 改文案 "当前 round 已应用"); 跨顿 TTL 留 v2
- 为何不并入 apps/debug-ui: 后者 D-087 read-only invariant 必须保留, 加可写交互会污染契约
- 共用后端: chisha.debug_server :8765 (新增 sandbox/* 端点, 见 S-04~S-08)
- 详见 brief: docs/design_briefs/2026-05-19-sandbox-lab.md
```

行数: 7 行 (≤ 15 ✓)

### Step 2 · docs/CONTRACTS.md 加 5 条 sandbox 约束

在 "调试台 (D-039 + D-075 + D-087)" 段之后, "范围红线 (Phase 0 内不做)" 之前, 插入新段:

```
## Sandbox Lab (D-088)

- **`sandbox_context` ContextVar 注入 sid, 不靠全局 active session。** 多 tab 并发 eat 不能串 session (D-088)。
- **`meal_to_trace.json` 是 sid → trace 唯一索引。** rollback / branch / trace 跳转都按此查, 不扫 recommend_trace 目录枚举。
- **D-077 旧签名兼容: `sandbox.{init,advance,reset,state,...}` 接受 `sid=None` 走 "_default" session。** 改签名前先检查老调用方。
- **`sessions/{sid}` 整目录拷贝/裁剪是 branch / rollback 的原子单位。** 必须同步重置 long_term_prefs / decisions / meal_to_trace / recommend_log / recommend_trace, 否则记忆跨分支泄漏。
- **Refine 单 round (Phase 0): backend 不维护跨顿 refine。** advance_meal 后 `last_recs.applied_refine` 清空; 跨顿 TTL 留 v2 (待 D-XXX)。
```

加完总行数: 134 + ~13 = ~147 (< 200 ✓), 每条 ≤ 10 行 ✓.

### Step 3 · CLAUDE.md 常用命令补 sandbox-lab

在 "常用命令" 块的 `cd apps/debug-ui && npm install && npm run dev` 行后, 紧接加:

```bash
# 新 sandbox-lab (D-088, 端口 5175, proxy /api → :8765)
cd apps/sandbox-lab && npm install && npm run dev  # http://127.0.0.1:5175
```

净增 3 行 (含空行), 总文件 91 + 3 = 94 (≤ 100 ✓).

### Step 4 · apps/sandbox-lab/README.md 完整化

现有 README 已经有结构 (项目定位 / 本地拉起 / Claude Code 自测约定 / dev query 参数). 缺的是:
- "当前状态" 段过时 (停在 S-02), 改为 D-088 落地完整态描述 (S-04~S-09 接入, mock + 真后端切换)
- 加 "数据流" 段 (仿 debug-ui: Backend sandbox/* endpoints → useSandbox hook → 主区组件)

不动: 本地拉起 / 多 worktree 端口冲突 / Claude Code 自测约定 / dev query 段 (这几段当前是对的).

### Step 5 · HANDOFF_PROMPT.md 加 "## 落地状态 (2026-05-20)" 节

文件末尾追加. 11 条逐条标态. 重要: chrome-devtools-mcp 在 S-08/S-09 因 browser
profile lock 没成功验, 必须如实写 "未验 (browser profile lock)", 不勾 ✓.

清单逐条 (引用 spec line 168-184):

```
## 落地状态 (2026-05-20)

设计 brief: `docs/design_briefs/2026-05-19-sandbox-lab.md` → D-088 已落 `docs/decisions.md`
主要 commits: S-01 (骨架) / S-02 (视觉) / S-03 (前端 mock 交互) / S-04~S-06c (后端 endpoints + L1 mutex + per-sid lock) / S-07 (rollback/branch) / S-08 (前后端联调) / S-09 (trace 跳转)

验收清单:
- [x] TopBar session 下拉切换 (S-08 useSandbox 改造)
- [x] "就这个 →" eat → history + clock+1 + 新推荐池 + transient banner 清空 (S-04 + S-08)
- [x] "跳过" skip → 推进时钟不学习 (S-05)
- [x] refine 输入 / preset chip → 推荐重排 + banner; 单 round 不持久化 (deviation: 跨顿 TTL 留 v2)
- [x] timeline 点过去某格 → 回顾卡 + D panel 切快照 (S-03 + S-09)
- [x] Esc 退出回顾 / 关 modal / drawer (S-03)
- [x] 回滚 → 二次确认 → history 裁剪 + clock 回位 (S-07)
- [x] 分支 → 二次确认 → 命名分支 + session 切换器 (S-07)
- [x] 主色切换 → accent 整套换 (S-02 4 套 ACCENT_PALETTE)
- [x] 深色主题 (S-02 dark-cool/dark-warm + light)
- [x] 推荐卡骨架与 chisha-user 一致 + 调试叠加层 (S-02)
- [-] chrome-devtools-mcp 自驱浏览器 e2e 验: **未验** — S-08 / S-09 均因 browser profile lock 失败 (志丹手测过 golden path); 待 lock 解除后补
- [x] trace 跳转 → debug-ui :5174/?trace= (S-09)

已知 deviation:
- Refine 单 round, B panel 文案 "当前 round 已应用" (非 "TTL=7 天"); 跨顿 TTL 持久化留 v2 (待 D-XXX)
- Tweaks 仅 `?dev=1` query 开启, 不暴露给最终用户

详见: docs/decisions.md D-088 + docs/CONTRACTS.md "Sandbox Lab (D-088)" 段
```

### Step 6 · brief status 转正

`docs/design_briefs/2026-05-19-sandbox-lab.md` 第 3 行:

旧: `> 状态:草稿 / 待 codex review · 创建 2026-05-19 · 志丹拍板 scope · 草稿期不进 docs/decisions.md`
新: `> 状态:落地完成 / D-088 已落 docs/decisions.md · 创建 2026-05-19 · 落地 2026-05-20`

### Step 7 · README.md 项目根加 sandbox-lab 索引

§项目结构 在 `├── apps/debug-ui/             # V12 DAG 调试台 SPA (D-075, 端口 5174)` 后加一行:

```
├── apps/sandbox-lab/          # 白盒时光机 sandbox SPA (D-088, 端口 5175)
```

不动其他段落 (README §项目状态 / Phase 0 表述本任务不重写, 留待 neat-freak 单独 pass).

### Step 8 · memory 新建

新建 `~/.claude/projects/-Users-chisha/memory/chisha_sandbox_lab_d088.md`,
仿 chisha_chrome_devtools_mcp.md frontmatter 结构.

### Step 9 · MEMORY.md 加索引

末尾追加一行:
```
- [D-088 Sandbox Lab](chisha_sandbox_lab_d088.md) — apps/sandbox-lab :5175 独立 SPA, 多 session + branch/rollback, refine 单 round
```

## Test strategy

- 不动代码, 不动测试. 但 run-task.md Phase 3 强制全量 pytest. 跑 `uv run pytest tests/ -q` 确认 0 regression (S-09 已通过 baseline, 本任务只改 md)
- 不跑 baseline_l2_snapshot (无打分链路改动)
- 不跑 chrome-devtools-mcp (无前端代码改动)
- 内链可达性: grep 验证 `docs/design_briefs/2026-05-19-sandbox-lab.md` / `chidiansha-sandbox/project/sandbox-lab/HANDOFF_PROMPT.md` 存在

## Regression risk + rollback

- risk: low (纯文档)
- rollback: `git revert <S-10 commit>` 即可, 无 schema / 数据 / 端点变更

## Constraint summary

| 约束 | 限额 | 本 plan 落点 |
|---|---|---|
| decisions.md ≤ 15 行/D | 15 | D-088: 7 行 ✓ |
| CONTRACTS.md 单条 ≤ 10 行 | 10 | 5 条均 1~2 行 ✓ |
| CONTRACTS.md 全文 ≤ 200 行 | 200 | 134 → ~147 ✓ |
| CLAUDE.md 全文 ≤ 100 行 | 100 | 91 → 94 ✓ |

## Changelog

### Iter 1 (post codex APPROVED, 4 LOW 全接受)

- **I1 (HANDOFF in exclude)**: §Step 5 加一句"`chidiansha-sandbox/` 在 `.git/info/exclude` 第 10 行, 整目录不进 git commit. 本步改动为本地设计稿归档, 与 D-088 收口的 git history 并存".
- **I2 (mono-font 漏)**: §Step 5 验收清单从 13 条 → 14 条, 补 `[x] 数字 / trace id / L3 分数使用 JetBrains Mono (S-02)`.
- **I3 (README §文档体系 表格不对称)**: §Step 7 除 §项目结构 加目录行外, 同步在 §文档体系 表格加 `apps/sandbox-lab/README.md` 条目.
- **I4 (CLAUDE.md §前端自测 漏 :5175)**: §Step 3 除补常用命令外, 同步在 §前端自测 段更新 ":5173 / :5174 / :5175" 端口列表 (单行 in-place 编辑, 不增段). CLAUDE.md 全文行数: 91 → 94 (Step 3 常用命令 +3 行) → 94 (§前端自测 in-place 改动 ±0). 仍 ≤ 100 ✓.
