# T-P1a-02 Plan Audit v2

## Issue 1

Status: RESOLVED

Quoted evidence from revised plan:
- "`**Codex blocker #2 修正**: 30 是 floor, 不是 ceiling。L3 期待 60 输入,要努力填到 `L3_INPUT_TOP_K=60`。`"
- "`target = L3_INPUT_TOP_K`  # 默认 60"
- "`floor = 30`               # brief §6 \"至少 30 个 intent 命中\""
- "`**Level 1 (精确)**: `intent_hit ≥ floor` → 返 exact + soft + rest 拼到 target (intent 命中放前,rest 兜底; rest 数量上限不再卡 30)`"
- "`**Level 2 (同源扩展)**: `floor > intent_hit ≥ 10` → 同 Level 1 (返 exact + soft + rest 至 target), 但 trace 标记 \"intent_hit_below_floor\" 警示`"
- "`**关键差别**: Level 1 vs 2 vs 3 不影响实际 combo 列表大小 (都尽量填到 60), 仅影响事件 label + 后续可观测性.`"

Verdict: The prior blocking concern is addressed. The plan now treats 30 as a floor and explicitly fills all levels toward `L3_INPUT_TOP_K=60`, with level differences limited to observability.

## Issue 2

Status: RESOLVED

Quoted evidence from revised plan:
- "`### Step 5 · 测试 (≥9 e2e)`"
- "`1. `test_recall_empty_intent_uses_default_per_rest_max`: intent=None → per_rest_max=3 (旧行为)`"
- "`2. `test_recall_with_intent_uses_refine_per_rest_max`: intent 非空 → per_rest_max=5 (新行为)`"
- "`3. `test_ingredient_want_reverse_lookup_pulls_in_dishes`:`"
- "`fixture: 1 个餐厅 6+ 道菜 sequential dish_id, 全部 main_ingredient_type=红肉`"
- "`profile.recall.per_restaurant_max=3 (默认), refine_per_restaurant_max=5`"
- "`intent.ingredient_want=[\"肉\"]`"
- "`断言: 反查后 dishes 池含原本被 [:3] 截断之外的 ingredient_want 菜`"
- "`4. `test_ingredient_want_reverse_lookup_no_duplicate_events`:`"
- "`反查路径触发 L0-A allergy → 事件只写一次, 不双重 (Codex check #5)`"
- "`用 mock 验证: 反查不直接调 hard_filter()`"
- "`11. **Codex check #5 补**: `test_per_rest_max_5_does_not_break_brand_cap`: refine 5 个 combo 同品牌 → apply_caps brand cap=2 仍砍到 2`"

Verdict: The test plan is now concrete enough for the previously under-specified fixture. It also adds explicit coverage for duplicate L0 event prevention, no `hard_filter()` call from reverse lookup, and brand-cap interaction.

## Issue 3

Status: RESOLVED

Quoted evidence from revised plan:
- "`D-073/D-073.1 cap 行为不动 (本任务不碰 score.py apply_caps)`"
- "`**per_brand_top_k=2 限制 ingredient_want 命中数** | brief §6 已说\"per_restaurant_max 5-8\" 但 brand cap 不在本任务范围 (`不动 apply_caps`). 接受局限:同品牌的 ingredient 命中仍被 cap 2. T-P2-02 簇式输出处理此 (Codex check #1 warn)`"
- "`不动 L2 weight / apply_caps (T-P1b 才动)`"

Verdict: The plan now explicitly acknowledges the `per_brand_top_k=2` limitation and defers changing `apply_caps`. This resolves the prior warning as a documented non-goal rather than an unaccounted interaction.

## Issue 4

Status: RESOLVED

Quoted evidence from revised plan:
- "`# T-P1a-02: refine 模式参数差异化 (Codex blocker #4 修正: 严格判定不用 hasattr).`"
- "`# refine.py 已经传 None 当空, 这里 if intent is None: 即可保 baseline 0 diff.`"
- "`has_intent = intent is not None and not intent.is_empty()`"
- "`intent=intent if not intent.is_empty() else None,`"

Verdict: The guard no longer uses the permissive `hasattr` fallback. The revised condition matches the requested strict non-empty intent check while preserving the empty-refine `None` path.

VERDICT: APPROVED
