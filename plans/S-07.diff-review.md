## Iter 1

Blocking issues:

1. `chisha/web_api.py:1278` / `chisha/web_api.py:1327` — `/api/sandbox/sessions/{sid}/recs` is still an unlocked writer while rollback is locked at `chisha/web_api.py:2186`. Concrete failure mode: request A enters `/recs`, reads `current_meal_idx` before rollback at `chisha/web_api.py:1291`-`chisha/web_api.py:1295`, then request B rolls back and deletes `last_recs.json` via the rollback action list at `chisha/web_api.py:2088`-`chisha/web_api.py:2089`; when request A resumes, it writes a stale `last_recs.json` with the old meal index at `chisha/web_api.py:1318`-`chisha/web_api.py:1327`. The next `/eat` does not verify `last_recs["meal_idx"]` against current state before accepting it (`chisha/web_api.py:1453`-`chisha/web_api.py:1480`), so rollback to idx=2 can consume recommendations generated for pre-rollback idx=4 and reattach the old/future trace id to the new idx.

2. `chisha/web_api.py:2033`-`chisha/web_api.py:2047` — rollback only collects trash trace/session ids from `meal_to_trace.json`, but `last_recs.json` is deleted later without first adding its `recommend_session_id` to `trash_tsids` (`chisha/web_api.py:1885`-`chisha/web_api.py:1894`, `chisha/web_api.py:2088`-`chisha/web_api.py:2089`). Concrete failure mode: on a real session, `/recs` creates a D-039 session file (`chisha/api.py:167`-`chisha/api.py:170`), appends `recommend_log.jsonl` (`chisha/api.py:219`-`chisha/api.py:225`), and writes a trace when `persist_trace` is enabled (`chisha/api.py:227`-`chisha/api.py:260`) before the meal is eaten, while `meal_to_trace` is only updated during `/eat` (`chisha/web_api.py:1479`-`chisha/web_api.py:1480`). Rolling back with an uneaten current recommendation therefore deletes `last_recs.json` but leaves that future recommendation's log row, trace, and session file in the rolled-back bucket, violating the S-07 requirement to delete future `sessions/recommend_log/meal_log/trace` state (`docs/design_briefs/2026-05-19-sandbox-lab.md:126`).

3. `chisha/web_api.py:1947`-`chisha/web_api.py:1963` — `_filter_jsonl_by_session_ids` swallows `OSError` and returns an empty string, and rollback then stages that empty content for `meal_log.jsonl` and `recommend_log.jsonl` at `chisha/web_api.py:2070`-`chisha/web_api.py:2074`. Concrete failure mode: if reading either log raises a transient `OSError` (permission error, short read, or filesystem error), rollback does not fail and restore; it commits empty replacement logs through the action list at `chisha/web_api.py:2085`-`chisha/web_api.py:2087` and `chisha/web_api.py:2128`-`chisha/web_api.py:2132`, then commits `state.json` last at `chisha/web_api.py:2111`-`chisha/web_api.py:2112`. That silently drops retained pre-rollback meal/recommend history despite S-07 requiring any failed step to roll back or use a single atomic commit (`specs/S-07.md:13`-`specs/S-07.md:33`).

4. `chisha/web_api.py:2104`-`chisha/web_api.py:2110` — rollback deletes v2 trace files and v3 trace directories but never deletes the matching D-039 recommend session JSON files under `data_root.sessions_dir`, even though those files are created for every real recommendation at `chisha/api.py:167`-`chisha/api.py:170` and stored under the sandbox-aware sessions directory (`chisha/session.py:55`-`chisha/session.py:62`, `chisha/data_root.py:107`-`chisha/data_root.py:121`). Concrete failure mode: branch copies the whole source bucket first (`chisha/web_api.py:2234`-`chisha/web_api.py:2238`) and then runs rollback on the new bucket (`chisha/web_api.py:2243`-`chisha/web_api.py:2248`); future recommendation session files for idx >= `from_meal_idx` remain in the branch, so code paths that can load a known session id, such as refine via `load_session` (`chisha/web_api.py:1726`-`chisha/web_api.py:1733`), can still access future candidates copied from the source branch.

VERDICT: BLOCKED

## Iter 2

### Issue Closure Verification

1. CLOSED — `/api/sandbox/sessions/{sid}/recs` validates localhost/sid first, then wraps the full state read, recommendation generation, `last_recs.json` write, and return inside `_session_op_lock(routed_sid, action="recs")` at `chisha/web_api.py:1291`-`chisha/web_api.py:1337`. I found no write or return path after sid routing that bypasses the lock.

2. CLOSED — `_rollback_session_impl` now reads `last_recs.json`, extracts `recommend_session_id` and `meal_idx`, and adds the id to `trash_tsids` when `lr_idx >= meal_idx` at `chisha/web_api.py:2053`-`chisha/web_api.py:2070`. The mock skip is `not lr_sid.startswith("mock_")` at `chisha/web_api.py:2066`-`chisha/web_api.py:2068`; under the current real id generator, real ids are `YYYYMMDD_{meal_type}_{hex}` at `chisha/api.py:27`-`chisha/api.py:34`, so they cannot start with `mock_`. An artifact-existence check would be more future-proof if session-id formats change, but the current condition is correct for the current generator.

3. CLOSED — `_filter_jsonl_by_session_ids` no longer catches `OSError`; the uncaught `src.read_text(...)` call is at `chisha/web_api.py:1947`-`chisha/web_api.py:1951`. It propagates through `_rollback_session_impl` calls at `chisha/web_api.py:2095`-`chisha/web_api.py:2096` to the rollback endpoint catch-all at `chisha/web_api.py:2214`-`chisha/web_api.py:2222`, which returns HTTP 500. Because this happens before the commit-action `try` starts at `chisha/web_api.py:2144`, no backup restore is needed on this path; no target files have been replaced yet.

4. CLOSED — rollback now adds D-039 recommend session JSON files to the delete action list as kind `"delete"` at `chisha/web_api.py:2135`-`chisha/web_api.py:2138`. The target path comes from `data_root.sessions_dir(root)` while the sandbox ContextVar is set at `chisha/web_api.py:2084`-`chisha/web_api.py:2092`; this matches writes via `chisha.session._session_path`, which uses `data_root.sessions_dir(root) / f"{session_id}.json"` at `chisha/session.py:53`-`chisha/session.py:62` and `save_session` at `chisha/session.py:84`-`chisha/session.py:90`.

### Fresh Adversarial Findings

1. BLOCKER — reset/disable no longer fully satisfy the Sandbox contract's L1 mutex invariant for background sandbox workers. `_build_decision_async` marks the job running, acquires the per-sid lock, reloads state, and only then attempts `_impl_l1_refresh(..., acquire_lock=True)` at `chisha/web_api.py:1363`-`chisha/web_api.py:1405`. Reset/disable only probe `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:2331`-`chisha/web_api.py:2344` before deleting/disabling sandbox state at `chisha/web_api.py:2367`-`chisha/web_api.py:2384`, and `CONTRACTS.md` requires reset/disable to block until L1 workers cannot write into prod at `docs/CONTRACTS.md:93`-`docs/CONTRACTS.md:101`. Confirmed race: a background job can hold the per-sid lock and be between the state guard and `_L1_EXTRACTION_LOCK` acquisition while reset/disable acquires/releases `_L1_EXTRACTION_LOCK` and proceeds; the worker can then acquire `_L1_EXTRACTION_LOCK` after sandbox was reset/disabled. Recommended fix: make background jobs acquire `_L1_EXTRACTION_LOCK` before entering any active worker window that reset/disable relies on, or make reset/disable also quiesce/cancel all per-sid sandbox jobs/locks before mutating sandbox lifecycle state.

2. WARNING — the OSError propagation fix is not directly regression-tested. `test_rollback_clears_uneaten_recs_artifacts` covers happy-path deletion of uneaten D-039/trace artifacts at `tests/test_sandbox_rollback_branch.py:376`-`tests/test_sandbox_rollback_branch.py:426`, and the atomicity tests inject failures during commit actions at `tests/test_sandbox_rollback_branch.py:259`-`tests/test_sandbox_rollback_branch.py:332`. I found no test that forces `_filter_jsonl_by_session_ids` / `src.read_text(...)` at `chisha/web_api.py:1947`-`chisha/web_api.py:1951` to raise before staging completes. Recommended fix: add a targeted rollback test that monkeypatches the log path read to raise `OSError`, asserts HTTP 500, and asserts `state.json`, `meal_log.jsonl`, and `recommend_log.jsonl` remain unchanged.

3. INFO — no recursive per-sid lock acquisition was found on the refine path. The lock is a plain non-reentrant `threading.Lock` at `chisha/web_api.py:1113`-`chisha/web_api.py:1139`; `api_sandbox_refine` holds it at `chisha/web_api.py:1707`-`chisha/web_api.py:1736`, then `_impl_refine` loads the D-039 session directly with `load_session` at `chisha/web_api.py:170`-`chisha/web_api.py:175` and `chisha/refine.py:111`-`chisha/refine.py:115`, with saves through `save_session` at `chisha/refine.py:275`-`chisha/refine.py:279`. I found no call from this path back into `_session_op_lock`.

4. INFO — `last_recs` rollback inclusion is correctly inclusive at the rollback target: `lr_idx >= meal_idx` at `chisha/web_api.py:2065`-`chisha/web_api.py:2070`, so `lr_idx == meal_idx` is trashed. The code handles only the single `last_recs.recommend_session_id` field at `chisha/web_api.py:2059`-`chisha/web_api.py:2070`; this matches the current `/recs`, `/swap`, and `/refine` overwrite model where each endpoint writes one replacement payload at `chisha/web_api.py:1321`-`chisha/web_api.py:1330`, `chisha/web_api.py:1673`-`chisha/web_api.py:1682`, and `chisha/web_api.py:1758`-`chisha/web_api.py:1767`.

5. INFO — D-039 session-file path construction matches the actual nested sandbox path. `data_root.sessions_dir` documents non-default sandbox output as `logs/sandbox/sessions/{sid}/sessions/` at `chisha/data_root.py:107`-`chisha/data_root.py:121`; branch rollback calls `_rollback_session_impl(new_sid, ...)` at `chisha/web_api.py:2270`-`chisha/web_api.py:2276`, and `_rollback_session_impl` sets the sandbox ContextVar before deriving `sessions_dir_target` at `chisha/web_api.py:2084`-`chisha/web_api.py:2092`, so new-branch deletion targets `logs/sandbox/sessions/{new_sid}/sessions/{tsid}.json`.

6. INFO — full pytest could not be run in this sandbox. Command attempted: `uv run pytest tests/ -q --tb=short 2>&1 | tail -30`; it failed before test collection with `failed to open file ~/.cache/uv/sdists-v9/.git: Operation not permitted`.

### Verdict

VERDICT: BLOCKED
  1. reset/disable can pass the L1 idle check while a background sandbox worker is active but has not yet acquired `_L1_EXTRACTION_LOCK`: `chisha/web_api.py:1363`-`chisha/web_api.py:1405`, `chisha/web_api.py:2331`-`chisha/web_api.py:2384`, `docs/CONTRACTS.md:93`-`docs/CONTRACTS.md:101`

## Iter 3

1. FAIL — Iter 2 #1 is only closed for lock order inside `_build_decision_async`, not for the reset/disable lifecycle invariant. BG sets the job to `"running"` before taking `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:1363`-`chisha/web_api.py:1377`, then takes `_L1_EXTRACTION_LOCK` before `_session_op_lock` at `chisha/web_api.py:1377`-`chisha/web_api.py:1378`; no later BG path skips that order before the stale guard or L1 extraction at `chisha/web_api.py:1379`-`chisha/web_api.py:1411`. However reset/disable only acquire and immediately release `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:2349`-`chisha/web_api.py:2352` before calling reset/disable at `chisha/web_api.py:2387`-`chisha/web_api.py:2396`, so a pending BG task can acquire the lock after the probe and run while sandbox is being reset/disabled.

2. PASS — Re-entrant deadlock avoidance is correct at the BG `_impl_l1_refresh` call site. `_impl_l1_refresh(acquire_lock=True)` would recursively enter `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:846`-`chisha/web_api.py:848`; Iter 3 calls it with `acquire_lock=False` while already under the outer `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:1377` and `chisha/web_api.py:1406`-`chisha/web_api.py:1411`. The only `_build_decision_async` scheduling site is `/eat` at `chisha/web_api.py:1538`-`chisha/web_api.py:1547`, and `rg` found no other source call path that still uses `acquire_lock=True` in BG context.

3. PASS — Test #11c adequately covers the claimed OSError staging failure behavior. It snapshots `state.json` and `meal_log.jsonl` before rollback at `tests/test_sandbox_rollback_branch.py:440`-`tests/test_sandbox_rollback_branch.py:442`, monkeypatches `Path.read_text` to raise `OSError` for the sandbox `meal_log.jsonl` at `tests/test_sandbox_rollback_branch.py:444`-`tests/test_sandbox_rollback_branch.py:453`, asserts HTTP 500 at `tests/test_sandbox_rollback_branch.py:455`-`tests/test_sandbox_rollback_branch.py:459`, and verifies state, meal log, and `.rollback_tmp` cleanup at `tests/test_sandbox_rollback_branch.py:460`-`tests/test_sandbox_rollback_branch.py:465`. The implementation also catches staging `OSError`, removes `tmp_dir`, and re-raises before commit actions at `chisha/web_api.py:2102`-`chisha/web_api.py:2110`.

4. WARN — No AB-BA deadlock cycle exists among the named locks, but the lifecycle mutex is still not held through reset/disable mutation. Rollback holds only the per-sid lock at `chisha/web_api.py:2224`-`chisha/web_api.py:2228` and its staging/commit path does not acquire `_L1_EXTRACTION_LOCK` at `chisha/web_api.py:1993`-`chisha/web_api.py:2213`; BG acquires `_L1_EXTRACTION_LOCK` first and then the per-sid lock at `chisha/web_api.py:1377`-`chisha/web_api.py:1378`; reset/disable probe `_L1_EXTRACTION_LOCK` with timeout at `chisha/web_api.py:2343`-`chisha/web_api.py:2356`. This avoids a per-sid/L1 AB-BA cycle, but because `_block_until_l1_idle_or_409` releases the lock before `_sandbox.reset` / `_sandbox.disable`, the D-077 reset/disable protection is still vulnerable to a queued worker entering after the probe.

5. FAIL — Cross-file D-077/D-078 compliance is incomplete. `docs/CONTRACTS.md:98` requires reset/disable to take `_L1_EXTRACTION_LOCK` so workers cannot save prefs into prod, and `docs/CONTRACTS.md:99` identifies `meal_log.jsonl` as source-of-truth; the rollback OSError path now preserves `meal_log.jsonl` by re-raising before commit at `chisha/web_api.py:2102`-`chisha/web_api.py:2110`, but reset/disable still releases the L1 mutex before lifecycle mutation at `chisha/web_api.py:2349`-`chisha/web_api.py:2352` and then mutates at `chisha/web_api.py:2387`-`chisha/web_api.py:2396`. If sandbox is disabled, ctx-only data paths fall back to prod at `chisha/data_root.py:81`-`chisha/data_root.py:89`, so a BG task entering after the probe can still hit the pollution mode the contract is trying to prevent.

6. PASS — Test #11 and #11c setup do not have a false-positive race for the audited behavior. The fixture clears `_JOB_TABLE` and `_SESSION_OP_LOCK_REGISTRY` per test at `tests/test_sandbox_rollback_branch.py:29`-`tests/test_sandbox_rollback_branch.py:31`; #11 holds the exact `s1` lock in a separate thread, signals after acquisition, shortens the handler timeout, and asserts rollback returns 409 at `tests/test_sandbox_rollback_branch.py:480`-`tests/test_sandbox_rollback_branch.py:510`. #11c uses a deterministic path/name predicate for the log read failure at `tests/test_sandbox_rollback_branch.py:448`-`tests/test_sandbox_rollback_branch.py:450` and undoes the monkeypatch before filesystem assertions at `tests/test_sandbox_rollback_branch.py:457`-`tests/test_sandbox_rollback_branch.py:465`.

VERDICT: BLOCKED
  1. reset/disable still only probe `_L1_EXTRACTION_LOCK` and release it before sandbox lifecycle mutation, so a queued `_build_decision_async` can acquire the lock after the probe and run with sandbox reset/disabled: `chisha/web_api.py:1363`-`chisha/web_api.py:1378`, `chisha/web_api.py:2349`-`chisha/web_api.py:2352`, `chisha/web_api.py:2387`-`chisha/web_api.py:2396`, `chisha/data_root.py:81`-`chisha/data_root.py:89`, `docs/CONTRACTS.md:98`.

## Iter 4

1. **SAFE** — `chisha/web_api.py:2360`-`chisha/web_api.py:2375`, `chisha/web_api.py:2398`-`chisha/web_api.py:2408`, `chisha/web_api.py:2411`-`chisha/web_api.py:2419`: `_l1_extraction_lock_or_409` now holds `_L1_EXTRACTION_LOCK` across the exact reset body `with _l1_extraction_lock_or_409("reset"):` / `return _sandbox.reset(root=ROOT)` and the exact disable body `with _l1_extraction_lock_or_409("disable"):` / `return _sandbox.disable(root=ROOT)`, so no code between acquire and `_sandbox.reset()` / `_sandbox.disable()` escapes the lock.

2. **SAFE** — `chisha/web_api.py:1113`-`chisha/web_api.py:1139`, `chisha/web_api.py:1377`-`chisha/web_api.py:1378`, `chisha/web_api.py:2200`-`chisha/web_api.py:2211`, `chisha/web_api.py:2224`-`chisha/web_api.py:2228`, `chisha/web_api.py:2366`-`chisha/web_api.py:2375`, `chisha/web_api.py:2407`-`chisha/web_api.py:2418`: acquisition sequences are BG `_JOB_LOCK` briefly then `_L1_EXTRACTION_LOCK` then per-sid, rollback per-sid then `_JOB_LOCK` briefly, reset/disable `_L1_EXTRACTION_LOCK` only, and branch source per-sid then new per-sid, so BG-holds-L1/reset-waits and reset-holds-L1/BG-waits are blocking waits with no AB-BA cycle and no third lock is acquired in reverse order with L1.

3. **SAFE** — `tests/test_sandbox_rollback_branch.py:472`-`tests/test_sandbox_rollback_branch.py:505`, `tests/test_sandbox_rollback_branch.py:508`-`tests/test_sandbox_rollback_branch.py:534`: `UV_CACHE_DIR=/private/tmp/chisha-uv-cache XDG_CACHE_HOME=/private/tmp/chisha-xdg-cache uv run pytest tests/ -q` passed with `932 passed, 1 skipped, 5 deselected`; `test_sandbox_clock.py`, `test_sandbox_context.py`, and `test_sandbox_layout_default_compat.py` passed together as `57 passed`, and the two new tests `test_reset_blocked_by_l1_lock_held` plus `test_reset_holds_l1_lock_through_mutation` passed as `2 passed`.

4. **VIOLATION** — `chisha/sandbox.py:573`-`chisha/sandbox.py:592`, `chisha/web_api.py:1153`-`chisha/web_api.py:1157`, `chisha/web_api.py:2007`-`chisha/web_api.py:2015`, `chisha/web_api.py:2078`-`chisha/web_api.py:2109`, `chisha/web_api.py:2117`-`chisha/web_api.py:2174`, `chisha/web_api.py:2224`-`chisha/web_api.py:2228`, `chisha/web_api.py:2407`-`chisha/web_api.py:2408`: a real reset-vs-rollback race remains because reset deletes `logs/sandbox` with `shutil.rmtree(d)` while rollback, which only holds the per-sid lock, stages `.rollback_tmp` and replaces targets under `logs/sandbox/sessions/{sid}`, so reset can delete paths rollback is writing or rollback can recreate sandbox paths after reset.

5. **SAFE** — `docs/CONTRACTS.md:93`-`docs/CONTRACTS.md:101`, `chisha/data_root.py:81`-`chisha/data_root.py:93`, `chisha/web_api.py:837`-`chisha/web_api.py:842`, `chisha/web_api.py:1377`-`chisha/web_api.py:1411`, `chisha/web_api.py:1491`-`chisha/web_api.py:1504`, `chisha/web_api.py:2398`-`chisha/web_api.py:2419`, `chisha/web_api.py:2603`-`chisha/web_api.py:2651`: invariants 93-101 are satisfied by the iter 4 diff for sandbox-as-real-web-mode/data isolation, L1 root propagation, reset/disable L1 locking, hard-failing accept meal-log writes, and inspect raw prefs; invariant 96 time injection is not touched by the one-file diff, and no iter 4 violation was found against those contract lines.

6. **SAFE** — changed file list from `git diff --name-only` is exactly `chisha/web_api.py`, with no output for `git diff --name-only -- chisha/refine chisha/rerank apps/web/src` and no `refine`-path match; the existing refine endpoint remains in `chisha/web_api.py:1702`-`chisha/web_api.py:1775`, but no file under `chisha/refine`, `chisha/rerank`, `apps/web/src`, or a refine-related path was modified.

VERDICT: BLOCKED 4

## Iter 5

(Codex output appended manually — Codex sandbox was read-only.)

1. **PASS** — Rollback acquires L1 before per-sid at chisha/web_api.py:2231-2234; rollback FS writes start later. Reset acquires L1 before `_sandbox.reset()`. Race from Iter 4 #4 is closed.
2. **PASS** — Lock acquisition order with no AB-BA: BG (L1→per-sid), Rollback (L1→per-sid), Branch (L1→src-sid→new-sid), Reset/disable (L1 only), eat/skip/swap/refine (per-sid only).
3. **PASS** — `test_reset_blocked_by_l1_lock_held` still valid. TestClient sequential, no self-deadlock.
4. **PASS** — No refine code path touched.
5. **PASS** — Consistent with docs/CONTRACTS.md:95 + :98.
6. **PASS** — No TOCTOU in branch handler (state read after both locks held).

VERDICT: APPROVED
