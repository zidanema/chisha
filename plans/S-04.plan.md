# S-04 实施 Plan v3 · sandbox_context + data_root session_id 显式参

> **任务**: 让 backend 永远显式 sid,不靠全局 active 标记,两 tab 并发 eat 不串。
> **设计源**: `specs/S-04.md` + `docs/design_briefs/2026-05-19-sandbox-lab.md`
> **regression_risk**: high — 触 `chisha/{sandbox, data_root, clock, sandbox_context}`
> **不动**: 推荐链路 (api/recall/score/rerank/refine/l1_extractor) / web_api 端点签名 / apps/sandbox-lab / apps/web / apps/debug-ui

---

## v2 → v3 修订 (回应 Codex 审计第 2 轮)

| Codex v2 反馈 | v3 处理 |
|---|---|
| #1: threading test 只是文档,不能 catch S-06a worker 漏 sid | (a) 把 `test_threading_does_not_propagate` 改名 `test_ctx_does_not_leak_into_threads` 并明确标注"documentation only";(b) 新增 **真守门** `test_copy_context_propagates_sid` — 验 `contextvars.copy_context().run(...)` 内 worker 能读到 sid;(c) 新增 `test_explicit_sid_kwarg_overrides_ctx_for_threading` — 验 worker 显式收 sid kwarg 也能落对路径。这两条 test 是 S-06a wrap web_api 时的契约样板,任何 S-06a 实现违反就 break。 |
| #2: sid validation 用 `fullmatch` + reject `_legacy` | `_validate_sid` 改 `_SID_PATTERN.fullmatch(sid)` (whole-string match,拒尾 `\n`);加 `_RESERVED_SIDS = {"_legacy"}` 黑名单(`_default` 白名单已有);测试加 `test_legacy_sid_rejected` |

## v1 → v2 主要修订 (回应 Codex 审计第 1 轮)

| Codex 反馈 | v1 处理 | v2 修订 |
|---|---|---|
| #1: spec §1 行为矩阵让 `_default` 落 `sessions/_default/` 子目录,但 web_api 端点没 ctx wrap → 等效"flat sid",不如就让 `_default` = flat | 路径变 `sessions/_default/{rel}` (与 spec §1 行为矩阵字面) | **改为 `_default` 即 flat path `logs/sandbox/{rel}`**;仅当显式 sid ≠ `_default` 才进 `sessions/{sid}/` 子树。理由:spec §4 + spec §"不做" 都说 S-04 只扩签名不动落盘结构;§1 行为矩阵被 §4 收紧 |
| #2: `_copy_real_data_to_sandbox` 写 flat 路径 → 与 reader 不一致 | 未考虑 | v2 在 `_default` flat 方案下**不需要改**(reader 也是 flat),copy_real_data 零变化 |
| #3: `test_web_api_sandbox.py` / `test_trace_store.py` / `test_sandbox_clock.py` 断言 flat sandbox 路径 | v1 没列 | v2 全部保持 flat → 旧测试 **真 zero-touch pass** |
| #4: ContextVar 不跨 threading.Thread | v1 没处理 | v2 显式: S-04 不 wrap web_api,后台 worker 拿 None ctx → 走 `_default` → flat → 与 D-077 当前行为一致。**S-06a 才需要 `copy_context()` 或显式 sid 参** — 加注释守门 |
| #5: sid 未 sanitize | 提了但未实现 | v2 加 `_validate_sid()`:`^[a-zA-Z0-9_-]{1,64}$`,违规 raise `ValueError`;`_default` 是合法 sid |
| #6: `sessions_dir` 命名冲突 (sandbox sid vs recommend sid) | 未提 | v2 明确:`data_root.sessions_dir()` 返**外层目录**(放 recommend session json);sandbox sid `≠ _default` 时为 `logs/sandbox/sessions/{sid}/sessions/`(嵌套),`_default` 时为 `logs/sandbox/sessions/`(flat)。命名虽 awkward 但 spec D-077 已定型,S-04 不重命名 |
| #7: 总结安全替代 | — | v2 全采纳 |

---

## 1. 现状盘点

### 1.1 implicit session 位置 (今天)

| 模块 | 全局/隐式状态 |
|---|---|
| `chisha/sandbox.py` | `logs/sandbox/state.json` 单一文件 |
| `chisha/data_root.py` | `_maybe_sandbox` flat 落 `logs/sandbox/{rel}` |
| `chisha/clock.py` | 读 `sandbox.current_date(root)` 单 state |
| `chisha/sandbox_context.py` | **不存在** |

### 1.2 完整 caller 图 (grep 验证)

**`data_root.*_path` 的所有 in-chisha caller**:

| caller | path helper |
|---|---|
| `chisha/web_api.py:40` | `profile_path(ROOT)` |
| `chisha/web_api.py:449` | `recommend_log_path(ROOT)` |
| `chisha/web_api.py:1076` | `long_term_prefs_path(ROOT)` |
| `chisha/recall.py:63,105` | `meal_log_path(root)` |
| `chisha/l1_prefs.py:75` | `long_term_prefs_path(root)` |
| `chisha/session.py:56` | `sessions_dir(root)` |
| `chisha/trace_store.py:125,160,226,479,484,498,858` | `recommend_trace_dir(root)` |
| `chisha/api.py:121,222` | `profile_path / recommend_log_path` |
| `chisha/long_term_prefs.py:74` | `feedback_history_path(root)` |
| `chisha/feedback_store.py:50` | `feedback_store_path(root)` |
| `chisha/web_api.py:755-787` | `_copy_real_data_to_sandbox` (直写 `root / "logs" / "sandbox" / ...`,不走 helper) |

**所有 caller 都不传 sid**。v2 默认 sid → ctx → `_default` → **flat path (与今天等同)**,所有 caller zero-change。

**`sandbox.*` API caller**:同 v1,加 sid kwarg 后 `_default`/`None` 走 D-077 单 state.json,旧 caller zero-change。

### 1.3 cross-file invariants (docs/CONTRACTS.md)

CONTRACTS L95-98, L106-109 全部 sandbox-only — v2 因 `_default` = flat → 无 sandbox 行为变更 → 所有 invariants 自动保持。

---

## 2. 实施方案 v2

### Step 0: baseline 快照 (改代码前)

```bash
uv run python -m scripts.baseline_l2_snapshot --out-dir tmp/baseline_traces_before
```

### Step 1: 新建 `chisha/sandbox_context.py`

```python
"""Sandbox per-request session context (S-04).

ContextVar-based: 每个 FastAPI 请求 wrapper 用 `with set_sandbox_session(sid):`
注入,data_root / clock / sandbox 透明读 ctx。绝不全局可写。

Codex must-fix #1: 包装调用 (eat 内部串 /recommend) 不靠 active.json 全局
切换,两 tab 并发 eat 不串。

S-04 范围:
- 仅提供 ctx 容器 + sid 校验,不强制 web_api wrap (那是 S-06a)
- sandbox 关闭时 sid 无意义 → 所有 path helper 直接 prod
- sandbox 启用 + sid 为 None / "_default" → flat 路径 logs/sandbox/{rel}
  (与 D-077 现行行为完全一致 — backward compat)
- sandbox 启用 + sid 为非 default → logs/sandbox/sessions/{sid}/{rel} 子树
  (S-05 / S-06a 真用)

线程 / asyncio:
- ContextVar 在同 thread 的 asyncio task 间正确隔离
- 跨 thread (threading.Thread) NOT propagated → 后台 worker (web_api
  _trigger_l1_extraction_async) 拿 None → _default → flat → 与今天等同
- S-06a 引入 web_api wrapper 时需用 contextvars.copy_context() 或显式
  sid 透传 — 本任务不解决,加 inline 注释守门
"""
from __future__ import annotations

import re
from contextlib import contextmanager
from contextvars import ContextVar

_current_sid: ContextVar[str | None] = ContextVar(
    "sandbox_session_id", default=None
)

# Codex must-fix #5: sid 必须可作文件路径片段, 不能 traversal
# v3: fullmatch (拒尾 \n), 黑名单 _legacy (S-05 用)
_SID_PATTERN = re.compile(r"[a-zA-Z0-9_-]{1,64}")
_DEFAULT_SID = "_default"
_RESERVED_SIDS = frozenset({"_legacy"})  # S-05 migrated old data 占用


def _validate_sid(sid: str) -> str:
    """合法 sid: fullmatch ^[a-zA-Z0-9_-]{1,64}$ 且不在 _RESERVED_SIDS.

    白名单: '_default' (current flat session).
    黑名单: '_legacy' (S-05 migration 保留).
    """
    if sid == _DEFAULT_SID:
        return sid
    if not isinstance(sid, str) or not _SID_PATTERN.fullmatch(sid):
        raise ValueError(
            f"invalid sandbox session_id: {sid!r} "
            f"(must fullmatch {_SID_PATTERN.pattern} or be '_default')"
        )
    if sid in _RESERVED_SIDS:
        raise ValueError(
            f"sandbox session_id {sid!r} is reserved (S-05 migration)"
        )
    return sid


@contextmanager
def set_sandbox_session(sid: str | None):
    """注入 sid 到 ctx. None 表示 explicit clear (走 _default fallback)."""
    if sid is not None:
        _validate_sid(sid)
    token = _current_sid.set(sid)
    try:
        yield
    finally:
        _current_sid.reset(token)


def current_sandbox_session() -> str | None:
    return _current_sid.get()
```

### Step 2: 改 `chisha/data_root.py`

#### 2.1 `_maybe_sandbox` 加 sid kwarg + `_default` flat 语义

```python
from chisha.sandbox_context import (
    _DEFAULT_SID,
    _validate_sid,
    current_sandbox_session,
)


def _resolve_sid(session_id: str | None) -> str:
    """显式 > ctx > _default. 返合法 sid."""
    sid = session_id if session_id is not None else current_sandbox_session()
    if sid is None:
        return _DEFAULT_SID
    return _validate_sid(sid)


def _maybe_sandbox(
    root: Path,
    rel_in_sandbox: str,
    rel_in_prod: str,
    session_id: str | None = None,
) -> Path:
    """sandbox 启用时返沙盒路径, 否则 prod.

    S-04: sid == _default → flat (logs/sandbox/{rel}), 与 D-077 完全一致.
    sid != _default → 子树 (logs/sandbox/sessions/{sid}/{rel}), S-06a 用.
    """
    if not sandbox.is_enabled(root):
        return root / rel_in_prod
    sid = _resolve_sid(session_id)
    if sid == _DEFAULT_SID:
        return root / "logs" / "sandbox" / rel_in_sandbox
    return root / "logs" / "sandbox" / "sessions" / sid / rel_in_sandbox
```

#### 2.2 8 个 path helper 加 `session_id` kwarg

每个 `*_path()` / `*_dir()` 加 `session_id: str | None = None` 透传:
- `meal_log_path`
- `sessions_dir` (D-039 recommend sessions, 命名冲突已知 — sandbox sid 嵌套 vs 同名 helper)
- `feedback_store_path`
- `recommend_log_path`
- `feedback_history_path`
- `long_term_prefs_path`
- `recommend_trace_dir`
- `profile_path` (副本 fallback 逻辑也加 sid)

`profile_path` 特殊:它判 `sandboxed.exists()` 决定走副本/prod。v2 行为:
```python
def profile_path(root=None, session_id=None) -> Path:
    r = _resolve_root(root)
    if sandbox.is_enabled(r):
        sid = _resolve_sid(session_id)
        if sid == _DEFAULT_SID:
            sandboxed = r / "logs" / "sandbox" / "profile.yaml"
        else:
            sandboxed = r / "logs" / "sandbox" / "sessions" / sid / "profile.yaml"
        if sandboxed.exists():
            return sandboxed
    return r / "profile.yaml"
```

→ `_default` 副本路径 `logs/sandbox/profile.yaml` **与 D-077 完全一致**,`test_profile_path_sandbox_with_copy` 不动。

### Step 3: 改 `chisha/clock.py`

```python
from chisha.sandbox_context import current_sandbox_session


def today(root=None):
    sid = current_sandbox_session()
    virtual = sandbox.current_date(root, session_id=sid)
    return virtual if virtual is not None else dt.date.today()
# now / now_utc 同理
```

S-04 内 `sandbox.current_date(root, session_id=...)` 仍读单 state.json(本任务 sandbox.py 实现忽略 sid)→ clock 行为完全等同 D-077。

### Step 4: 改 `chisha/sandbox.py`

#### 4.1 全 API 加 `session_id: str | None = None` kwarg

10 个公开 API:`init / advance / reset / disable / state / current_date / current_datetime / current_datetime_utc / record_l1_extraction / is_enabled`

S-04 阶段:
- 所有 API 内部 **忽略 sid**,继续读/写 `logs/sandbox/state.json` 单文件
- 注释明确:S-05 才拆 `logs/sandbox/sessions/{sid}/state.json`,本任务签名预留以解耦 clock/data_root 调用点

`is_enabled(root, session_id=None)` 也加 kwarg(spec §4 D-077 兼容清单),sid 忽略。

#### 4.2 sandbox 文件物理路径 100% 不变

`_STATE_REL = "logs/sandbox/state.json"` 不动。`_copy_real_data_to_sandbox` 不动。`reset()` 删 `logs/sandbox/` 不动。

### Step 5: 新增 `tests/test_sandbox_context.py`

14 个 case (v3 加 S-06a 真守门 + sid 黑名单):

1. `test_default_returns_none`: 无 ctx → `current_sandbox_session() is None`
2. `test_set_and_get`: `with set_sandbox_session("a"): assert current_sandbox_session() == "a"`
3. `test_nested_restores`: 嵌套退出后恢复外层
4. `test_asyncio_isolation`: `asyncio.gather` 两 task 不同 sid 各自不串
5. `test_invalid_sid_raises`: `set_sandbox_session("../escape")` raises ValueError
6. `test_default_sid_allowed`: `set_sandbox_session("_default")` 不 raise
7. `test_legacy_sid_rejected`: **v3 新增** `set_sandbox_session("_legacy")` raises (S-05 保留)
8. `test_trailing_newline_rejected`: **v3 新增** `set_sandbox_session("abc\n")` raises (fullmatch 守门)
9. `test_data_root_explicit_nondefault_sid`: `data_root.meal_log_path(root, session_id="abc")` (sandbox on) 返 `logs/sandbox/sessions/abc/meal_log.jsonl`
10. `test_data_root_ctx_sid_nondefault`: `with set_sandbox_session("y"): meal_log_path(root)` 返 `logs/sandbox/sessions/y/meal_log.jsonl`
11. `test_data_root_default_sid_flat`: sandbox 启用 + 无显式无 ctx → `logs/sandbox/meal_log.jsonl` (flat, 与 D-077 一致)
12. `test_data_root_explicit_wins_over_ctx`: 显式 sid 优先于 ctx
13. `test_data_root_prod_when_sandbox_off`: sandbox 关闭时显式 sid 不生效 → prod

**v3 新增 S-06a forward guards** (这两条 test 是 S-06a wrap web_api 时的契约样板):

14. `test_ctx_does_not_leak_into_threads`: **(documentation only)** 裸 `threading.Thread` 内 `current_sandbox_session()` 返 None — 这是 Python ContextVar 文档化行为,守门防意外被改"修复"
15. `test_copy_context_propagates_sid`: **真 S-06a 守门** — `with set_sandbox_session("sid_a"): ctx = copy_context(); t = Thread(target=lambda: ctx.run(_worker)); t.start(); t.join()` → `_worker` 必须能在 thread 内读到 `"sid_a"`,且 `data_root.meal_log_path(root)` 必须返 `logs/sandbox/sessions/sid_a/meal_log.jsonl`(非 default 路径)。任何 S-06a 实现违反契约就 break
16. `test_explicit_sid_kwarg_overrides_ctx_for_threading`: **真 S-06a 守门** — `with set_sandbox_session("sid_a"): t = Thread(target=_worker_explicit, args=("sid_b",)); t.start(); t.join()` → `_worker_explicit` 显式收 `sid="sid_b"`,在 thread 内调 `data_root.meal_log_path(root, session_id="sid_b")` 必须返 `sessions/sid_b/`,证明显式 kwarg path 也是 S-06a 合法实现选项

### Step 6: 既有测试零修改验证

v2 关键性质:`_default` = flat,故:
- `tests/test_data_root.py` (10 tests): 完全不改 ✓
- `tests/test_sandbox_clock.py` (16 tests): 完全不改 ✓
- `tests/test_trace_store.py` (含 `test_sandbox_isolation` flat path 断言): 完全不改 ✓
- `tests/test_web_api_sandbox.py`: 完全不改 ✓
- `tests/test_acceptance_d076_d077.py`: 完全不改 ✓
- `tests/test_refine_trace_persist.py`: 完全不改 ✓
- `tests/test_l1_extractor.py`: 完全不改 ✓

唯一新增: `tests/test_sandbox_context.py`

### Step 7: baseline_l2_snapshot 验证

```bash
uv run python -m scripts.baseline_l2_snapshot --out-dir tmp/baseline_traces_after
uv run python -m scripts.compare_traces  # 0 diff (EPSILON=1e-6)
```

baseline 跑 prod 模式 → 完全走 prod 路径,与改前等同 → 0 diff。

### Step 8: 全测试

```bash
uv run pytest tests/ -q
```

预期: 既有 791+ 测试全过 + test_sandbox_context.py 新增 12 case 全过。

---

## 3. Affected files

新建:
- `chisha/sandbox_context.py`
- `tests/test_sandbox_context.py`

改:
- `chisha/data_root.py` (`_maybe_sandbox` + 8 path helper 加 sid kwarg)
- `chisha/clock.py` (3 helper 透 ctx sid)
- `chisha/sandbox.py` (10 API 加 sid kwarg, 实现忽略)

**零修改**:
- `chisha/{api,recall,score,rerank,refine,l1_extractor,session,trace_store,feedback_store,web_api,l1_prefs,long_term_prefs}.py`
- `tests/*` 既有全部
- `apps/*`
- `docs/*`(可在 Phase 5 给 CONTRACTS.md 加 1 行 sandbox_context 出现说明,但不强制)

---

## 4. 默认值策略 (v2 关键)

| 调用形式 | 行为 |
|---|---|
| sandbox 关闭 (任意 sid) | prod 路径 (零变化) |
| sandbox 启用 + 显式 `session_id="abc"` | `logs/sandbox/sessions/abc/{rel}` |
| sandbox 启用 + 无显式 + ctx 有 `set_sandbox_session("xyz")` | `logs/sandbox/sessions/xyz/{rel}` |
| sandbox 启用 + 无显式 + ctx None | `logs/sandbox/{rel}` (**flat,与 D-077 一致**) |
| sandbox 启用 + sid=`_default` (显式或 ctx) | `logs/sandbox/{rel}` (**flat,与 D-077 一致**) |

`_default` 是 "legacy/flat sentinel" — 等价于 D-077 现行落盘行为。S-05 才会进一步迁移。

---

## 5. 回归风险 + Rollback

### 风险表 v2

| 风险 | 缓解 |
|---|---|
| 既有 sandbox 测试断言 flat 路径 | v2 `_default` = flat → 全部 zero-change |
| `_copy_real_data_to_sandbox` 写 flat 路径 | v2 不动:S-04 内非显式 sid → flat → consistent |
| ContextVar 不跨 threading.Thread (S-06a 隐患) | S-04 不 wrap web_api → 后台 worker 拿 None → `_default` → flat → 与今天等同;`test_threading_does_not_propagate` 守门防 S-06a 漏掉 |
| sid 路径注入 ("../etc/passwd") | `_validate_sid` regex + raise 拒绝;`_default` 白名单 |
| `sessions_dir` 命名冲突 (sandbox sid vs D-039 recommend sid) | 已知;S-04 内 `_default` flat → recommend session 文件仍在 `logs/sandbox/sessions/{recommend_sid}.json`(原行为);非 default sid 时嵌套 `logs/sandbox/sessions/{sandbox_sid}/sessions/{recommend_sid}.json`(S-05 才真触发) |
| baseline_l2_snapshot 0 diff 验证 | snapshot 不启用 sandbox 且无显式 sid,在 root/sandbox state 一致下走 prod → 与改前等同 → 0 diff (EPSILON=1e-6) |

### Rollback

git revert + delete 2 新文件 + 还原 3 改动文件。文件物理上零变化(因 `_default` = flat),user 本机 sandbox 数据丝毫不动。

---

## 6. 自审 (v2 完整覆盖核对)

- [x] data_root 8 path helper 全部加 sid kwarg(含 `recommend_trace_dir` / `profile_path`)
- [x] sandbox 10 API 全部加 sid kwarg(含 `is_enabled` D-077 兼容)
- [x] clock 3 helper 全部透 ctx sid
- [x] ContextVar(非 threading.local)→ asyncio 友好
- [x] sid sanitization (`_validate_sid` regex)
- [x] `_default` = flat → 全部既有测试 zero-touch
- [x] state.json 单文件不动 → D-077 行为保留
- [x] `_copy_real_data_to_sandbox` 不动 → copy_real_data zero-touch
- [x] sandbox.reset 删 `logs/sandbox/` → 包括非 default sid 的 `sessions/{sid}/` (递归删) → 自洽
- [x] trace_store 调用透明:write/read 同 helper → 路径自洽
- [x] threading.Thread 不跨 ctx 守门测试
- [x] sessions_dir 命名冲突已 ack (S-05 才真触发嵌套)
- [x] baseline_l2_snapshot 不启用 sandbox → 0 diff 保证

---

## 7. Done When

- `chisha/sandbox_context.py` 存在,导出 `set_sandbox_session` / `current_sandbox_session` / `_validate_sid` / `_DEFAULT_SID`
- `chisha/data_root.py` 8 path helper 全加 `session_id` kwarg + 行为矩阵
- `chisha/clock.py` 3 helper 透 ctx sid
- `chisha/sandbox.py` 10 API 加 sid kwarg(忽略实现)
- `tests/test_sandbox_context.py` 16 case 全过 (含 v3 加的 S-06a forward guards)
- 既有 791+ 测试 zero-touch 全过
- `baseline_l2_snapshot` before/after → `compare_traces` 0 diff
