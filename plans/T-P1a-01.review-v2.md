## Fix 1 Verification (Trace Persistence)

PASS.

- `chisha/refine.py:153-162` initializes `refine_hard_filter_events` and appends a `make_hard_filter_event(...)` only when `not intent.is_empty()` and `intent.allows_methodology_break()` are both true.
- The response includes `_refine_hard_filter_events` at `chisha/refine.py:180-181`, so the event leaves the refine function.
- `chisha/web_api.py:190-194` reads `raw.get("_refine_hard_filter_events") or []`, initializes `base_trace["l1"]` with `setdefault("l1", {})`, initializes `l1["hard_filter_events"]` with `setdefault("hard_filter_events", [])`, then uses `events.extend(refine_events)`. This appends and does not overwrite existing events.
- `chisha/web_api.py:195` calls `trace_store.write_trace(req.session_id, base_trace, root=ROOT)` after the append, so the appended events are persisted to trace, not just kept in the response object.

## Fix 2 Verification (Halal Keywords)

BLOCKED.

- `叉烧` is present in `_HALAL_PORK_KEYWORDS` at `chisha/l0_constraints.py:25-27`.
- `白切` is also present in the same tuple at `chisha/l0_constraints.py:25-27`.
- Matching is substring-based: `_dish_text` concatenates `canonical_name` and `raw_name` at `chisha/l0_constraints.py:60-62`, and halal matching uses `if kw in text` at `chisha/l0_constraints.py:100-103`.
- Therefore a halal profile would reject `白切鸡`: `白切` is a substring of `白切鸡`, so `dish_violates_l0_b(...)` would return `halal_pork:白切`. This is a false positive blocker because `白切鸡` is a halal-compatible poached chicken dish.
- The test extension covers `叉烧螺蛳粉`, `蒜香烤香肠`, and preservation of `红柳羊肉串` / `炭烤牛肉串` at `tests/test_l0_constraints.py:160-181`.
- The tests do not cover the `白切鸡` edge case; there is no assertion that `白切鸡` is preserved.

## Baseline Check

PASS.

- `python3 -m scripts.compare_traces --before-dir tmp/baseline_traces --after-dir tmp/baseline_traces_after` passed with all four snapshots OK and reported L2 trace 0 diff.
- `uv run ...` could not be used in this sandbox because uv attempted to access `~/.cache/uv/sdists-v9/.git` and hit `Operation not permitted`; the same compare script passed via `python3`.

## VERDICT

BLOCKED

1. `chisha/l0_constraints.py:25-27` includes `白切`, and `chisha/l0_constraints.py:100-103` matches halal keywords by substring, so `白切鸡` is incorrectly banned as `halal_pork:白切`.
2. `tests/test_l0_constraints.py:160-181` does not cover the `白切鸡` preservation edge case, so the false positive is not guarded by tests.
