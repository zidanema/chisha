# T-P1a-03 Plan · refine 多 slot LLM 解析层 (scaffold 版)

## 范围调整

brief §4 提了非常完整的多 slot schema (redirect/constrain/reference/reject_previous/raw_understanding) 加 3 条安全带 + 30-50 条 eval set。完整实现 需要:
- 重写 prompt `prompts/parse_refine_intent.md` (大幅扩展)
- 写 eval set 30-50 条人工标注
- 改下游消费方 (recall / score / rerank) 对接新 slot
- 字段空洞透传到 L3 prompt

**本次 scaffold 版只做**:
1. 加 `RefineIntentV2` dataclass (多 slot 占位, **不消费**)
2. 加 `extract_refine_intent_v2(text)` 包装当前 `parse_refine_intent`, 把现有结构填进新 V2 shape
3. 三条安全带骨架: schema 验证函数 + 失败降级 + raw_understanding 字段 (LLM 输出留空时填 raw_text)
4. trace 双存: refine 路径已经在 `refine_intent_trace.jsonl` 落 `intent.to_log_dict()`, 加 v2 schema 上去
5. ≥6 个 schema 单测 (validation 各边界 + fallback path)
6. **Feature gate**: `RefineIntentV2.schema_version = "2.0"`, 旧 `RefineIntent.schema_version = "1.0"` 共存。下游全部仍走 v1.
7. brief §5 "字段空洞务实降级" 实现: V2 dataclass slot 存在但 L1/L2 不消费 (因为 V2 还没接 LLM 多 slot prompt, 实际 slot 都为空), trace 标 `unsupported_in_recall=true` 字段

**Deferred (follow-up tasks)**:
- prompt `parse_refine_intent.md` 真正重写支持多 slot LLM 解析 (~1 天工作)
- 30-50 条 eval set 人工标注 + runner script (~0.5 天)
- 下游 recall / score / rerank 对接 V2 (T-P2-01 自然需要 reference slot, 那时再做)
- 真正的字段空洞 L3 透传 (T-P1b-02 narrative 时一起改)

## What

### Step 1 · `chisha/refine_intent_v2.py` (新建)

```python
@dataclass
class RefineIntentV2:
    """Faithful Refine 多 slot schema (brief §4).

    本任务 scaffold: 仅定义结构 + 验证, 下游不消费. P1a-03 follow-up 真正接 LLM.
    """
    redirect: dict = field(default_factory=lambda: {...all slots empty list...})
    constrain: dict = field(default_factory=dict)
    reference: dict | None = None
    reject_previous: bool = False
    raw_understanding: str = ""
    raw_text: str = ""
    schema_version: str = "2.0"
    # brief §5 数据层暂不支持的字段名 (quality_floor / delivery_only / max_distance_km / reference)
    unsupported_in_recall: list[str] = field(default_factory=list)
    # Codex audit blocker: V1 fields (cooking_method/flavor_tags/raw_flavor/portion/
    # staple_preference/price_band/freeform_note) 在 V2 schema 没有自然位, scaffold
    # 期间用 legacy_v1 完整保留, 让 from_legacy 无损迁移.
    legacy_v1: dict = field(default_factory=dict)

    def is_empty(self) -> bool: ...
    def to_log_dict(self) -> dict: ...
    @classmethod
    def from_legacy(cls, intent: RefineIntent) -> "RefineIntentV2":
        """无损迁移: V1 字段进 legacy_v1 dict (使用 intent.to_log_dict()).
        cuisine_want/avoid + ingredient_want/avoid 拷到 redirect 对应 slot.
        其他 V1 字段 (cooking_method/flavor_tags/...) 通过 legacy_v1 保留.
        """
```

### Step 2 · `extract_refine_intent_v2(text)` wrapper
```python
def extract_refine_intent_v2(text: str, ...) -> RefineIntentV2:
    """安全带 1: schema 验证 + 失败降级.

    走当前 parse_refine_intent 拿到 RefineIntent v1, 再用 from_legacy 包成 v2.
    所有"新 slot" (cuisine_candidates_expanded / brand_avoid / food_form_avoid /
    constrain.* / reference) 都留空, 标到 unsupported_in_recall.

    LLM 失败时: 返 empty RefineIntentV2 with raw_text 保留 (brief 安全带 #1 + #2).
    """
```

### Step 3 · 验证 helper + safety net 测试
```python
def validate_v2_schema(d: dict) -> tuple[bool, list[str]]:
    """检查 dict 是否符合 V2 schema. 返 (ok, errors)."""
```

### Step 4 · 测试 (≥6)

1. `test_refine_v2_from_legacy_preserves_fields` — **断言所有 V1 13 字段无损落到 V2 (cuisine_want/avoid + ingredient_want/avoid 到 redirect, 其他到 legacy_v1)**
2. `test_refine_v2_empty_default` — 默认空 V2 各 slot 为空集合
3. `test_refine_v2_unsupported_in_recall_lists_field_voids` — 列出 quality_floor/delivery_only/max_distance_km/reference 4 个 §5 字段
4. `test_refine_v2_schema_validation_accepts_v2_dict` — 校验函数接 minimal valid dict
5. `test_refine_v2_schema_validation_rejects_bad_shape` — schema_version 错 / redirect 不是 dict / constrain 不是 dict / reject_previous 不是 bool 各一条
6. `test_extract_v2_llm_failure_returns_empty_with_raw_text` — 安全带 1: LLM 失败返空 V2 (raw_text 保留)
7. `test_refine_v2_to_log_dict_includes_schema_version_and_raw_understanding` — trace 双存格式
8. `test_refine_v2_is_empty_ignores_raw_text` — raw_text/raw_understanding 不计入 is_empty()
9. `test_v1_caller_still_returns_v1_schema_version` — Codex audit 5: parse_refine_intent 仍返 RefineIntent + schema_version="1.0", 不破 V1 路径

## Affected files
- 新建 `chisha/refine_intent_v2.py`
- 新建 `tests/test_refine_intent_v2.py`
- 不动 `chisha/refine_intent.py` (v1 保留)
- 不动 `chisha/refine.py` (下游仍走 v1)
- 不动 prompt / eval set / 下游

## Regression risks
| 风险 | 缓解 |
|---|---|
| V2 引入 break v1 调用 | V2 是独立 module + 独立测试, 不破 v1 |
| baseline diff | V2 不接生产链路, 0 diff 保住 |
| schema_version 字符串硬编码导致 V3 切换困难 | scaffold 范围内可接受, 真正 LLM 接入时再做版本管理 |

## 不做
- 不写 LLM prompt
- 不写 eval set (放 follow-up)
- 不动 recall / score / rerank 消费
- 不接生产 refine 链路 (CONTRACTS.md "字段空洞透传" 在 T-P1b-02 narrative 处一起改)
