## Re-audit: T-P1a-01 Plan v2

### Blocker 1: combo_passes_plate_rule return type
RESOLVED — Step 4 explicitly says "不改返回类型" and keeps `combo_passes_plate_rule(...) -> bool`, with `combo_passes_plate_rule_with_reason(...) -> tuple[bool, str | None]` as a sibling. The caller audit names `chisha/recall.py:482, :490, :513, :521` and `tests/test_recall.py:127, :129, :131` as still using the bool interface, while `_build_l1_trace` uses the reason-returning sibling.

### Blocker 2: L0-B halal block scope
RESOLVED — Step 1 corrects the halal rule to ban only dishes whose `canonical_name` or `raw_name` contains pork/processed-meat keywords `猪/培根/火腿/腊肠/腊肉/热狗` or has `processed_meat_flag=True`, and explicitly calls out preserving proper halal lamb/beef. Step 7 includes `test_l0_b_halal_preserves_lamb_beef`, requiring lamb/beef to remain while pork/bacon drop.

### Blocker 3: Trace event nesting location
RESOLVED — Step 5 states events are written to `trace["l1"]["hard_filter_events"]` per T-00, not the root, and clarifies `_build_l1_trace` returns the l1 segment by assigning `trace["hard_filter_events"] = hard_filter_events`. Test #9 also spells out `trace.l1.hard_filter_events`.

### Blocker 4: What-if invariant test
RESOLVED — Step 7 adds `test_what_if_zero_override_preserves_hard_filter_events` in `tests/test_d079_pr2_what_if.py`. The plan says the base trace triggers one L0-A event and What-if with zero overrides must preserve the same `l1.hard_filter_events` because it uses frozen L1.

---
VERDICT: APPROVED
