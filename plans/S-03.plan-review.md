# S-03 Plan Review

## Iter 1
[FALLBACK: Claude Opus adversarial review — Codex unavailable (quota)]

### Blocking issues

**1. derived banner `refine_conflict` + dismissedBannerIds Set 是 stale-id 死锁 (plan §3.3 + §3.2 dismissBanner)**
- File: `plans/S-03.plan.md:141, 147-150`
- 派生 banner 用固定 id `"refine_conflict"`,dismiss 后 id 进 `dismissedBannerIds` Set 永驻。
- 失败 mode: D3 午 conflict → dismiss → eat 进 D3 晚 → 新 currentRecs 也有 conflict (`freshRecsForMeal` 只是 shuffle + 把 r[0] 即蓉香记/戒辣的那条搬位置,conflict 字段不会消) → derived 又生成 `refine_conflict` 同 id → filter 直接过滤掉 → 用户看不到本顿的冲突警告。
- 同样问题:plan §3.3 review-mode banner id 用 `"review_mode"`,如果用户在某顿 dismiss 过这个 (虽然 plan 标 `dismissable: false`,但 §3.2 dismiss 实现没区分 dismissable),进入回顾模式 banner 也消失。
- handoff §banner 语义本意:dismiss 只清"本顿的此条",不是"全程压制这个 id"。Plan 缺一个 "clock 推进时同时清 dismissedBannerIds 中所有派生 id" 或 "派生 banner 不走 dismissedBannerIds 过滤,改为另一个 transient-dismissals state per-meal" 的机制。
- 验收第 4 / 第 6 条会被压住:eat 后再触发同类 banner 看不到。

**2. handleRollback 边界条件错 — 验收语义不符 (plan §3.2 handleRollback)**
- File: `plans/S-03.plan.md:124-131`
- Plan: `history = history.filter(h => h.idx < idx); currentMealIdx = idx`. 但 ConfirmModal (`ConfirmModal.tsx:30, 42`) 文案是 "撤销 D{meal.day} {meal.slot} 及之后的 N 顿,该格将变为 current",即 meal.idx 本身也要回滚成 current。
- 验收 #7 描述 "history 裁 + clock 回 + selectedCell 清"。Plan 选 `idx < idx` 等于把 meal.idx 那一顿**也**裁掉,clock 回到 idx → 这条**和 modal 文案行为一致**。但和真正语义比 — meal.idx 是 eat/skip 完成态,plan 把它当 current,等于把"已发生"擦掉。这是设计意图,plan 内说"裁到分叉点之前"(§9 Q6) 用 `slice(0, fromIdx)` 也是这个。但 plan §3.2 handleBranch step 4 "clone history to new session map, 裁到 fromIdx (类似 rollback)" 没写"是否包含 fromIdx",新 session 起点是 fromIdx 还是 fromIdx+1 没定。
- 失败 mode: 验收 #8 "分支后顶栏 session 下拉出现新分支" pass,但前端 active session 切到新 sid 后, currentMealIdx 没初始化 (新 SessionMeta 没 currentMealIdx 字段, S-02 type 也没这字段, 见 `types/sandbox.ts:15-24`) → 渲染 clock 用什么 idx? Plan §3.1 clock 注释"派生自 currentMealIdx",而 currentMealIdx 是 hook 内一个全局 state,不是 per-session map。**切 session 时 currentMealIdx 不 reset 就直接停在 4** → 新分支虽然 history 裁到 fromIdx,但 clock 仍指 4 → 主区 DecisionArea 喂的是新 session "未来" 的 idx=4 mock,违反"分支起点 = fromIdx"。
- Plan §7 Regression 表格只说 "切 session 时重置 history/currentRecs/lastDecision/selectedCellIdx",**漏了 currentMealIdx**。

**3. Per-session state 模型完全没说清,plan §4.3 自相矛盾 (plan §3.1, §4.3)**
- File: `plans/S-03.plan.md:213-220`
- §3.1 contract: `history: Meal[]` 是单数,`currentMealIdx: number` 单值。
- §4.3 "所有 session 共用 HISTORY 初始" + 后面又写 "更细致的做法 (推荐) SESSION_INITIAL_STATE_MAP" → 选哪个没拍。
- 严重后果:handleBranch step 4 "clone history to new session map" 但 §3.1 根本没声明 session map state 存在。如果只有单 `history` state,branch 后切回原 session,原 session 的 history 是什么?**没存**,直接丢了 — 验收 #8 (verify 新分支 + 老 session 都在 session 下拉),用户切回老 session 会看到新分支裁过的 history。
- handoff brief §scope 拍板 "多 session + branch/rollback 完整版,不做单 session 简化",plan 这条违反 brief。
- 必须 sessionId → {history, currentMealIdx, currentRecs, lastDecision} 的 Map state,plan §3.1 contract 必须改。

**4. handleEat / handleSkip 到达 totalMeals 后 silent return,无 done banner (plan §3.2 + §7)**
- File: `plans/S-03.plan.md:99, 284`
- §3.2 "currentMealIdx += 1 (若已 >= totalMeals 不做)" + §7 风险表 "State 推进越界 (idx > total) → handleEat/handleSkip 内 `if (currentMealIdx >= totalMeals) return`"。
- 失败 mode: session 跑到第 14 顿,用户点"就这个 →" → state 不变,无任何反馈。验收 #2 "history 多一条 + clock +1 + 新推荐池 + transient banner 清空" 在 idx=13 上 fail (history 不加,clock 不动)。chrome-devtools-mcp 不会复现这一步 (只跑前几顿),但 production-like 跑完 14 顿就是 dead-button。
- 至少要落个 "session 已完成 (14/14)" banner + 把 RecCard / DecisionArea 切到 done 形态 (handoff session.status="done" 已有概念,plan §4.3 也提到 "session.status == done")。
- 是否 in scope:验收 #2 是 "整个 14 顿都跑通",plan §6.2 步骤 "启 Vite + chrome-devtools-mcp 11 条" 没含 14 顿压测,但 spec `Done When` 行 63: "chrome-devtools-mcp 自驱跑完整 14 顿 + handoff §验收清单 11 条"。Plan 验收没覆盖这条 spec 要求 → blocking。

**5. ConfirmModal onClose 双调用风险 (plan §5 ConfirmModal 接线)**
- File: `plans/S-03.plan.md:234, components/modals/ConfirmModal.tsx:73-75`
- ConfirmModal 内部 `onConfirm?.(...); onClose?.();`(组件自己在 confirm 后又 call onClose)。
- Plan §5 写 `onConfirm={(branchName) => sb.confirmKind === "rollback" ? sb.handleRollback(...) : sb.handleBranch(...)}` + 单独 `onClose={sb.closeConfirm}`。
- handleRollback 内部又有 step 6 `confirmKind = null (关 modal)` (§3.2:130) → onClose 在 handleRollback 之前还是之后调? 顺序是: `onConfirm` 跑 → handleRollback 把 confirmKind=null → 然后组件再 `onClose?.()` 又把 confirmKind=null。一次幂等没事,但同样 `confirmMeal` 在 handleRollback 里没被清 (plan 没明示),后续 confirmKind=null 但 confirmMeal 还在 → 下次 openConfirm("branch") 时 confirmMeal 是上次 rollback 的 meal,如果 selectCell 已被 reset 但 confirmMeal 不更新,就用错 idx 调 handleBranch。
- 必须明确: openConfirm(kind) 必须**同时**根据 `selectedCellIdx` 取 confirmMeal,而不是预存 — 否则 stale。Plan §3.1 把 confirmMeal 当独立 state,这就是 stale 来源。

### Non-blocking findings (mark clearly)

- **NB-1 demo-* query 行为漂移**: 用户的 audit 问题 #7。Plan §5:247 "demo-modal 仅设初始 state (useState 初值),之后 hook 管"。问题是 hook 内 `useState(initialOpen)` 必须读 query。这是可行的,但 §3.1 contract 没标 `summaryOpen` 初值参数,加一行 `useSandbox({initialModal?: DemoModal})` 即可。**不是 blocking**,但 plan 应显式标出来,否则实施时 demo-modal 全废,S-02 视觉回归 url 全失效。
- **NB-2 Esc 一键关多层 UX**: 用户问题 #5。Plan onEsc 一把全关 (clearSelection + closeSummary + closeConfirm + closeRefineModal)。常规 UX 习惯是 modal > drawer > review-mode 一层一层关。但本 SPA 同时打开多 modal 概率极低 (modal/drawer/review 互斥触发),所以**实际不算 bug**。建议加一句"按 priority modal > drawer > review-mode 顺序找第一个 open 的 close"。**不 blocking**。
- **NB-3 freshRecsForMeal deterministic shuffle 仍有 conflict 字段**: plan §4.1 `[...CURRENT_RECS.slice(seed), ...]`,蓉香记的 conflict=戒辣 永远在。验收 #2 "新推荐池" 视觉上不变 (5 个名字一样),用户看不出"换了"。建议 mock 让 conflict 随 idx 偶发 (e.g. `idx % 3 === 0` 才注入 conflict)。**不 blocking** — 只是验收视觉差。
- **NB-4 `selectCell(idx)` 重复:plan §3.1 同时声明 `selectCell` 和 `clearSelection`,§3.2 又写 `clearSelection: selectCell(null)`**: 重复函数,API 表面冗余。**不 blocking**,但 codex 风格会建议 prune。
- **NB-5 handleSwap 不推进 idx 但 `transient banners` 不清**: §3.2 handleEat 清 transient,handleSwap 不清,handleRefine 不清。多次 swap → 多个 transient banner 堆。Plan §9 Q5 "transient banner 上限 — 不限制",但 banner stack 4+ 视觉糟糕 + 没 dismiss 都按了。建议 swap/refine 时**先**清同类 transient 再 push 新的。**不 blocking**。
- **NB-6 typecheck 风险 (用户问题 #6)**: 看过所有改动 component 的 prop 签名 (TopBar/Timeline/OpBar/DecisionArea/ReviewCard/Banners/SummaryDrawer/ConfirmModal/RefineModal),plan §5 接线 100% 兼容现签名,**无 hidden prop 变更**。
- **NB-7 useKeyboard onEsc 依赖闭包稳定性**: §3.4 `useEffect [opts.onEsc]`,如果 App 内 onEsc 是 inline 函数 `() => { clearSelection(); ... }` 每次 render 重建 → useEffect 反复 attach/detach window listener。性能小,**不 blocking**,但加 useCallback 或在 hook 内部维护 listener。

### VERDICT
VERDICT: BLOCKED

## Iter 2
[Fallback: Claude Sonnet adversarial review — Codex unavailable (quota), iter=2 manual audit against codebase]

### Re-check: 5 Blockers from Iter 1

**BLOCKER #1 (dismissed banner stale-id dead-lock) → RESOLVED by 修订 B**
- v2: `dismissedBannerIds` state removed. Derived banners (`refine_conflict`, `review_mode`) are never dismissable (`dismissable: false`), so they never enter any dismiss filter. Transient banners use `swap_${Date.now()}` / `refine_${Date.now()}` — unique per action, cannot collide.
- `Banners.tsx:23` already guards: `{b.dismissable && onDismiss && ...}` — no X button rendered for non-dismissable banners. Verified in codebase.
- One residual issue below (NB-2-iter2): `§3.3` still declares `refine_conflict` with `dismissable: true`. Contradicts the intent of 修订 B. Non-blocking if implementation follows 修订 B text, but is a spec inconsistency.

**BLOCKER #2 (handleRollback boundary + branch currentMealIdx) → RESOLVED by 修订 C**
- v2: `history.filter(h => h.idx < targetIdx)`, `currentMealIdx = targetIdx`. Matches ConfirmModal text "该格将变为 current" (`ConfirmModal.tsx:41-44`).
- `handleBranch` now explicitly: `history = oldHistory.filter(h => h.idx < fromIdx)`, `currentMealIdx = fromIdx`. New session starts at branch point, old session untouched.
- Per-session Map (修订 A) means `currentMealIdx` is per-session — the cross-session leak bug is gone.

**BLOCKER #3 (no per-session state map, §3.1 / §4.3 contradiction) → RESOLVED by 修订 A**
- v2 §0 explicitly declares `sessionStates: Map<string, SessionInternalState>` with `{history, currentMealIdx, currentRecs, lastDecision, dismissedDerivedBanners}` (note: `dismissedDerivedBanners` field appears in A but is orphaned after 修订 B removes that mechanism — see NB-1-iter2 below).
- Hook exposes all fields as derived from `sessionStates.get(activeSessionId)`. Branch creates new map key, old key untouched. §4.3 contradiction is resolved; §3.1 contract updated accordingly.

**BLOCKER #4 (no done state) → PARTIALLY RESOLVED by 修订 D**
- v2 adds done banner: `currentMealIdx >= totalMeals && !isInReviewMode` → info banner. Inline JSX placeholder in main area ("跑完了 — 回到 timeline..."). `handleEat/handleSkip` return early but done banner provides user feedback.
- One new issue: plan §3.2 handleRollback step 7 adds a transient banner `{id: "rollback_" + Date.now(), ...}` — but 修订 B says `dismissedBannerIds` is removed. Rollback banner uses unique id so it goes into `transientBanners`, which is consistent. OK.
- Spec says `handleEat/handleSkip` in done state return early and **done banner is visible**. Plan's inline JSX approach is fine for S-03 scope.

**BLOCKER #5 (ConfirmModal stale confirmMeal + double-close) → RESOLVED by 修订 E**
- v2: `confirmMeal = selectedCellIdx !== null ? history[selectedCellIdx] : null` — purely derived, no independent state. No stale risk.
- Double-close resolved: `ConfirmModal.tsx:73-74` calls `onConfirm?.(...); onClose?.()` — `handleRollback` / `handleBranch` no longer internally set `confirmKind = null` (plan §3.2 step 6 "confirmKind = null" explicitly deleted in 修订 E). `onClose` (= `sb.closeConfirm`) is the single closer. Idempotent.

---

### New Issues Found in v2 修订

**BLOCKER-NEW-1: `SessionInternalState` includes `dismissedDerivedBanners: Set<string>` (修订 A §0) but 修订 B eliminates the entire dismiss-for-derived mechanism**
- File: `plans/S-03.plan.md:17` (修订 A struct) vs `plans/S-03.plan.md:23-27` (修订 B decision)
- 修订 A: `SessionInternalState = {..., dismissedDerivedBanners: Set<string>}`.
- 修订 B: derived banners are non-dismissable; `dismissedBannerIds` state removed.
- If implementer faithfully follows `SessionInternalState` spec and allocates `dismissedDerivedBanners`, it's dead weight — no code path populates it, and it has no consumer. If implementer notices the contradiction and drops it from the struct, that's a deviation from the spec. 
- Ruling: **BLOCKER** only if `§3.3` still says `refine_conflict` has `dismissable: true` AND the `banners` filter formula (`plans/S-03.plan.md:230`) still filters by `dismissedBannerIds`. Those two lines were not retracted in v2. The filter formula at §3.3 end: `banners = [...derivedBanners, ...transientBanners].filter(b => !dismissedBannerIds.has(b.id))` — this references `dismissedBannerIds` which 修订 B said is removed. If the hook still has this filter expression but `dismissedBannerIds` is gone, there's a compile error / undefined reference. **This is a spec contradiction that will produce a TypeScript error at implementation time.**
- Severity: **BLOCKING** — the §3.1 contract still lists `dismissedBannerIds: Set<string>` in `UseSandboxResult` (plan:147). 修订 B says remove it, but §3.1 was not updated. Implementer following §3.1 literally will add the field and the filter, reintroducing the stale-id bug.

**BLOCKER-NEW-2: §3.4 `useKeyboard` — `[opts.onEsc]` dep-array means listener re-attaches every render; 修订 H specifies ref-stable handler with empty deps `[]` but §3.4 code block still uses `[opts.onEsc]`**
- File: `plans/S-03.plan.md:243` (§3.4 code) vs `plans/S-03.plan.md:74` (修订 H)
- 修订 H: "useKeyboard 内部 ref 存最新 handler, useEffect deps 为空 (一次性 attach)".
- §3.4 code block:
  ```typescript
  useEffect(() => { ... }, [opts.onEsc]);
  ```
  This directly contradicts 修订 H. The code block is the authoritative spec that Codex will implement. The verbal description in 修订 H was not propagated back to §3.4.
- Consequence: `onEsc` is an inline arrow function in App.tsx → `[opts.onEsc]` changes identity every render → `addEventListener` / `removeEventListener` cycle every render → performance regression + potential missed key events during React batching.
- Severity: **BLOCKING** — §3.4 is the implementation spec; it has not been updated to reflect 修订 H. Implementer will follow §3.4 code.

**BLOCKER-NEW-3: `confirmMeal` derivation fails for "branch" when `selectedCellIdx` is the current cell (idx == clock.idx)**
- File: `plans/S-03.plan.md:55-57` (修订 E) + `plans/S-03.plan.md:199` (selectCell spec)
- 修订 E: `confirmMeal = selectedCellIdx !== null ? history[selectedCellIdx] : null`.
- §3.2 `selectCell`: "idx == clock.idx → 不进 review, `selectedCellIdx = null`" — clicking the current cell keeps `selectedCellIdx = null`.
- But the user can also open `openConfirm("branch")` from OpBar when a **past** cell is selected. `OpBar.tsx:163-164` calls `history.find(h => h.idx === selected)`. If `selected` is a past idx, `history[selectedCellIdx]` is correct.
- Edge case: `history` is a sparse array by idx. Plan §3.2 handleRollback sets `currentMealIdx = targetIdx` but `history` is built by filter/push, so `history[selectedCellIdx]` is **array index**, not `history.find(h => h.idx === selectedCellIdx)`. After rollback + branch actions, the history array indices and meal.idx values may diverge. Example: after rollback to idx=2, `history = [{idx:0,...},{idx:1,...}]` — array length=2, array[2] is undefined. If user then clicks timeline cell idx=1 (selectedCellIdx=1) and opens confirm, `history[1]` is correct. But if selectedCellIdx=0 and history[0].idx=0, also fine. **However**: plan §3.2 handleEat builds `{idx: clock.idx, ...}` and pushes — never gaps. history entries are in order. So `history[selectedCellIdx]` assumes `history` is sorted and dense from 0 to currentMealIdx-1. This holds IF rollback always produces a dense 0..targetIdx-1 range. Confirmed: `filter(h => h.idx < targetIdx)` preserves all prior entries. So `history[n].idx === n` is an invariant — `history[selectedCellIdx]` is correct.
- Verdict on BLOCKER-NEW-3: **NOT ACTUALLY BLOCKING**. The history array is always dense 0..N-1 because eat/skip always push sequential idx. `history[selectedCellIdx]` == `history.find(h => h.idx === selectedCellIdx)` as long as entries are in order and no gaps. The invariant holds. **Downgrade to NB.**

**NB-1-iter2 (orphan field in struct): `SessionInternalState.dismissedDerivedBanners`**
- Already covered under BLOCKER-NEW-1. The struct field is orphaned by 修订 B. Even if impl drops it from struct, the §3.1 `UseSandboxResult.dismissedBannerIds` and the §3.3 filter formula will cause confusion. The real blocker is §3.1 + §3.3 not being retracted.

**NB-2-iter2: §3.3 `refine_conflict` banner has `dismissable: true` (not updated from 修订 B)**
- File: `plans/S-03.plan.md:227`
- 修订 B decided derived banners are `dismissable: false`. §3.3 still says `dismissable: true`. This is the source of BLOCKER-NEW-1.

**NB-3-iter2: Done state — `s_cold.days = 14` → `totalMeals = 28`, not 14**
- File: `apps/sandbox-lab/src/mocks/sbxMocks.ts:45-48`
- `s_cold: {days: 14, status: "done"}`. If `totalMeals = activeSession.days * 2` (plan §3.1), then `s_cold` has 28 total meals, not 14. The plan says "currentMealIdx: 14 for status='done'" but 14 < 28, so s_cold would not be in "done" state by the `currentMealIdx >= totalMeals` criterion. The plan assumes `totalMeals = 14` for all sessions. The mock `TOTAL_MEALS = 14` (sbxMocks:257) is a constant — but plan §3.1 says `totalMeals = activeSession.days * 2`. For s_cold (14 days), that's 28. The mock data is inconsistent with the formula. 
- Impact: `s_cold` will never show "done" banner under the correct formula. The plan's `修订 A` says "currentMealIdx: 14 for s_no_spicy/s_cold (status='done')" — for s_no_spicy (7 days → 14 meals), 14 == totalMeals, correct. For s_cold (14 days → 28 meals), 14 < 28 — s_cold will appear mid-session, not done.
- Severity: **NB** (does not break core eat/rollback/branch flow, only affects done-state display for one mock session). But it's a spec bug worth flagging.

**NB-4-iter2: `TopBar` needs `activeSessionName` but hook exposes `activeSessionId` — App must look up name**
- File: `apps/sandbox-lab/src/components/TopBar.tsx:9` prop `activeSessionName: string`
- Plan §5 says `onSelectSession={sb.setActiveSessionId}` but does not address how App computes `activeSessionName` from `activeSessionId`. App must do `sessions.find(s => s.id === sb.activeSessionId)?.name ?? ""`. Plan silently omits this derivation. Non-blocking (trivial one-liner) but plan's §5 is incomplete.

**NB-5-iter2: `SummaryDrawer.onOpenTrace` receives `Meal` but plan §5 wires `onOpenTrace={() => window.alert(...)}` — signature mismatch**
- File: `apps/sandbox-lab/src/components/modals/SummaryDrawer.tsx:19` — `onOpenTrace?: (meal: Meal) => void`
- Plan §5: `onOpenTrace={() => window.alert(...)}` — ignores the `meal` arg, but TypeScript will accept this (callback with fewer params is assignable). No compile error. Non-blocking.

---

### Specific Audit Questions from Task Brief

**Q3: Done state placeholder inline JSX vs proper component**
- Plan's "~5 行 inline JSX" approach is fine for S-03 scope. The constraint "不改 components" is stated in §2. Inline JSX in App.tsx does not violate that. No component boundary needed for a placeholder.

**Q4: Per-session state map — "done" sessions need 14 entries, mock HISTORY has 4**
- Plan §0 修订 A: "currentMealIdx: 14 for s_no_spicy/s_cold" + "session.status == 'done' → history 全 14 顿 (从 HISTORY 扩展, mock 补 10 顿 dummy)". The plan acknowledges the gap and says "mock 补 10 顿 dummy". This is a deliberate simplification. Not a blocker.
- However, as noted in NB-3-iter2, s_cold (14 days → 28 meals) will not reach done state at idx=14 under the correct formula. Minor inconsistency.

---

### Summary: Blocker Count vs Iter 1

| | Iter 1 | Iter 2 |
|---|---|---|
| Blocking issues | 5 | 2 |
| Non-blocking findings | 7 | 5 |

Iter 1 BLOCKERs #1-#5: all resolved by 修订 A-J. Two new BLOCKERs introduced by 修订 inconsistencies not propagated back to the main spec sections.

**VERDICT: BLOCKED**

1. **BLOCKER-NEW-1** (plan:147 + plan:230): `§3.1 UseSandboxResult` still declares `dismissedBannerIds: Set<string>` and `§3.3` filter formula still references it. 修订 B says remove this field, but §3.1 and §3.3 were not updated. Implementer following the contract spec will implement the stale-id bug that 修订 B was supposed to fix. Fix: delete `dismissedBannerIds` from §3.1 interface, update §3.3 filter to `banners = [...derivedBanners, ...transientBanners]` (no filter, since transients are cleared on advance and derived are always-on), and set `dismissable: false` on `refine_conflict` banner in §3.3:227.

2. **BLOCKER-NEW-2** (plan:243): `§3.4 useKeyboard` code block uses `[opts.onEsc]` dep-array, directly contradicting 修订 H which specifies empty deps `[]` + internal ref pattern. Codex will implement §3.4 code literally, creating the re-attach bug. Fix: update §3.4 code block to use `useRef<() => void>` pattern with `useEffect(()=>{...}, [])`.

## Iter 3
[Manual adversarial audit — Codex unavailable (quota), iter=3 confirming iter 2 fixes]

### Re-check: Iter 2 BLOCKERs

**BLOCKER-NEW-1 (§3.1 `dismissedBannerIds` + §3.3 filter formula) → RESOLVED**
- `plans/S-03.plan.md:147-149`: §3.1 interface has no `dismissedBannerIds` field. Interface exposes `transientBanners`, `derivedBanners`, `banners` only. Clean.
- `plans/S-03.plan.md:234`: §3.3 filter is `banners = [...derivedBanners, ...transientBanners]` — no dismiss filter, matches 修订 B intent.
- `plans/S-03.plan.md:230`: `refine_conflict` banner has `dismissable: false`. Correct.
- Residual: `plans/S-03.plan.md:223-225` `dismissBanner` implementation description still references "`dismissedBannerIds` Set". This is an orphan paragraph — the state no longer exists in §3.1, the filter in §3.3 does not reference it. Implementer will correctly reduce `dismissBanner` to just `transientBanners.filter(b => b.id !== id)`. **Downgraded to NB** (§3.1 + §3.3 are the authoritative contract; §3.2 prose is secondary).

**BLOCKER-NEW-2 (§3.4 `useKeyboard` deps `[opts.onEsc]`) → RESOLVED**
- `plans/S-03.plan.md:241-249`: code block uses `useRef(opts.onEsc)` + `handlerRef.current = opts.onEsc` + `useEffect(()=>{...}, [])`. Empty deps. Matches 修订 H exactly.
- App onEsc priority chain `plans/S-03.plan.md:255-260`: four-layer priority `closeConfirm → closeSummary → closeRefineModal → selectCell(null)`. Correct.

### Residual Non-blocking findings (from iter 2, not re-blocking)

- **NB-residual-1**: `plans/S-03.plan.md:223-225` `dismissBanner` prose references orphaned `dismissedBannerIds`. Trivial: implementer should implement as `transientBanners = transientBanners.filter(b => b.id !== id)` only.
- **NB-residual-2**: `plans/S-03.plan.md:213` handleRollback step 6 `confirmKind = null` was not deleted. 修订 E decided `handleRollback` should not close the modal internally (ConfirmModal's `onClose` covers it). Having step 6 is idempotent (setting null twice is harmless) but is dead code per 修订 E. Not blocking.
- **NB-residual-3** (iter 2 NB-3-iter2): `s_cold.days=14 → totalMeals=28`, mock initial idx=14 < 28 → s_cold won't show done banner. Minor mock inconsistency only; does not affect core flow.

### Summary: Blocker Count Trend

| | Iter 1 | Iter 2 | Iter 3 |
|---|---|---|---|
| Blocking issues | 5 | 2 | 0 |
| Non-blocking findings | 7 | 5 | 3 (residual) |

Converging: 5 → 2 → 0. All blockers resolved.

VERDICT: APPROVED

No blocking issues remain. The two iter 2 BLOCKERs (§3.1 interface cleanup + §3.4 useRef/empty-deps pattern) are correctly addressed in the current plan v2. Residual NBs are implementation-time trivialities that do not invalidate the spec.
