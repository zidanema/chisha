## Iter 2

**Audit scope**: S-06b plan v2, adversarial review against actual codebase.

---

### What was resolved from iter 1

- **BLOCKER-1 (sid path isolation)**: RESOLVED. §3.2.a introduces `_state_path_for_sid(sid, root)` that directly derives the sid-aware path without calling `state()` / `_state_path()`. The helper is correctly implemented: `sid is None / "_default"` → `_state_path(root)` (existing flat default); non-default sid → `_sessions_root(root) / sid / "state.json"`. Verified `_sessions_root` exists at sandbox.py:237, `_state_path` at sandbox.py:71, `_DEFAULT_SID` at sandbox_context.py:39. Path derivation is correct.

- **BLOCKER-2 (ContextVar bypass)**: RESOLVED. `advance_meal` no longer wraps with `set_sandbox_session(sid)`. Path is derived directly. The S-04 stub `state()` line 100 `del session_id` is not called. Non-default sid now correctly writes to `sessions/{sid}/state.json`.

- **FINDING-3 (total_meals sentinel semantics)**: RESOLVED. §0 修订 B documents `total_meals=14` as done-sentinel; idx ∈ [0, total] legal; `meal_idx_to_slot` only valid for [0, total-1]; caller must handle idx == total separately.

---

### New issues found in v2

**BLOCKER-3 — §6 step 2 contradicts §3.2 / §0 修订A: still mentions `with set_sandbox_session(sid):` wrapper**

`plans/S-06b.plan.md` line 220:
```
加 `advance_meal(*, sid=None, root=None) -> dict` (带 `with set_sandbox_session(sid):` 包 + `with _STATE_LOCK:` 双重锁)
```
This directly contradicts §3.2 (step 1 says `_state_path_for_sid`, no mention of `set_sandbox_session`) and §0 修订 A ("不依赖 ContextVar"). If an implementer follows §6 literally, they will wrap with `set_sandbox_session(sid)` which is a no-op (ContextVar not consumed by `_state_path`), giving false confidence the sid isolation works. The authoritative spec (§3.2 code block) is correct, but §6 is a stale artifact that will cause implementation confusion.

This is a BLOCKER because §6 is the step-by-step implementation guide that Codex/implementers follow directly, and the contradicting line will produce incorrect code.

---

**FINDING-4 — `list_sessions` `has_state: False` hardcode for non-default sessions not updated**

`sandbox.py` line 300: non-default session entries return `"has_state": False` with comment `# S-06b 才真造 state.json`. After S-06b's `advance_meal` can write `sessions/{sid}/state.json`, this field remains hardcoded `False` for non-default sessions even after a real state file exists. The plan does not mention updating `list_sessions` to dynamically check `(sessions_root / sid / "state.json").exists()`.

`web_api.py` line 977 uses `has_state: bool = False` in the Pydantic schema, which cascades to the frontend `GET /sandbox/sessions` response. The inconsistency is not a crash but is a silent semantic error: after `advance_meal(sid="s1")` creates the state file, `list_sessions` still reports `has_state=False` for s1.

Severity: FINDING (not BLOCKER) — plan explicitly defers S-06c integration, and `has_state` staleness doesn't break `advance_meal` correctness. But S-06c implementer must know to fix this.

---

**FINDING-5 — `test_advance_meal_non_default_sid` test setup requires `sessions/s1/` directory to pre-exist**

Plan §4.2 says the test "手工写 `logs/sandbox/sessions/s1/state.json`". `advance_meal` calls `state_p.exists()` → raises if not found. But the test also needs the parent directory `sessions/s1/` to exist before writing the state file. The plan doesn't mention creating the directory via `create_session("s1", root=root)` or `mkdir(parents=True)`. This is a minor test-writing pitfall — not a logic bug in the plan's contract, but an implementer trap.

Severity: FINDING (non-blocking, obvious to fix during impl).

---

**FINDING-6 — `advance_meal` step 7: dinner→day advance uses `cur_idx % 2 == 1` but `cur_idx` is the index BEFORE increment**

From the §3.2 code block (line 37):
```python
if cur_idx % 2 == 1:  # was dinner
    cur_date = dt.date.fromisoformat(s["current_date"])
    s["current_date"] = (cur_date + dt.timedelta(days=1)).isoformat()
```
`cur_idx` is the index of the meal just consumed. `cur_idx % 2 == 1` means the consumed meal was dinner (odd index). This is semantically correct: after eating dinner (idx=1, idx=3, …), day advances. Verified: idx=0 (lunch) → new_idx=1 (dinner), no date change ✓. idx=1 (dinner) → new_idx=2 (next-day lunch), date+1 ✓. The logic is correct.

---

### Cross-file invariant check

- `docs/CONTRACTS.md`: No Sandbox section contract is violated. `advance_meal` is additive-only (new function, existing `advance()` untouched). `_STATE_LOCK` is reused correctly. `_reject_nondefault_sid` is deliberately not called (correct per §7 Q1).
- `sandbox_context._DEFAULT_SID` = `"_default"` at line 39. `_state_path_for_sid` comparison `sid == _DEFAULT_SID` is correct (not hardcoded string comparison).
- `sandbox_decision_diff.py` is a pure function module; no imports from recommendation chain (`api/recall/score/rerank/refine`). No regression risk on L1/L2/L3.
- Faithful Refine invariant: not touched by this plan. CONTRACTS.md §Faithful Refine: not impacted.
- `test_advance_meal_exceeds_total_raises` test plan: total_meals=2, advance × 2 ok, 3rd raises. Verify: idx starts 0 → after 2 advances idx=2 = total (done sentinel, ok per §0 修订B); 3rd advance new_idx=3 > 2 → raises. Correct.

---

### Summary

| Issue | Type | Status |
|---|---|---|
| iter1 BLOCKER-1: sid path isolation | BLOCKER | RESOLVED |
| iter1 BLOCKER-2: ContextVar bypass | BLOCKER | RESOLVED |
| iter1 FINDING-3: total_meals semantics | FINDING | RESOLVED |
| BLOCKER-3: §6 step 2 contradicts §3.2/§0 (set_sandbox_session残留) | BLOCKER | NEW |
| FINDING-4: list_sessions has_state not updated | FINDING | NEW |
| FINDING-5: test setup missing mkdir for sessions/s1 | FINDING | NEW |
| FINDING-6: dinner-advance logic verification | FINDING | CONFIRMED CORRECT |

**Total blocking issues this iter: 1** (vs iter 1: 2 blockers). Converging.

VERDICT: BLOCKED
1. [BLOCKER-3] `plans/S-06b.plan.md` line 220 (§6 step 2): "带 `with set_sandbox_session(sid):` 包" contradicts §0 修订A / §3.2 which explicitly say NOT to use ContextVar wrapper. This stale line must be removed before implementation to prevent Codex from writing incorrect code that silently breaks sid isolation.

## Iter 3

**Audit scope**: S-06b plan v2 (iter 3 input: BLOCKER-3 fix applied + FINDING-4/5 noted), adversarial review.

---

### Resolution check from iter 2

**BLOCKER-3 (§6 step 2 stale `set_sandbox_session` reference)**: RESOLVED.
`plans/S-06b.plan.md` line 221 now reads: "用 `_state_path_for_sid` + `with _STATE_LOCK:`, **不**用 ContextVar/`set_sandbox_session` 包". The contradiction is gone. §6 step 2 is now consistent with §0 修订A and §3.2 code block.

**FINDING-4 (list_sessions has_state hardcode)**: Deferred to S-06c per plan. Acknowledged.

**FINDING-5 (test_advance_meal_non_default_sid missing mkdir)**: Plan §4.2 (line 186) now explicitly states "手工 mkdir `logs/sandbox/sessions/s1/` + 手工写 `logs/sandbox/sessions/s1/state.json`". mkdir is called out. The test setup pitfall is addressed.

---

### Adversarial checks — iter 3 clean pass

1. **Missed dependencies**: `_STATE_LOCK` (sandbox.py), `_state_path` (sandbox.py:71), `_sessions_root` (sandbox.py:237), `_DEFAULT_SID` (sandbox_context.py:39) — all verified to exist. Import `from chisha.sandbox_context import _DEFAULT_SID` in `_state_path_for_sid` is valid.

2. **Signature / side effects**: `advance_meal` reads and writes `state.json` under `_STATE_LOCK`. Existing `advance()` also uses `_STATE_LOCK`. No deadlock risk — two separate call sites, same non-reentrant lock, not nested. `meal_idx_to_slot` is pure. `build_decision` is pure. No side effects beyond state.json write.

3. **Regression risk**: `advance()` (whole-day jump) is untouched. `init/reset/disable/state/list_sessions` are untouched. `sandbox_decision_diff.py` imports nothing from `api/recall/score/rerank/refine`. Faithful Refine invariant not touched. Baseline L2 snapshot expected 0 diff.

4. **Test coverage**: 8 cases for meal_clock (including non-default-sid path isolation + done-sentinel boundary), 6 cases for decision_diff (eat/same-twice/skip/no-change/empty-prefs/taste-down). Edge cases covered. `test_advance_meal_non_default_sid` now has explicit mkdir in setup — implementation trap closed.

5. **Cross-file invariants (CONTRACTS.md)**: No sandbox contract violated. `_reject_nondefault_sid` intentionally not called in `advance_meal` (§7 Q1 documents rationale). `_DEFAULT_SID` comparison uses imported constant, not hardcoded string.

6. **Affected files existence**: `chisha/sandbox.py` ✓, `chisha/sandbox_context.py` ✓ (for `_DEFAULT_SID`), `tests/` directory ✓. New files `chisha/sandbox_decision_diff.py` and test files are net-new, no collision risk.

7. **Faithful Refine**: Not touched by this plan. No refine chain files in affected list.

---

### No new blocking issues found.

| Issue | Type | Iter 3 status |
|---|---|---|
| iter2 BLOCKER-3: §6 step 2 stale ContextVar line | BLOCKER | RESOLVED |
| iter2 FINDING-4: list_sessions has_state | FINDING | Deferred S-06c (acknowledged) |
| iter2 FINDING-5: test mkdir setup | FINDING | RESOLVED in plan text |

**Total blocking issues this iter: 0** (vs iter 2: 1 blocker). Converging → done.

VERDICT: APPROVED

## Iter 1 (diff review)

**Audit scope**: Actual working-tree diff for S-06b (sandbox.py +94 lines, sandbox_decision_diff.py new 144 lines, 2 new test files, 19 new tests). Adversarial focus per caller.

---

### 1. Plan/Diff Conformance — 修订 A / B / C

**修订 A** (advance_meal uses own sid-aware path helper, bypasses state()):
`_state_path_for_sid` at sandbox.py:430-442 is exact match to §3.2.a spec. Lazy import of `_DEFAULT_SID` avoids circular import. `advance_meal` calls only `_state_path_for_sid(sid, root)` — `state()` and `set_sandbox_session` never appear. CONFORMANT.

**修订 B** (total_meals done sentinel, idx ∈ [0, total]):
sandbox.py:484 guard is `if new_idx > total:`, allowing new_idx == total (done sentinel). Docstring explicitly documents the boundary semantics. CONFORMANT.

**修订 C** (build_decision history[-1] = current meal contract):
sandbox_decision_diff.py module docstring (lines 6-7) and function docstring (lines 37-38) both state the contract. Implementation at line 90 uses `history[:-1]`. CONFORMANT.

---

### 2. Sid-Aware Path Correctness

`_state_path_for_sid(sid=None)` → `_state_path(root)` = `logs/sandbox/state.json`. `_state_path_for_sid(sid="s1")` → `_sessions_root(root) / "s1" / "state.json"` = `logs/sandbox/sessions/s1/state.json`. Both branches correct.

`test_advance_meal_non_default_sid` validates actual filesystem isolation: writes default state at `current_meal_idx=5`, advances sid="s1", asserts s1 disk=1 AND default disk unchanged=5. Passes.

FINDING (non-blocking): `_state_path_for_sid` does not call `_validate_sid(sid)` before path construction. Adversarial sid like `"../../etc"` would path-traverse. Pre-existing pattern in advance() (which uses `_reject_nondefault_sid` instead). S-06c should add `_validate_sid(sid)` call in `advance_meal`. Not in S-06b scope (single-user assumption, CONTRACTS.md).

---

### 3. Race / Lock Semantics

`_STATE_LOCK` is module-level non-reentrant lock. `advance_meal` acquires it for the full read-modify-write cycle. `advance()`, `reset()`, `disable()` also use the same lock non-nestedly — no deadlock. Single global lock covers all sid buckets (conservative: serializes s1/s2 advances even though they touch different files). Given single-user assumption (sandbox.py:26-28), this is correct and safe. ACCEPTABLE.

Pre-existing finding: `state_p.write_text` is not atomic (no temp-file swap). Mid-write crash corrupts state. Pre-existing in `advance()`/`disable()` — not a new regression.

---

### 4. Build_Decision Purity

`del root` on line 59 confirms no filesystem access. No global state. No imports from recommend chain. Deterministic: Python 3.7+ dict insertion-order guarantees same output for same input. `history=[]` with non-None `picked_rec`: `history[:-1]` = `[]`, `same_count=0`, no crash — correct behavior. `from typing import Any` is imported but unused (minor lint noise). PURE AND DETERMINISTIC.

---

### 5. Out-of-Scope Creep

`git diff HEAD -- chisha/web_api.py chisha/api.py` = 0 lines. Existing sandbox functions (`advance`, `init`, `reset`, `disable`, `state`, `list_sessions`, `create_session`, `delete_session`, `rename_session`, `record_l1_extraction`) are byte-identical to HEAD. `from typing import Literal as _Literal` appears mid-module (line 411) — non-idiomatic placement but functionally correct. SCOPE CLEAN.

---

### 6. Test Coverage Gaps

11 tests in test_sandbox_meal_clock.py (plan spec called for ~8): includes all plan-specified cases plus `corrupt_state_raises` and `default_explicit_sid` extras. 8 tests in test_sandbox_decision_diff.py (plan spec "5+"). All 19 pass.

FINDING (non-blocking): `meal_idx_to_slot(14)` (done sentinel value) is not tested. It silently returns `("lunch", 8)` — no ValueError. Spec says `[0, 13]` valid but does not mandate a guard at 14. Callers must handle idx==total_meals themselves per §0 修订B. A docstring note suffices; no test gap that affects correctness.

---

### Summary

| Check | Result |
|---|---|
| 修订 A (no ContextVar) | PASS |
| 修订 B (done sentinel) | PASS |
| 修订 C (history contract) | PASS |
| sid path isolation | PASS — sessions/{sid}/state.json confirmed by test |
| sid traversal hardening | FINDING: no _validate_sid in advance_meal; non-blocking |
| Lock semantics | ACCEPTABLE — global lock safe for single-user |
| build_decision purity | PASS — no side effects, deterministic |
| Out-of-scope creep | CLEAN — web_api/api.py 0 delta |
| Test coverage | PASS — 19/19 green; minor gap meal_idx_to_slot(14) not tested |

No blocking issues. All three plan revisions faithfully implemented. Sid isolation correct and tested.

VERDICT: APPROVED
