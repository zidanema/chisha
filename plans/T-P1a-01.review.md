## Check 1: recall.py combo_passes_plate_rule call sites
Findings:
- `chisha/recall.py:579` calls `combo_passes_plate_rule([cm], profile, intent=intent)`.
- `chisha/recall.py:587` calls `combo_passes_plate_rule(c, profile, intent=intent)`.
- `chisha/recall.py:610` calls `combo_passes_plate_rule(dishes, profile, intent=intent)`.
- `chisha/recall.py:618` calls `combo_passes_plate_rule(dishes, profile, intent=intent)`.

Verdict per check: PASS. All four observed call sites pass `intent=intent`.

## Check 2: tests/test_recall.py bool callers
Findings:
- `tests/test_recall.py:3-12` imports `combo_passes_plate_rule`, not `combo_passes_plate_rule_with_reason`.
- `tests/test_recall.py:127` asserts `combo_passes_plate_rule([veg, meat], basic_profile)`.
- `tests/test_recall.py:129` asserts `not combo_passes_plate_rule([meat], basic_profile)`.
- `tests/test_recall.py:131` asserts `not combo_passes_plate_rule([veg], basic_profile)`.
- The function signature in `chisha/recall.py:406-411` is `combo_passes_plate_rule(..., intent=None) -> bool`, and the implementation returns boolean literals at `chisha/recall.py:417-430`.
- The reason-returning sibling is separate at `chisha/recall.py:433-438` and returns `tuple[bool, str | None]`.

Verdict per check: PASS. The existing bool interface is preserved for these tests.

## Check 3: debug_recommend._build_l1_trace intent threading
Findings:
- `_build_l1_trace` is defined at `chisha/debug_recommend.py:202-210`; it accepts `intent=None` at `chisha/debug_recommend.py:209`.
- Inside `_build_l1_trace`, combo generation passes `intent=intent` into `build_combos_for_restaurant` at `chisha/debug_recommend.py:254-256`.
- `_build_l1_trace` appends the methodology/refine-break hard-filter event only when `intent.allows_methodology_break()` is true at `chisha/debug_recommend.py:295-304`.
- Observed call site `chisha/api.py:368-370` calls `_build_l1_trace(profile, rests, tagged, meal_log, today, meal_type=meal_type)` and does not pass `intent=`.
- Observed call site `chisha/debug_recommend.py:592` calls `_build_l1_trace(profile, rests, tagged, meal_log, today, meal_type=meal_type)` and does not pass `intent=`.
- Observed call site `tests/integration/test_claude_code_cli_e2e.py:56-58` calls `_build_l1_trace(..., meal_type="lunch")` and does not pass `intent=`.
- `chisha/refine.py` does not call `_build_l1_trace` in the observed refine path; it calls `recall(..., intent=intent if not intent.is_empty() else None, n=n)` at `chisha/refine.py:98-102`.
- Because refine uses `recall` directly, the L0-C relaxation can affect combo generation via `recall.py:681-683`, but the `_build_l1_trace` event append at `chisha/debug_recommend.py:295-304` is not executed.
- `chisha/web_api.py:184-189` persists refine by replacing `base_trace["refine"]` and writing the trace; those lines do not add or regenerate `base_trace["l1"]["hard_filter_events"]`.

Verdict per check: BLOCKER. Refine behavior threads `intent` into recall, but the refine trace path does not execute the only observed code that appends the L0-C methodology event, so L0-C unlock events are silently absent from persisted L1 trace/debug output.

## Check 4: What-if immutability in debug_what_if.py
Findings:
- `_build_what_if_trace` is defined at `chisha/debug_what_if.py:286-302`.
- `_build_what_if_trace` imports L2/L3 formatting helpers at `chisha/debug_what_if.py:304-308`; it does not import or call `hard_filter`, `recall`, or `_build_l1_trace` in the observed function body.
- The returned preview trace sets `"recall_latency_ms": 0` at `chisha/debug_what_if.py:378`.
- The returned preview trace sets `"l1": base.get("l1") or {"trace": "frozen, see base"}` at `chisha/debug_what_if.py:382`.

Verdict per check: PASS. What-if reuses the base L1 trace, including base `hard_filter_events`; it does not rerun hard_filter with a new intent in `_build_what_if_trace`.

## Check 5: halal keyword coverage sanity
Findings:
- The halal ban keyword tuple is defined at `chisha/l0_constraints.py:23-26` as `("猪", "培根", "火腿", "腊肠", "腊肉", "热狗")`.
- For halal, the logic first bans `processed_meat_flag` at `chisha/l0_constraints.py:96-98`, then scans `_dish_text(dish)` for those keywords at `chisha/l0_constraints.py:99-103`.
- `_dish_text` uses only `canonical_name` and `raw_name` at `chisha/l0_constraints.py:59-61`.
- `叉烧螺蛳粉` appears in data with `raw_name` and `canonical_name` at `data/shenzhen-bay/dishes_tagged.json:312995-312999`, `main_ingredient_type` is `"红肉"` at `data/shenzhen-bay/dishes_tagged.json:313002-313004`, and `processed_meat_flag` is `false` at `data/shenzhen-bay/dishes_tagged.json:313010-313012`. Current halal logic does not match `叉烧`, so this dish escapes.
- `肉夹馍` appears with `raw_name` and `canonical_name` at `data/shenzhen-bay/dishes_tagged.json:317365-317368`, `main_ingredient_type` is `"主食"` at `data/shenzhen-bay/dishes_tagged.json:317371-317374`, and `processed_meat_flag` is `false` at `data/shenzhen-bay/dishes_tagged.json:317380-317382`. Current halal logic does not match it; whether this is correct depends on actual filling, which the current fields do not identify.
- `原味螺蛳粉+网红炸蛋+热狗` appears at `data/shenzhen-bay/dishes_tagged.json:313575-313579`; it is banned because `processed_meat_flag` is `true` at `data/shenzhen-bay/dishes_tagged.json:313591-313593` and also contains keyword `热狗` from `chisha/l0_constraints.py:24-26`.
- `炭烤羔羊肉串` appears at `data/shenzhen-bay/dishes_tagged.json:15392-15395`, has `main_ingredient_type` `"红肉"` at `data/shenzhen-bay/dishes_tagged.json:15399-15401`, and `processed_meat_flag` `false` at `data/shenzhen-bay/dishes_tagged.json:15407-15409`. Current halal logic preserves it because no halal keyword matches.
- `现烤羊肉串肉夹馍` appears at `data/shenzhen-bay/dishes_tagged.json:242021-242024`, has `main_ingredient_type` `"红肉"` at `data/shenzhen-bay/dishes_tagged.json:242028-242030`, and `processed_meat_flag` `false` at `data/shenzhen-bay/dishes_tagged.json:242036-242038`. Current halal logic preserves it because no halal keyword matches.

Verdict per check: BLOCKER. At least one observed pork-coded dish (`叉烧螺蛳粉`) escapes the halal hard constraint because the ban list lacks `叉烧` and the tag has `processed_meat_flag=false`.

## Check 6: L0-A allergy substring over-matching
Findings:
- `_dish_text` concatenates only `canonical_name` and `raw_name` at `chisha/l0_constraints.py:59-61`; it does not inspect tags, nutrition fields, or ingredients.
- `dish_violates_l0_a` iterates `c.medical_allergies` and returns the first allergy where `allergy in text` at `chisha/l0_constraints.py:71-77`.
- The implementation comment explicitly says substring matching is intentional and medical safety is prioritized over recall depth at `chisha/l0_constraints.py:68-69`.
- For `profile.allergies=["蛋"]`, a dish named `蛋黄派` would be filtered because `蛋` is a substring of the concatenated name per `chisha/l0_constraints.py:59-61` and `chisha/l0_constraints.py:73-76`.
- For `profile.allergies=["蛋"]`, a dish named `木耳炒蛋` would also be filtered for the same reason at `chisha/l0_constraints.py:59-61` and `chisha/l0_constraints.py:73-76`.

Verdict per check: PASS WITH NOTE. This is medical-safety-first behavior as documented in the code, but it will over-filter snack or ambiguous names when the surface name contains the allergy substring.

## Check 7: hard_filter_events ordering stability
Findings:
- `hard_filter` initializes `l0_a_dropped` and `l0_b_dropped` as dict accumulators at `chisha/recall.py:241-242`.
- In the per-dish loop, L0-A is checked before L0-B at `chisha/recall.py:255-279`, and an L0-A hit `continue`s before the L0-B check at `chisha/recall.py:261-267`.
- Event emission appends all L0-A accumulator entries first at `chisha/recall.py:323-329`, then all L0-B accumulator entries at `chisha/recall.py:330-336`.
- The accumulator keys are inserted while iterating the input `dishes` list at `chisha/recall.py:248-279`; no set-based event accumulator is used in the observed event append path.

Verdict per check: PASS WITH NOTE. Category order is deterministically L0-A events before L0-B events in the observed code. Within each category, ordering follows the dict insertion order of first-hit allergy/rule keys from the input dish order.

## Blockers
1. Check 3: refine does not execute the L1 trace-builder path that appends the L0-C methodology/refine-break event; `recall` receives `intent`, but persisted trace only merges refine metadata and leaves `l1.hard_filter_events` unchanged (`chisha/refine.py:98-102`, `chisha/debug_recommend.py:295-304`, `chisha/web_api.py:184-189`).
2. Check 5: halal hard constraint misses observed pork-coded `叉烧螺蛳粉` because the keyword list lacks `叉烧` and the dish has `processed_meat_flag=false` (`chisha/l0_constraints.py:23-26`, `data/shenzhen-bay/dishes_tagged.json:312995-313012`).

## Notes
- Check 1 and Check 2 passed: combo call sites pass `intent=intent`, and the public bool interface remains intact (`chisha/recall.py:579`, `chisha/recall.py:587`, `chisha/recall.py:610`, `chisha/recall.py:618`, `tests/test_recall.py:127-131`).
- What-if preserves base L1 immutably by assigning `"l1": base.get("l1")` in the preview trace (`chisha/debug_what_if.py:358-382`).
- L0-A only matches dish names (`canonical_name` + `raw_name`), not tags or ingredients (`chisha/l0_constraints.py:59-61`, `chisha/l0_constraints.py:73-76`).

VERDICT: BLOCKED
