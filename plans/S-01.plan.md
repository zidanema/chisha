# S-01 · 实施计划

> Task: 创建 `apps/sandbox-lab/` Vite + React + TS 工程脚手架,跑通空壳 + 主题/accent 切换 demo。
> Spec: `specs/S-01.md` · Design brief: `docs/design_briefs/2026-05-19-sandbox-lab.md`

## 1. Affected files (新建,绝不动现有 apps/*)

```
apps/sandbox-lab/
├── package.json                    新
├── vite.config.ts                  新
├── tsconfig.json                   新
├── index.html                      新
├── README.md                       新 (≤ 50 行,起步说明)
└── src/
    ├── main.tsx                    新
    ├── App.tsx                     新 (demo 区块)
    ├── styles.css                  新 (CSS variables + 主题切换)
    └── hooks/
        └── useTweaks.ts            新 (本任务只暴露 accent + theme,5 字段扩展是 S-02 的事)
```

不动:
- `apps/debug-ui/**` 完全不动
- `apps/web/**` 完全不动
- `chisha/**` 完全不动

## 2. 文件具体内容

### `apps/sandbox-lab/package.json`

镜像 debug-ui 的 deps 版本,只换 name + port:

```json
{
  "name": "chisha-sandbox-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "chisha Sandbox Lab 白盒时光机 (D-088, 内部 debug 沙箱)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 5175",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
```

### `apps/sandbox-lab/vite.config.ts`

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

const DEFAULT_PORT = 5175;
const port = Number(process.env.VITE_PORT) || DEFAULT_PORT;

export default defineConfig({
  plugins: [react()],
  server: {
    host: "127.0.0.1",
    port,
    strictPort: true, // 端口被占 → 显式报错而非静默漂移; 多 worktree 用 VITE_PORT 覆盖
    proxy: {
      "/api": {
        target: process.env.VITE_API_TARGET || "http://127.0.0.1:8765",
        changeOrigin: false,
      },
    },
  },
  build: { outDir: "dist", sourcemap: true },
});
```

**多 worktree 端口冲突说明** (Codex BLOCKING #1):
- 5175 已被另一个 worktree (debug_trace_merge) 占。本 worktree 实测时用 `VITE_PORT=5180 npm run dev`
- README 必须文档化此 env 覆盖,镜像 debug-ui 的 VITE_API_TARGET 模式

### `apps/sandbox-lab/tsconfig.json`

复刻 debug-ui tsconfig.json (strict + jsx react-jsx + bundler resolution),include=src。

### `apps/sandbox-lab/tsconfig.node.json` (取消,镜像 debug-ui)

**Codex BLOCKING #2 决议**: debug-ui 没有 tsconfig.node.json,本任务镜像它 → **不建** tsconfig.node.json。
- spec S-01.md §1.3 与 §affected files 列出此文件,与 debug-ui 不一致 → 视为 spec 笔误,本任务不交付该文件
- S-10 文档收尾时同步把 spec 的 tsconfig.node.json 引用删除
- 验证:`apps/debug-ui/tsconfig.json` 已经包含 `include: ["src"]`,vite.config.ts 本身不被 tsc typecheck,Vite 运行时直接 esbuild,无需 node tsconfig

### `apps/sandbox-lab/index.html`

```html
<!doctype html>
<html lang="zh-CN" data-theme="light">
  <head>
    <meta charset="utf-8" />
    <title>chisha · sandbox lab</title>
    <meta name="viewport" content="width=1440" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Sans+SC:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="theme-light">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### `apps/sandbox-lab/src/main.tsx`

```ts
import React from "react";
import ReactDOM from "react-dom/client";
import { App } from "./App";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

### `apps/sandbox-lab/src/styles.css`

从 `chidiansha-sandbox/project/sandbox-lab/styles.css` 顶部 `:root` 块搬,加 4 套 accent palette 切换 + 主题切换 + demo 区块样式。**只搬本任务需要的部分**:
- `:root` 的 Type / Neutrals (light) / Text / Accent / Semantic / Radii / Shadows / Layout vars
- `body.theme-dark` 覆盖 vars (从原型 line 2173 复刻)
- `.demo-shell` / `.demo-card` / `.demo-row` / `.swatch` 等本任务 demo 用的 class

完整 styles.css 1:1 搬运是 S-02 的事,本任务只要 demo 区块跑得动 + 主题切换看得见。

### `apps/sandbox-lab/src/hooks/useTweaks.ts`

```ts
import { useEffect, useState } from "react";

type Theme = "light" | "dark";
const ACCENTS = ["#6366f1", "#059669", "#e11d48", "#d97706"] as const;
type Accent = (typeof ACCENTS)[number];

type Tweaks = { theme: Theme; accent: Accent };

const ACCENT_PALETTE: Record<Accent, Record<string, string>> = {
  "#6366f1": { accent: "#6366f1", hover: "#4f46e5", soft: "#eef0ff", softer: "#f5f6ff", ink: "#3730a3", ring: "rgba(99,102,241,0.18)" },
  "#059669": { accent: "#059669", hover: "#047857", soft: "#ecfdf5", softer: "#f0fdf4", ink: "#065f46", ring: "rgba(5,150,105,0.18)" },
  "#e11d48": { accent: "#e11d48", hover: "#be123c", soft: "#ffe4e6", softer: "#fff1f2", ink: "#9f1239", ring: "rgba(225,29,72,0.18)" },
  "#d97706": { accent: "#d97706", hover: "#b45309", soft: "#fef3c7", softer: "#fffbeb", ink: "#92400e", ring: "rgba(217,119,6,0.18)" },
};

const KEY = "chisha:sandboxTweaks"; // 与 debug-ui chisha:theme / chisha:tbCollapsed 同前缀 (Codex WARNING #1)

function load(): Tweaks {
  try {
    const raw = localStorage.getItem(KEY);
    if (raw) {
      const v = JSON.parse(raw) as Partial<Tweaks>;
      if (v.theme && v.accent && ACCENTS.includes(v.accent as Accent)) return v as Tweaks;
    }
  } catch { /* ignore */ }
  return { theme: "light", accent: "#6366f1" };
}

export function useTweaks() {
  const [tweaks, setTweaksState] = useState<Tweaks>(load);

  useEffect(() => {
    // accent
    const pal = ACCENT_PALETTE[tweaks.accent];
    const root = document.documentElement;
    root.style.setProperty("--accent", pal.accent);
    root.style.setProperty("--accent-hover", pal.hover);
    root.style.setProperty("--accent-soft", pal.soft);
    root.style.setProperty("--accent-softer", pal.softer);
    root.style.setProperty("--accent-ink", pal.ink);
    root.style.setProperty("--accent-ring", pal.ring);
    // theme
    document.body.classList.toggle("theme-dark", tweaks.theme === "dark");
    document.body.classList.toggle("theme-light", tweaks.theme === "light");
    try { localStorage.setItem(KEY, JSON.stringify(tweaks)); } catch { /* ignore */ }
  }, [tweaks]);

  function setTweak<K extends keyof Tweaks>(k: K, v: Tweaks[K]) {
    setTweaksState((t) => ({ ...t, [k]: v }));
  }

  return { tweaks, setTweak, ACCENTS };
}
```

### `apps/sandbox-lab/src/App.tsx`

```tsx
import { useTweaks } from "./hooks/useTweaks";

export function App() {
  const { tweaks, setTweak, ACCENTS } = useTweaks();

  return (
    <div className="demo-shell">
      <header className="demo-header">
        <div className="brand">
          <span className="brand-mark" />
          <span className="brand-title">Sandbox Lab</span>
          <span className="brand-sub">白盒时光机 · /sandbox-lab (S-01 scaffold)</span>
        </div>
        <div className="demo-controls">
          <span className="control-label">主题</span>
          <button
            className={`pill ${tweaks.theme === "light" ? "on" : ""}`}
            onClick={() => setTweak("theme", "light")}
          >
            浅色
          </button>
          <button
            className={`pill ${tweaks.theme === "dark" ? "on" : ""}`}
            onClick={() => setTweak("theme", "dark")}
          >
            深色
          </button>
          <span className="control-label">主色</span>
          {ACCENTS.map((c) => (
            <button
              key={c}
              aria-label={`accent ${c}`}
              className={`swatch ${tweaks.accent === c ? "on" : ""}`}
              style={{ background: c }}
              onClick={() => setTweak("accent", c)}
            />
          ))}
        </div>
      </header>

      <main className="demo-main">
        <section className="demo-card">
          <h2 className="demo-h">字体 + tokens 验证</h2>
          <p className="demo-p">
            Inter sans-serif + Noto Sans SC 中文混排 — 此句应当字体协调,字重 400。
          </p>
          <p className="demo-p">
            Mono 字号(数字 + trace id):
            <code className="mono"> 0123456789 · tr_D3午_寿司拼盘 · L3=92 </code>
          </p>
          <p className="demo-p" style={{ color: "var(--text-2)" }}>
            tokens 自检: text-1 / text-2 / text-3 / text-4 应呈现 4 档对比。
          </p>
          <div className="demo-row">
            <button className="btn-primary">就这个 →</button>
            <button className="btn-ghost">查看详情</button>
            <span className="chip">✓ 高蛋白</span>
            <span className="chip warn">⚠ 含辣</span>
          </div>
        </section>

        <section className="demo-card">
          <h2 className="demo-h">accent 切换</h2>
          <p className="demo-p">
            点上方 4 个色块,本卡片所有 accent 派生(按钮 / chip / ring / soft)应整套换。
          </p>
          <div className="demo-row demo-row-tight">
            <span className="dot dot-eat" /> eat
            <span className="dot dot-skip" /> skip
            <span className="dot dot-refine" /> refine
            <span className="dot dot-conflict" /> conflict
            <span className="dot dot-explore" /> explore
          </div>
        </section>
      </main>
    </div>
  );
}
```

### `apps/sandbox-lab/README.md`

≤ 50 行,模仿 debug-ui/README.md 起步段落,标 S-01 状态。

## 3. 验证策略

### 3.1 本任务范围内必须通过

1. **依赖安装**: `cd apps/sandbox-lab && npm install` 无 error,生成 `package-lock.json`
2. **dev server**: 本 worktree 5175 已被占,用 `VITE_PORT=5180 npm run dev` 起来 + 浏览器打开 `http://127.0.0.1:5180/` 看到 demo 页
3. **typecheck**: `npm run typecheck` 全绿
4. **build**: `npm run build` 全绿,验 vite bundle 出 dist/index.html + assets (Codex WARNING #3)
5. **chrome-devtools-mcp 验证** (设计 brief §前端自测 + CLAUDE.md §前端自测 — 后者已覆盖 apps/web 和 apps/debug-ui,本任务作为 D-088 第一站,验证标准等同):

   golden path:
   - `mcp__chrome-devtools__navigate` http://127.0.0.1:5180/
   - `take_snapshot` 看 demo 页是否完整渲染 (header + 2 card)
   - `take_screenshot` 截一张做事后核验
   - `evaluate_script`: 读 `getComputedStyle(document.documentElement)` 的 `--accent` / `--accent-soft` / `--accent-ring`,初始应为 indigo 6 个值
   - `click` 第 2 个色块 (`--accent` swatch 绿) → 重读 → 4 个 var 全套变绿
   - `click` "深色" pill → `document.body.classList.contains('theme-dark')` 应 true → `--bg` 变深色
   - `evaluate_script` 取 `<code>` 元素 `getComputedStyle.fontFamily` 应含 `JetBrains Mono`
   - `list_console_messages` 无 error / warning (允许 React StrictMode 双渲染信息)
   - `list_network_requests` 无 4xx / 5xx (字体 + 静态资源 + Vite HMR ws)

   edge case:
   - 刷新页 (`navigate` 同 URL) → 验主题/色 持久 (localStorage 读取生效)
   - `evaluate_script` 注入坏 localStorage: `localStorage.setItem('chisha:sandboxTweaks', '{garbage')` → 刷新 → 应 fallback 到默认 (light + indigo),不抛 (`useTweaks.load()` catch 已覆盖)

6. **持久化**: 切完主题后刷新页,选择保持 (golden path 第 1 步已覆盖)

### 3.2 旧测试回归

- `uv run pytest tests/ -q` — 本任务不动 Python,但跑一遍确认没有副作用 (理论 0 影响,实际跑一遍稳)
- `apps/debug-ui` / `apps/web` 各起 dev server 验证不受影响 — 不强制 (本任务不动这两处)

## 4. 回归风险

- **极低**。新建独立目录,无 import 跨 app,无 package.json workspace 影响 (Codex INFO #1 确认: 根目录无 package.json,无 workspaces 自动 pick up)。
- 风险点:`strictPort: true` + 5175 被占 → vite 直接报 error 退出,易诊断;用 VITE_PORT 覆盖
- 风险点:react 18.3.1 三 hosting (apps/debug-ui / apps/web / apps/sandbox-lab) 共三份 → 磁盘 ~60MB/份,无运行时冲突

## 5. 回滚方案

- `rm -rf apps/sandbox-lab/` 完全回滚,零副作用
- git: 本任务在自己的 commit 里,revert 该 commit 即可

## 6. 文档更新 (本任务范围内必须做)

- `apps/sandbox-lab/README.md` 起步说明 (本任务交付物)
- 不动 `CLAUDE.md` / `docs/decisions.md` / `docs/CONTRACTS.md` (S-10 统一收尾)

## 7. 不做 (S-02 起的事)

- TopBar / Timeline / RecCard / panels 等业务组件
- 完整 styles.css 搬运 (本任务只搬 demo 必需 + token + 主题块)
- Tweaks 完整 5 字段 (本任务 useTweaks 只暴露 theme + accent)
- mock 数据 / types/sandbox.ts (S-02)
- 后端联调 (S-08)
