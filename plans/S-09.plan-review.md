## Iter 1

VERDICT: BLOCKED

1. `apps/debug-ui/src/App.tsx:85` / `apps/debug-ui/src/hooks/useWaTrace.ts:178` / `apps/debug-ui/src/hooks/useWaTrace.ts:187` - The plan's mount-once `useEffect` in `App` can be overwritten by the existing `useWaTrace` bootstrap. `useWaTrace` fetches `/api/traces` on mount with an empty dependency array and closes over the initial mock `activeTraceId`; when the backend list returns, `if (!list.find((t) => t.id === activeTraceId)) setActiveTraceIdState(list[0].id)` can replace the query-selected trace with the first trace in the list. This breaks the S-09 requirement that sandbox-lab opens debug-ui directly on `trace=<tsid>`. Fix by initializing/passing the URL trace into `useWaTrace`, or guard the list bootstrap with the requested trace id before falling back to `list[0].id`. Add coverage for loading `/?trace=<non-first backend trace>&round=R1` and asserting the selected trace remains that id after `/api/traces` resolves.

## Iter 2

VERDICT: BLOCKED

1. `plans/S-09.plan.md:89` / `plans/S-09.plan.md:98` / `apps/debug-ui/src/hooks/useWaTrace.ts:182` / `apps/debug-ui/src/api/client.ts:126` / `chisha/web_api.py:2736` - The Iter 1 race is fixed only for URL traces that are present in the first `/api/traces?limit=50` page. Moving URL parsing into `useWaTrace` and using it as the `useState` initializer means a non-first trace that appears in the list will no longer be overwritten by the bootstrap, so the original blocker is addressed for the planned e2e case. However, the revised guard still treats list membership as the source of truth: if `urlTrace` is absent from the first page, the `else if (!list.find((t) => t.id === activeTraceId)) setActiveTraceIdState(list[0].id)` branch will replace the URL-selected id with `list[0].id` before the detail endpoint can authoritatively 404 or load it. That violates the S-09 spec's missing-trace fallback requirement (`specs/S-09.md:28`-`specs/S-09.md:43`) and makes deep links depend on the trace browser page size even though `/api/trace/{session_id}` supports direct lookup (`chisha/web_api.py:2766`-`chisha/web_api.py:2786`). Fix by making a non-empty `trace=` pin `activeTraceId` through bootstrap regardless of list membership, then let `fetchTraceDetail(urlTrace)` decide success vs 404/mock fallback; only fall back to `list[0].id` when no URL trace was requested. Add coverage for `/?trace=<valid trace not returned by fetchTraces limit page>&round=R1` and `/?trace=<missing>&round=R1`, asserting the first fetches that id via `/api/trace/{id}` and the second does not silently select `list[0]`.

## Iter 3

VERDICT: BLOCKED

1. `specs/S-09.md:10` / `chisha/web_api.py:1321` / `chisha/web_api.py:1488` / `plans/S-09.plan.md:76` / `plans/S-09.plan.md:80` - Iter 2 fixes the debug-ui bootstrap race, but the plan still loses the S-09 `meal=null` current-trace path. `mealToTrace` is written only after `POST /eat` records a history meal, while the current uneaten recommendation trace lives in `last_recs.json.recommend_session_id`. The planned `openTrace(null)` fallback looks up `sb.mealToTrace[String(sb.currentMealIdx)]`, so selecting the current timeline cell, or any null fallback after bootstrap/refresh, will alert "该顿没有 trace" even though `last_recs` has a valid trace id. Fix by carrying the current recommendation trace id through the frontend state as well, either by adding `currentTraceId`/`currentRecommendSessionId` to `FullSnapshotResp` from `last_recs_data["recommend_session_id"]` and storing it in `useSandbox`, or by implementing the optional `/meal/{idx}/trace` helper. Then make `openTrace(null)` use that current id, while historical meals continue to use `mealToTrace[meal.idx]`. Add coverage for opening trace from the current timeline cell after a fresh `/sessions/{sid}` bootstrap where `last_recs.json` exists but the meal has not been eaten yet.

## Iter 4

[FALLBACK: Claude Opus adversarial review — Codex unavailable (quota)]

VERDICT: BLOCKED

1. `apps/sandbox-lab/src/hooks/useSandbox.ts:282-310` / `chisha/web_api.py:1691` / `chisha/web_api.py:1776` / `plans/S-09.plan.md:66-71` - The plan adds `currentTraceId` to `applySnapshot` from the snapshot piece, but never threads through the **new** `recommend_session_id` returned by `postRecs` / `postSwap` / `postRefine`. (a) After eat → polling done → `applySnapshot` finds `currentRecs.length === 0`, calls `postRecs(sid)` (line 284), and updates state with only `recs`. The fresh `recsResp.recommend_session_id` (BackendRecsResp.recommend_session_id, backend-types.ts:108) is discarded. `applySnapshot`'s `snapPiece.currentTraceId` is `null` here because `last_recs.json` was just deleted at web_api.py:1525 before snapshot was taken — so frontend ends with `currentTraceId=null` immediately after polling completes, while backend now has a brand new `last_recs.json` (created by the auto-postRecs call inside applySnapshot). Subsequent `openTrace(null)` will alert "该顿没有 trace" instead of opening the just-created trace. (b) Identical bug in `handleSwap` (useSandbox.ts:581-587): `postSwap` returns `recommend_session_id` (backend-types.ts:128) but the handler only updates `currentRecs`. (c) Same in `handleRefine` (useSandbox.ts:634-639). Fix: applySnapshot must set `currentTraceId: recsResp.recommend_session_id` when the auto-postRecs branch fires. handleSwap and handleRefine must include `currentTraceId: resp.recommend_session_id` in the state update. Add coverage that after eat→poll→done, `openTrace(null)` opens the NEW trace (not stale/null).

2. `chisha/web_api.py:1894-1901` / `plans/S-09.plan.md:42` - The plan reads `last_recs_data.get("recommend_session_id")` from the snapshot's loaded last_recs. But `last_recs_data` defaults to `{}` if the file is missing or corrupt (web_api.py:1900). Plan should explicitly handle the empty-string case: `last_recs.get("recommend_session_id", "")` returns `""` not `None` when key exists but value is empty (web_api.py:1480 pattern). Use `current_tsid = last_recs_data.get("recommend_session_id") or None` (the plan already says this — confirm in implementation). Non-blocking nit but worth flagging because `""` would propagate through as a falsy-but-not-null tsid and the frontend uses `?? null`, which doesn't catch empty string. Use `|| null` in adapter, not `?? null`. (Pure correctness, low impact, but easy fix.)

3. `apps/sandbox-lab/src/hooks/useSandbox.ts:88` / `plans/S-09.plan.md:67` - The mock offline path (initialSessionStates) creates `mealToTrace: {}, currentTraceId: null`. But offline-mode `handleEat` (useSandbox.ts:486-510) and offline `handleSkip` advance `currentMealIdx` and never update `mealToTrace`. Spec §1 explicitly says skip should NOT have a trace (correct), but for offline mock eat, the user might click "打开 trace" on a just-eaten cell and get "no trace" — that's actually correct (mock has no real trace). However the alert text "该顿没有 trace (mock 模式或 skip 顿或未推荐)" is fine. No fix needed. Marking as **non-blocking**, not counting toward "≥ 3 blockers".

After adversarial pass I find #1 is a real blocker, #2 is a small correctness nit (could be folded into impl), and #3 is non-blocking by design.

## Iter 5 (stuck override, low risk)

Main agent self-converge: codex quota exhausted (Iter 4 fallback to Opus inline). Iter 4 #1 + #2 both addressed in plan. No fresh-context reviewer available for Iter 5. Per run-task.md "iter == 4 → 强制 stuck override (risk 护栏 low/medium allowed)", proceeding to Phase 3 implementation. Codex/Opus blockers were genuine and resolved — not a "过度谨慎" override, but the iter cap forces convergence here. Phase 5 will tag commit `(with codex disagreement, see plans/S-09.diff-review.md)` per protocol if Phase 4 surfaces residual disagreement; otherwise normal `done`.

VERDICT: APPROVED (stuck override)
