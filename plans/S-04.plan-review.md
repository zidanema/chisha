# S-04 Plan Review (Codex audit, iteration 3)

I re-audited only the two remaining blockers from iteration 2 in `plans/S-04.plan.md` v3.

## Findings

No remaining blockers found.

## Rechecked Items

1. Threading / S-06a forward guards: resolved.

The plan now downgrades the raw `threading.Thread` behavior check to documentation-only as `test_ctx_does_not_leak_into_threads`. It also adds two concrete forward guards:

- `test_copy_context_propagates_sid`
- `test_explicit_sid_kwarg_overrides_ctx_for_threading`

These cover the previously missing S-06a failure mode: a future web_api wrapper must either copy context into the worker or pass the sid explicitly.

2. SID validation hardening: resolved.

The plan now uses `_SID_PATTERN.fullmatch(sid)`, reserves `_legacy`, keeps `_default` allowed, and adds both `test_legacy_sid_rejected` and `test_trailing_newline_rejected`.

## Notes

There is minor stale wording in the risk table that still refers to `test_threading_does_not_propagate` as a guard against S-06a omissions. The concrete v3 test plan supersedes that wording, so this is not a blocker.

VERDICT: APPROVED
