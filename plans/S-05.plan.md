# S-05 实施 Plan (narrowed scope · v5)

> **[V1.0 后历史归档]** 本 plan 已 done; 其中提到的 `scripts/migrate_sandbox_layout.py`
> 已于 V1.0 收尾时随 `scripts/deprecated/` 整目录删除 (migration 一次性兜底已完成).
> 文件保留作为 S-05 执行历史叙事, 不再当作 Phase 1 可用入口。

> **本轮 scope 故意收窄** — v4 (929 行) 因 rollback/inspect/lock/migration 全混在一起, 3 轮 Codex 审计均 BLOCKED. v4 已归档 `plans/S-05.plan-v4-bigscope.md.bak`.
> 本轮只做 4 件事, 5 件事显式 defer to S-06a.

> **v5 → v5.1 修订** (Codex iter 1 反馈 4 项 BLOCKED, in-scope fix):
> - #1 `--relocate-legacy` 把 `sessions/` 拷进自己子孙 → 从 `_LEGACY_ARTIFACTS` 删除 `"sessions"`. D-039 recommend sessions 留原地 (本来就是 default 桶).
> - #2 `--relocate-legacy` 在默认 migrate 后 silent no-op → meta 加 `relocated_legacy: bool`, 幂等判断改成"schema_v2 + relocate flag 匹配请求"才 already_migrated; flag 不匹配走单独 relocate-only 分支.
> - #3 `.bak` collision 不安全 → 改用时间戳后缀 `<name>.bak.<utc-ts>`, 不复用名字.
> - #4 baseline 比较命令默认 dir 错 → 显式 `--before-dir/--after-dir` flag.

> **v5.1 → v5.2 修订** (Codex iter 2 反馈 1 项 BLOCKED):
> - #1 relocate 幂等不完整: 跑 relocate 时所有 artifact 已不在 src (全已在 `_legacy/` 或 absent) → 旧逻辑 `bool(relocated)` 写 `relocated_legacy: false` → 下次再跑还是 `relocate_only`. 修法: `relocated_legacy` 改判 "已无 src side 待挪 + 至少一项在 `_legacy/` 或本来全 absent" → 满足即 True. 新加 test 守门: 跑两次 relocate 第二次 `already_migrated`.

---

## §1. 本轮 scope vs deferred to S-06a

### IN-scope (本轮必做)

1. **Session_id 分桶的静态路径布局** — `data_root` 和 `trace_store` 写入 `<root>/logs/sandbox/sessions/<sid>/...` 子树 (非 default sid). **Default sid 走 flat 老路径** `<root>/logs/sandbox/<rel>` (与 S-04 + D-077 完全一致, 不动). 不引入 `sessions/_default/` 子目录, 不动既有路径断言.
2. **Migration 脚本** — `scripts/migrate_sandbox_layout.py` (CLI) + `chisha/sandbox_migration.py` (核心函数). 检测 `logs/sandbox/` 是否存在历史数据, 写 `_meta.json` schema marker. 必须: idempotent + `--dry-run` mode + 文件级 rollback notes (`.bak` 保留).
3. **1 guard test** — `tests/test_sandbox_layout_default_compat.py` 确认 default sid 读写路径与 S-04 完全一致 (flat) + migration 不破坏 default 桶读路径.
4. **baseline_l2_snapshot 0 diff** — 改前 / 改后双跑.

### DEFERRED to S-06a (本轮显式不做, Codex / Opus 审计请勿基于此 BLOCKED)

- **A.** `/sandbox/inspect` 在 "layout 已建但 enabled=False" 时的 listing 改动
- **B.** rollback API 的持锁修订 / 持锁语义
- **C.** 非 default sid 的 runtime 路由门控 (web_api wrap with `set_sandbox_session(sid)`)
- **D.** 多 session 并发写入的 lock 文件设计 (跨进程 fcntl)
- **E.** `_meta.lock` 重定位 / sandbox layout reset 时序 (init/reset 时机)

### 决策摘要

| 决策点 | 选 | 理由 |
|---|---|---|
| Default sid 路径 | **flat `logs/sandbox/<rel>`** (不进 `sessions/_default/`) | S-04 已是此布局; 零路径断言改动; backward compat 完全保留 |
| Migration target | **原地不动 + 写 `_meta.json` 标记** (默认); `--relocate-legacy` opt-in 才挪到 `sessions/_legacy/` (默认 False, 本轮不强测此分支) | 默认场景零数据搬运 → 风险最低; opt-in 路径仍幂等 |
| Migration trigger | **显式 CLI** (`scripts/migrate_sandbox_layout.py`). 不在 `init()` 自动调 (那是 §B/E 范围). | scope 收窄 — auto-trigger 涉及 init 时序 (deferred E) |
| guard test 数量 | **1 test 文件多个 case** | scope 收窄要求 |
| `_meta.json` 字段 | `{"schema_version": 2, "created_at": iso, "migrated_at": iso \| null, "default_layout": "flat"}` | 最小信息 + 显式记录 default layout 选择 (S-06a 读此判断布局) |
| `_legacy` 桶预留 | **保留 sid 黑名单** (S-04 已建) + migration `--relocate-legacy` 写入 `sessions/_legacy/` | 不在路径决策链上, 只是预留位 |

---

## §2. Affected files

### 改
- `chisha/sandbox.py` — 加 `_meta.json` 路径常量 + `has_sandbox_meta(root)` helper (不动 `init`/`reset` 逻辑). 长度增量 ~30 行.

### 新建
- `chisha/sandbox_migration.py` — 核心 migration 函数 (~120 行):
  - `migrate_to_v2(root, *, dry_run=False, relocate_legacy=False) -> dict`
  - `meta_path(root)`, `read_meta(root)`, `_atomic_write_meta(root, data)`
  - 幂等检测: `_meta.json` 已存在 + schema_version=2 → no-op (返 already_migrated)
- `scripts/migrate_sandbox_layout.py` — CLI wrapper (~50 行):
  - `--root <path>` 默认 project root
  - `--dry-run` 仅 log, 不写盘
  - `--relocate-legacy` 把 `logs/sandbox/{state.json, meal_log.jsonl, recommend_log.jsonl, feedback/, recommend_trace/, sessions/, long_term_prefs.json, profile.yaml, feedback_history.jsonl}` 挪到 `sessions/_legacy/<rel>` (保留 `.bak` 备份)
- `tests/test_sandbox_layout_default_compat.py` — 1 个 guard test 文件 (~150 行, 详 §4)

### 不动 (本轮)
- `chisha/data_root.py` — S-04 已含 sid 派生, 本轮不改逻辑 (default → flat, non-default → sessions/{sid}/)
- `chisha/trace_store.py` — 走 `data_root.recommend_trace_dir(root, session_id=...)`, S-04 已透 sid
- `chisha/sandbox_context.py` — S-04 已建, `_legacy` 已 reserved
- `chisha/web_api.py` — runtime 路由 deferred (§C)
- `chisha/api.py / recall.py / score.py / rerank.py / refine.py / l1_extractor.py` — 推荐链路不动
- 既有所有 sandbox 测试 (`test_sandbox_clock.py`, `test_sandbox_context.py`, `test_data_root.py`, etc.) — 路径断言**零修改** (因 default=flat)

---

## §3. Implementation

### 3.1 `chisha/sandbox_migration.py`

```python
"""S-05: sandbox layout v1 → v2 migration (narrowed scope).

v2 layout 关键变化:
- `logs/sandbox/_meta.json` 新增 (schema_version + migration history)
- Default sid 仍 flat (`logs/sandbox/<rel>`); 非 default sid 走
  `logs/sandbox/sessions/<sid>/<rel>` (S-04 data_root 已实现)
- 老 `logs/sandbox/<rel>` 数据保持原地 = default 桶 (零搬运)
- `--relocate-legacy` opt-in: 把老数据挪到 `sessions/_legacy/` 子树备份

幂等:
- `_meta.json.schema_version == 2` → no-op
- relocate 二次跑: 已挪过的文件不再挪 (检测 target 存在 + 源不存在)
"""

import datetime as dt
import json
import shutil
from pathlib import Path
from typing import Optional, TypedDict

SCHEMA_VERSION = 2
_META_REL = "logs/sandbox/_meta.json"
_LEGACY_DIRNAME = "_legacy"

# 老 layout 顶层文件/目录清单 (relocate 时挪它们)
# v5.1 Codex fix #1: 删除 "sessions" — 它是 _legacy 的 parent (sessions/_legacy/),
# 拷自己子孙会 infinite recurse. D-039 recommend sessions 本来就是 default 桶,
# 不需挪. 同理不挪 _meta.json 自身.
_LEGACY_ARTIFACTS: tuple[str, ...] = (
    "state.json",
    "meal_log.jsonl",
    "recommend_log.jsonl",
    "feedback_history.jsonl",
    "long_term_prefs.json",
    "profile.yaml",
    "feedback",        # dir
    "recommend_trace", # dir
    # NOTE: "sessions" 不在清单 — 是 _legacy parent, 也是 D-039 recommend
    #       sessions dir, 留原地 = default 桶语义.
)

class MigrationResult(TypedDict, total=False):
    status: str          # "already_migrated" | "migrated" | "relocate_only" |
                         # "no_sandbox" | "dry_run"
    schema_version: int
    relocated: list[str]
    skipped: list[str]
    meta_path: str

def meta_path(root: Path) -> Path:
    return root / _META_REL

def read_meta(root: Path) -> Optional[dict]:
    p = meta_path(root)
    if not p.exists():
        return None
    try:
        data = json.loads(p.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return None
    return data if isinstance(data, dict) else None

def _atomic_write_meta(root: Path, data: dict) -> None:
    p = meta_path(root)
    p.parent.mkdir(parents=True, exist_ok=True)
    tmp = p.with_suffix(".json.tmp")
    tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(p)

def _ts_suffix() -> str:
    """UTC timestamp for collision-safe .bak names (Codex fix #3)."""
    return dt.datetime.now(dt.timezone.utc).strftime("%Y%m%dT%H%M%SZ")

def migrate_to_v2(
    root: Path,
    *,
    dry_run: bool = False,
    relocate_legacy: bool = False,
) -> MigrationResult:
    """One-shot migration. Idempotent. dry-run 仅返回 plan, 不写盘.

    场景 (v5.1 Codex fix #2: 显式分离 meta-write vs relocate):
    - sandbox 目录不存在: no_sandbox
    - 已 migrated (schema_v2) + 当前请求 relocate flag 与 meta 记录一致: already_migrated
    - 已 migrated (schema_v2) + 请求 relocate=True 但 meta.relocated_legacy=False:
      relocate_only (跑 relocate, 更新 meta.relocated_legacy=True)
    - 未 migrated: write meta (+ 可选 relocate)
    """
    sandbox_dir = root / "logs" / "sandbox"
    if not sandbox_dir.exists():
        return {"status": "no_sandbox", "schema_version": SCHEMA_VERSION,
                "relocated": [], "skipped": [], "meta_path": str(meta_path(root))}

    existing = read_meta(root)
    already_v2 = bool(existing and existing.get("schema_version") == SCHEMA_VERSION)
    already_relocated = bool(existing and existing.get("relocated_legacy"))

    # Codex fix #2: relocate 在 already_v2 但未 relocated 时仍可跑
    if already_v2 and (not relocate_legacy or already_relocated):
        return {"status": "already_migrated",
                "schema_version": SCHEMA_VERSION,
                "relocated": [], "skipped": [],
                "meta_path": str(meta_path(root))}

    now_iso = dt.datetime.now(dt.timezone.utc).isoformat()
    relocated: list[str] = []
    skipped: list[str] = []

    # v5.2 Codex fix #1: relocate_satisfied 派生 — 跑完后, 每个 artifact 必须
    # (a) 刚被搬走 (in `relocated`), 或 (b) 已在 _legacy (dst.exists()), 或
    # (c) src 完全不存在 + dst 不存在 (从未有过). 三种都满足 → relocate_satisfied.
    relocate_satisfied = False
    if relocate_legacy:
        legacy_dir = sandbox_dir / "sessions" / _LEGACY_DIRNAME
        ts = _ts_suffix()  # 单次跑共用 ts, 多文件 .bak 关联可读
        all_ok = True   # 假设全部 artifact 都已 settled, 中途若发现没处理掉的翻 False
        for name in _LEGACY_ARTIFACTS:
            src = sandbox_dir / name
            dst = legacy_dir / name
            if not src.exists():
                # src 不在: settled if dst 在 OR 全 absent (从未有过)
                skipped.append(name)
                continue
            # src 在
            if dst.exists():
                # dst 也在: skip (幂等保护), 但 src 还在意味着上次跑被打断; 不重搬,
                # 让 caller 手工解决 (skipped + all_ok=False 让 relocate_satisfied=False).
                skipped.append(name)
                all_ok = False
                continue
            if dry_run:
                relocated.append(name)
                continue
            dst.parent.mkdir(parents=True, exist_ok=True)
            # Codex fix #3: collision-safe bak 名 (ts 后缀, 不复用)
            bak_name = f"{src.name}.bak.{ts}"
            bak = src.parent / bak_name
            if bak.exists():
                # 极不可能 (同 ts 同名), fail-loud
                raise RuntimeError(
                    f"backup name collision: {bak} already exists; "
                    f"refusing to overwrite. Inspect manually."
                )
            # copy → bak rename 两步
            if src.is_dir():
                shutil.copytree(src, dst)
            else:
                shutil.copy2(src, dst)
            src.rename(bak)
            relocated.append(name)
        # 跑完: 只要无 "src 在 + dst 在" 的中断态, 即视为 satisfied
        relocate_satisfied = all_ok

    if dry_run:
        return {"status": "dry_run", "schema_version": SCHEMA_VERSION,
                "relocated": relocated, "skipped": skipped,
                "meta_path": str(meta_path(root))}

    # v5.1: relocate-only 分支保留原 meta 其他字段, 翻 relocated_legacy
    if already_v2 and relocate_legacy:
        new_meta = dict(existing)  # type: ignore[arg-type]
        # v5.2 Codex fix #1: 用 relocate_satisfied 而非 bool(relocated)
        new_meta["relocated_legacy"] = relocate_satisfied or already_relocated
        new_meta["relocated_at"] = now_iso
        _atomic_write_meta(root, new_meta)
        return {"status": "relocate_only", "schema_version": SCHEMA_VERSION,
                "relocated": relocated, "skipped": skipped,
                "meta_path": str(meta_path(root))}

    new_meta = {
        "schema_version": SCHEMA_VERSION,
        "created_at": (existing or {}).get("created_at") or now_iso,
        "migrated_at": now_iso,
        "default_layout": "flat",  # 决策: default sid 走 flat 路径
        # v5.2 Codex fix #1: 用 relocate_satisfied
        "relocated_legacy": bool(relocate_legacy and relocate_satisfied),
    }
    if relocate_legacy and (relocated or relocate_satisfied):
        new_meta["relocated_at"] = now_iso
    _atomic_write_meta(root, new_meta)
    return {"status": "migrated", "schema_version": SCHEMA_VERSION,
            "relocated": relocated, "skipped": skipped,
            "meta_path": str(meta_path(root))}
```

### 3.2 `scripts/migrate_sandbox_layout.py`

```python
"""CLI: 把现有 logs/sandbox/ 老 layout migrate 到 v2 (写 _meta.json).

幂等可重跑. 默认不挪文件 (default 桶 = flat = 老路径).

用法:
    uv run python -m scripts.migrate_sandbox_layout          # 写 _meta.json
    uv run python -m scripts.migrate_sandbox_layout --dry-run
    uv run python -m scripts.migrate_sandbox_layout --relocate-legacy  # 挪到 sessions/_legacy/
"""

import argparse
import json
import sys
from pathlib import Path

from chisha.sandbox_migration import migrate_to_v2

def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description="Migrate sandbox layout v1 → v2")
    parser.add_argument("--root", type=Path, default=None,
                        help="project root (default: auto-detect)")
    parser.add_argument("--dry-run", action="store_true",
                        help="log plan only, no disk writes")
    parser.add_argument("--relocate-legacy", action="store_true",
                        help="move logs/sandbox/<flat> → sessions/_legacy/<rel> (keeps .bak)")
    args = parser.parse_args(argv)

    root = args.root or Path(__file__).resolve().parent.parent
    result = migrate_to_v2(root, dry_run=args.dry_run, relocate_legacy=args.relocate_legacy)
    print(json.dumps(result, ensure_ascii=False, indent=2))
    return 0

if __name__ == "__main__":
    sys.exit(main())
```

### 3.3 `chisha/sandbox.py` 加 `has_sandbox_meta` helper

```python
# 加在文件底部
from typing import Optional as _Optional

def has_sandbox_meta(root: _Optional[Path] = None) -> bool:
    """检测 _meta.json 是否存在 (S-05 migration 已跑过).

    本任务内仅给 test + future S-06a 用, 不进 path 决策链.
    is_enabled() 仍走 state.json (D-077 语义).
    """
    return ((root or _project_root()) / _META_REL).exists()

_META_REL = "logs/sandbox/_meta.json"
```

---

## §4. Test strategy

### 4.1 唯一新增 test: `tests/test_sandbox_layout_default_compat.py`

```python
"""S-05 guard: default sid 读写路径 == S-04 + migration 不破坏 default 桶.

scope 收窄:
- 只 cover default sid (flat layout) + migration 幂等
- 非 default sid 路由 / runtime ctxvar = deferred to S-06a
"""
import json
from pathlib import Path
import pytest
from chisha import data_root, sandbox
from chisha.sandbox_context import _DEFAULT_SID
from chisha.sandbox_migration import migrate_to_v2, read_meta, SCHEMA_VERSION

@pytest.fixture
def tmp_root(tmp_path: Path) -> Path:
    return tmp_path

# ── default sid 路径 (回归 S-04 行为)
def test_default_sid_path_unchanged_after_migrate(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    base_before = data_root.meal_log_path(tmp_root)
    assert base_before == tmp_root / "logs" / "sandbox" / "meal_log.jsonl"
    migrate_to_v2(tmp_root)
    base_after = data_root.meal_log_path(tmp_root)
    assert base_after == base_before  # flat path 不变
    assert data_root.meal_log_path(tmp_root, session_id=_DEFAULT_SID) == base_after

def test_default_bucket_data_readable_after_migrate(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    (tmp_root / "logs" / "sandbox" / "meal_log.jsonl").write_text(
        '{"meal":"lunch"}\n', encoding="utf-8"
    )
    migrate_to_v2(tmp_root)
    # 数据原地, default sid 仍读得到
    content = (tmp_root / "logs" / "sandbox" / "meal_log.jsonl").read_text()
    assert '"meal":"lunch"' in content

# ── _meta.json 行为
def test_meta_written_with_schema_version_2(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    result = migrate_to_v2(tmp_root)
    assert result["status"] == "migrated"
    meta = read_meta(tmp_root)
    assert meta["schema_version"] == SCHEMA_VERSION
    assert meta["default_layout"] == "flat"
    assert meta["migrated_at"]

def test_idempotent_rerun_is_noop(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    r1 = migrate_to_v2(tmp_root)
    assert r1["status"] == "migrated"
    r2 = migrate_to_v2(tmp_root)
    assert r2["status"] == "already_migrated"
    # 二次跑后 meta 不变
    meta = read_meta(tmp_root)
    assert meta["schema_version"] == SCHEMA_VERSION

def test_no_sandbox_dir_returns_no_sandbox(tmp_root):
    # sandbox 从未初始化
    result = migrate_to_v2(tmp_root)
    assert result["status"] == "no_sandbox"
    assert not (tmp_root / "logs" / "sandbox" / "_meta.json").exists()

# ── dry-run
def test_dry_run_no_disk_writes(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    result = migrate_to_v2(tmp_root, dry_run=True)
    assert result["status"] == "dry_run"
    assert read_meta(tmp_root) is None

# ── relocate-legacy opt-in
def test_relocate_legacy_moves_files_with_bak(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    result = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert "meal_log.jsonl" in result["relocated"]
    # legacy bucket 有
    assert (sb / "sessions" / "_legacy" / "meal_log.jsonl").exists()
    # 源 ts-suffix .bak 保留 (v5.1 Codex fix #3)
    baks = list(sb.glob("meal_log.jsonl.bak.*"))
    assert len(baks) == 1

def test_relocate_idempotent_second_run_skips(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    migrate_to_v2(tmp_root, relocate_legacy=True)
    # 二次跑 (强制重置 meta 模拟 mid-failure)
    (sb / "_meta.json").unlink()
    r2 = migrate_to_v2(tmp_root, relocate_legacy=True)
    # target 已存在 → skip (源已被搬走, 走 skipped 分支)
    # 注: 源也不在了 (变成 .bak.<ts>), 所以也会落 skipped 是因 not src.exists()
    assert "meal_log.jsonl" in r2["skipped"]

# ── has_sandbox_meta helper
def test_has_sandbox_meta_helper(tmp_root):
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    assert not sandbox.has_sandbox_meta(tmp_root)
    migrate_to_v2(tmp_root)
    assert sandbox.has_sandbox_meta(tmp_root)

# ── v5.1 Codex fix tests
def test_sessions_dir_not_relocated(tmp_root):
    """Codex fix #1: sessions/ 不挪 (是 _legacy parent + D-039 recommend dir)."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "sessions").mkdir(parents=True, exist_ok=True)
    (sb / "sessions" / "abc_xyz.json").write_text("{}", encoding="utf-8")
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    result = migrate_to_v2(tmp_root, relocate_legacy=True)
    # sessions 不在 relocated 列表
    assert "sessions" not in result["relocated"]
    # sessions/abc_xyz.json 原地保留 (不被搬走, 不被备份)
    assert (sb / "sessions" / "abc_xyz.json").exists()
    # 也不在 _legacy 下
    assert not (sb / "sessions" / "_legacy" / "sessions").exists()
    # meal_log 走正常 relocate
    assert "meal_log.jsonl" in result["relocated"]

def test_relocate_after_default_migrate_runs(tmp_root):
    """Codex fix #2: 默认 migrate 后, --relocate-legacy 仍可跑 (relocate_only 分支)."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    # 1. 默认 migrate (不 relocate)
    r1 = migrate_to_v2(tmp_root)
    assert r1["status"] == "migrated"
    assert read_meta(tmp_root)["relocated_legacy"] is False
    # 2. 后跑 relocate 应进 relocate_only 分支
    r2 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r2["status"] == "relocate_only"
    assert "meal_log.jsonl" in r2["relocated"]
    # meta 翻 relocated_legacy=True
    meta = read_meta(tmp_root)
    assert meta["relocated_legacy"] is True
    assert meta["schema_version"] == SCHEMA_VERSION
    # 3. 再跑应 already_migrated
    r3 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r3["status"] == "already_migrated"

def test_relocate_twice_second_is_already_migrated(tmp_root):
    """v5.2 Codex fix #1: 第二次 relocate-only 必须 already_migrated, 不再 relocate_only."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    r1 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r1["status"] == "migrated"
    assert read_meta(tmp_root)["relocated_legacy"] is True
    # 第二次 — src 已不在 (变成 .bak.<ts>), dst 已在 → all_ok=True → satisfied
    r2 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r2["status"] == "already_migrated", (
        f"second relocate should be no-op, got status={r2['status']}"
    )

def test_relocate_with_no_artifacts_marks_satisfied(tmp_root):
    """v5.2 Codex fix #1: sandbox 目录在但无任何 legacy artifact → satisfied=True."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    # sandbox 目录在 (sandbox.init 建了 state.json), 删 state.json 模拟空目录
    (sb / "state.json").unlink()
    r1 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r1["status"] == "migrated"
    # 没东西可挪 → 仍 satisfied (relocated_legacy=True)
    assert read_meta(tmp_root)["relocated_legacy"] is True
    # 二次跑 → already_migrated
    r2 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r2["status"] == "already_migrated"

def test_relocate_interrupted_mid_run_not_satisfied(tmp_root):
    """v5.2 Codex fix #1 corollary: src 和 dst 同时在 (上次跑被打断) → satisfied=False, 等手工."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("new\n", encoding="utf-8")
    # 模拟上次跑只 copy 没 rename (src+dst 都在)
    legacy = sb / "sessions" / "_legacy"
    legacy.mkdir(parents=True, exist_ok=True)
    (legacy / "meal_log.jsonl").write_text("old\n", encoding="utf-8")
    r1 = migrate_to_v2(tmp_root, relocate_legacy=True)
    # src 和 dst 都在 → skip + all_ok=False → relocated_legacy=False
    assert "meal_log.jsonl" in r1["skipped"]
    assert read_meta(tmp_root)["relocated_legacy"] is False
    # 二次仍 relocate_only (等手工解决)
    r2 = migrate_to_v2(tmp_root, relocate_legacy=True)
    assert r2["status"] == "relocate_only"

def test_bak_name_is_timestamped_and_collision_safe(tmp_root):
    """Codex fix #3: .bak 用 ts 后缀, 不复用. 预存 .bak 不被覆盖."""
    sandbox.init(start_date="2026-05-20", root=tmp_root)
    sb = tmp_root / "logs" / "sandbox"
    (sb / "meal_log.jsonl").write_text("data\n", encoding="utf-8")
    # 预存一个老 .bak (模拟手工 rollback 残留)
    (sb / "meal_log.jsonl.bak").write_text("OLD_BACKUP\n", encoding="utf-8")
    migrate_to_v2(tmp_root, relocate_legacy=True)
    # 老 .bak 完整保留
    assert (sb / "meal_log.jsonl.bak").read_text() == "OLD_BACKUP\n"
    # 新 bak 用 ts 后缀
    baks = list(sb.glob("meal_log.jsonl.bak.*"))
    assert len(baks) == 1, f"expected one ts bak, got {[b.name for b in baks]}"
    # ts 格式校验 (YYYYMMDDTHHMMSSZ)
    suffix = baks[0].name.split(".bak.", 1)[1]
    assert len(suffix) == 16 and suffix.endswith("Z")
```

### 4.2 baseline_l2_snapshot 0 diff (Codex fix #4: 显式 dir flags)

`scripts/compare_traces.py` 默认 `--before-dir tmp/baseline_traces` (不是 `_before`),
所以必须显式传 flags, 否则 compare 拿到错误 before-dir 误报 0 diff (假阳性).

```bash
# 改前
uv run python -m scripts.baseline_l2_snapshot --out-dir tmp/baseline_traces_before
# 改后
uv run python -m scripts.baseline_l2_snapshot --out-dir tmp/baseline_traces_after
# 显式比较
uv run python -m scripts.compare_traces \
    --before-dir tmp/baseline_traces_before \
    --after-dir  tmp/baseline_traces_after
# 必须 0 diff
```

### 4.3 既有测试

`uv run pytest tests/ -q` 全绿. **零既有测试断言修改** (因 default 走 flat = S-04 现状).

---

## §5. Regression risks + rollback notes

### Risks

| Risk | Probability | Mitigation |
|---|---|---|
| Default sid 路径漂移 → 既有 sandbox 测试断言挂 | 低 | default 走 flat 不动; `test_default_sid_path_unchanged_after_migrate` 守门 |
| Migration relocate 时 partial-write crash | 中 | copy→rename(`.bak`) 两步, src `.bak` 保留 = 手工 rollback (mv `*.bak` 回原名) |
| `_meta.json` 损坏 → S-06a 读不到 schema | 低 | `read_meta` 返 None 而非抛; S-06a 可重跑 migration 重写 |
| baseline_l2_snapshot 漂移 | 极低 | 本任务零改推荐链路文件; 双跑守门 |
| 用户跑 `--relocate-legacy` 后 default 桶为空导致 sandbox 读不到 old 数据 | 中 | 显式 opt-in (默认 False); README/docstring 警告; `.bak` 保留可手工回滚 |
| ContextVar `_legacy` 黑名单冲突 (relocate 后 listing 反查) | 中 | S-04 已 reserve `_legacy`; sid 校验拒. listing 改动 = deferred §A |

### Rollback (manual)

- `_meta.json` 写后回滚: `rm logs/sandbox/_meta.json`
- `--relocate-legacy` 回滚: 对每个 `<name>.bak`: `mv <name>.bak <name>` + `rm -rf sessions/_legacy/<name>`
- 任何情况下: `git revert` 代码改动 + manual 文件 rollback

---

## §6. Migration dry-run flow

### Scenario A: 全新 install (no sandbox dir)

```bash
$ uv run python -m scripts.migrate_sandbox_layout --dry-run
{"status": "no_sandbox", ...}
```

### Scenario B: 有老数据, 默认 (不 relocate)

```bash
$ ls logs/sandbox/
state.json  meal_log.jsonl  recommend_log.jsonl  feedback/  recommend_trace/  sessions/

$ uv run python -m scripts.migrate_sandbox_layout --dry-run
{"status": "dry_run", "relocated": [], "skipped": [], ...}

$ uv run python -m scripts.migrate_sandbox_layout
{"status": "migrated", "relocated": [], "skipped": [], "meta_path": ".../logs/sandbox/_meta.json"}

$ ls logs/sandbox/
_meta.json  state.json  meal_log.jsonl  ...  # 文件原地, 多了 _meta.json

$ uv run python -m scripts.migrate_sandbox_layout
{"status": "already_migrated", ...}  # 幂等
```

### Scenario C: 有老数据, opt-in relocate

```bash
$ uv run python -m scripts.migrate_sandbox_layout --relocate-legacy --dry-run
{"status": "dry_run", "relocated": ["state.json", "meal_log.jsonl", ...], ...}

$ uv run python -m scripts.migrate_sandbox_layout --relocate-legacy
{"status": "migrated", "relocated": [...], ...}

$ ls logs/sandbox/
_meta.json  state.json.bak.20260519T080000Z  meal_log.jsonl.bak.20260519T080000Z  ...  sessions/

$ ls logs/sandbox/sessions/_legacy/
state.json  meal_log.jsonl  ...

# 二次跑 (Codex fix #2: 已 relocate 后再跑 = already_migrated)
$ uv run python -m scripts.migrate_sandbox_layout --relocate-legacy
{"status": "already_migrated", ...}  # 幂等
```

### Scenario D: relocate 后想回滚 (手工)

```bash
$ cd logs/sandbox/
# bak 文件名格式: <orig>.bak.<UTC ts>; 找到对应 ts 还原
$ for f in *.bak.*; do
    orig="${f%.bak.*}"
    mv "$f" "$orig"
  done
$ rm -rf sessions/_legacy
$ rm _meta.json
```

### Scenario E: 默认 migrate 后想加跑 relocate (新 Codex fix #2 场景)

```bash
$ uv run python -m scripts.migrate_sandbox_layout
{"status": "migrated", "relocated_legacy": false, ...}

$ uv run python -m scripts.migrate_sandbox_layout --relocate-legacy
{"status": "relocate_only", "relocated": [...], ...}
# meta 翻 relocated_legacy=true, schema_version 不变

$ uv run python -m scripts.migrate_sandbox_layout --relocate-legacy
{"status": "already_migrated", ...}  # 现在真的 noop
```

---

## §7. Done When

- `chisha/sandbox_migration.py` + `scripts/migrate_sandbox_layout.py` 落地
- `chisha/sandbox.py` 加 `has_sandbox_meta` helper
- `tests/test_sandbox_layout_default_compat.py` 全绿 (≥8 test cases)
- `uv run pytest tests/ -q` 全绿
- baseline_l2_snapshot 改前/改后 0 diff (`uv run python -m scripts.compare_traces`)
- migration 自测: dry-run / 默认 / `--relocate-legacy` 三 path 都跑过
- 既有 sandbox 测试路径断言零修改

---

## §8. 行数预算

| 文件 | 增量 |
|---|---|
| `chisha/sandbox_migration.py` (新) | ~120 行 |
| `scripts/migrate_sandbox_layout.py` (新) | ~50 行 |
| `chisha/sandbox.py` (改) | ~10 行 |
| `tests/test_sandbox_layout_default_compat.py` (新) | ~150 行 |
| **合计** | **~330 行 new code + test** |

(对比 v4 plan 929 行设计 + 多文件大改, 本轮约 1/3 体量, scope 明显收窄.)
