# T-00 Diff Review (Codex)

**VERDICT: BLOCKED**

## Blocker

`chisha/api.py:_build_trace` (~472-499) 和 `chisha/debug_recommend.py:_build_l1_trace` (~283-326) 不会在 `write_trace()` 前主动 init `l1["hard_filter_events"] = []`。`TRACE_SCHEMA_VERSION` 已 bump 到 2 (`chisha/trace_store.py:28`),但 `write_trace()` 只强制 `__version` 不规范化 L1 形状。后果: `recommend_meal()` 正常产出的全新 v2 trace 可能完全没有 `hard_filter_events`,违反 T-00 契约。

测试覆盖只有 (a) v1 on-read migration 和 (b) 手工填好 v2 trace,没有断言"正常 write 出的 v2 trace 含 `hard_filter_events: []`"。

## Notes (non-blocking)

`append_hard_filter_event` 用 `isinstance(value, int)` 做 count 校验,会静默通过 `True/False` (Python `bool` 是 `int` 子类)。当下影响有限,值得加固。

## Fix required

`write_trace()` 给 v2 trace 兜底注入 `l1.hard_filter_events = []` (集中防护,所有写路径都覆盖) + 加测试断言 fresh write 含字段。同时显式 reject bool。
