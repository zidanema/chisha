# S-02 · 实施计划

> Task: 把 `chidiansha-sandbox/project/sandbox-lab/` HTML 原型 1:1 拆成 TS 组件,全部走 mock 数据渲染。**不实现任何交互逻辑** (S-03)。
> Spec: `specs/S-02.md` · Design brief: `docs/design_briefs/2026-05-19-sandbox-lab.md`

## 1. Affected files (apps/sandbox-lab/src/ 内 新建/扩,绝不动其他 app)

### 新建
```
src/
├── types/sandbox.ts                          types: Session, Meal, Rec, Decision, ActiveRule, Taste*, Keyword*, Fatigue*
├── mocks/sbxMocks.ts                         data.js 改 TS,export const SESSIONS / HISTORY / CURRENT_RECS / LAST_DECISION / TASTE / KEYWORDS / ACTIVE_RULES / BLACKLIST / RECENT / FATIGUE / CURRENT_IDX / TOTAL_MEALS
├── components/
│   ├── TopBar.tsx                            brand + Session 下拉(dropdown 静态展开/收起) + meta chips + 起点 tag + 时钟 pill + 摘要按钮 + tweaks 按钮
│   ├── Banners.tsx                           banner stack + level (warn/danger/review/info) + 收起按钮
│   ├── Timeline.tsx                          14 格 + variant=bars/calendar + day grouping. 静态: 选 D3午 (currentIdx=4)
│   │   └── (内含 Cell 子组件 + OpBar 子组件)
│   ├── DecisionArea.tsx                      5 RecCard + refine 行 + 6 preset chips + skip footer
│   ├── ReviewCard.tsx                        只读卡 (eat 模式 + skip 极简模式)
│   ├── RecCard.tsx                           推荐卡 (rank/tags/price/title/why/meta/双按钮) + showDebug 叠加层 (L3 chip / L1/L2/L3 in why / conflict pill / 探索 tag)
│   ├── TweaksPanel.tsx                       简化版浮层 (5 字段, 不引入原型的 host protocol)
│   ├── panels/
│   │   ├── DPanel.tsx                        紧凑/标准/详细 3 档 + 三段 (动作摘要 / 状态 diff / 下顿影响)
│   │   ├── APanel.tsx                        长期画像 taste 条 + L1 关键词 (open/closed compact 模式)
│   │   ├── BPanel.tsx                        活跃规则 — refine 文案改"当前 round 已应用 X" + blacklist (open/closed)
│   │   └── CPanel.tsx                        近期 4 顿 + fatigue 计数 (open/closed)
│   └── modals/
│       ├── SummaryDrawer.tsx                 抽屉 + filter chips + history list + 跳 trace
│       ├── ConfirmModal.tsx                  rollback/branch 两态 + 名字输入 (branch)
│       └── RefineModal.tsx                   textarea + 5 preset chips (handoff 保留, S-03 才接逻辑)
├── App.tsx                                   重写: 拼装 + state 静态喂 mock + Tweaks 上挂全套
└── hooks/useTweaks.ts                        从 S-01 极简版扩成 5 字段: timelineVariant / rightDensity / showDebugLayer / accent / theme
```

### 改写
- `src/styles.css` — 1:1 从原型 styles.css 搬全部 2311 行 (现 S-01 demo 区块只是子集,要扩到 full)
- `src/App.tsx` — 完全替换 S-01 demo

### 不动
- apps/debug-ui / apps/web / chisha/ 全部
- apps/sandbox-lab/{package.json, vite.config.ts, tsconfig.json, index.html, main.tsx, .gitignore} 不动 (S-01 已稳定)

### 可能小改 (≤ 1 行)
- apps/sandbox-lab/README.md — 把 "S-01 scaffold 阶段" 那句改成 "S-02 完整视觉落地, 交互骨架待 S-03"。本任务范围 (避免 codex hidden gotcha 矛盾)

## 2. types/sandbox.ts shape

直接对应 handoff §数据契约 (Codex Q2 修正: origin/flags 字面值与 handoff 严格对齐):

```ts
export type Theme = "light" | "dark";
export type Accent = "#6366f1" | "#059669" | "#e11d48" | "#d97706";

export interface SessionMeta {
  id: string;
  name: string;
  days: number;          // 7 | 14
  seed: number;
  profile: string;       // "profile@v2"
  origin: "真实快照" | "空白";  // Codex Q2: 与 handoff 88-101 行字面值一致
  status: "running" | "done";
  lastUsed: string;
}

export type MealState = "eat" | "skip" | "current" | "future";
// Codex Q2 修正 (iter2): handoff §104-112 严格只列 ("conflict" | "refine")[]; mock 与
// timeline UI 用 ("swap" | "event") 是 sandbox-lab 视图扩展. 严格 split:
export type HandoffMealFlag = "conflict" | "refine";
export type SbxMealFlag = "swap" | "event";

// HandoffMeal: 后端契约严格对齐 (S-08 adapter 入口校验用)
export interface HandoffMeal {
  idx: number;
  day: number;
  slot: "午" | "晚";
  state: MealState;
  dish: string;
  score: number | null;
  flags: HandoffMealFlag[];
  altScores: AltScore[];
}

// SbxMeal: sandbox-lab 内部 view-model, flags 联合允许扩展
export interface Meal extends Omit<HandoffMeal, "flags"> {
  flags: (HandoffMealFlag | SbxMealFlag)[];
}

export interface AltScore {
  name: string;
  score: number;
  picked?: boolean;
}

// (Meal 在上方已通过 extends HandoffMeal 定义; AltScore 必须在 HandoffMeal 前 forward-declare
//  时, TS 接受 interface 顺序无关. 实施时把 AltScore 放在 HandoffMeal 上方亦可)

export interface RecMeta {
  eta: string;
  dist: string;
  protein: number;
  oil: string;
}

export interface ConflictInfo {
  rule: string;
  reason: string;
}

export interface Rec {
  rank: number;
  name: string;
  venue: string;
  dishes?: string[];
  price: number;
  l2: number;              // 0-1
  l3: number;              // 0-100
  boost: number;
  intent: string;
  l1Hits: string[];
  explore?: boolean;
  conflict?: ConflictInfo | null;
  meta: RecMeta;
  why: string;
}

// Codex Q2 iter2 修正: handoff §132-138 完整列 5 路 (add/rm/up/dn/ttl).
// 原型 DiffRow 只实现 4 路 (panels.jsx 149-208), 把 "rm" 也加上, 渲染时 fallthrough
// 到 ttl 同样 ⏱ 形态 (S-02 visual-only; mock 不实际产 rm 行, 但 types 完整对齐).
export type HandoffDiffKind = "add" | "rm" | "up" | "dn" | "ttl";
export type DiffKind = HandoffDiffKind;

export interface DiffEntry {
  kind: DiffKind;
  field: string;
  from?: string;
  to?: string;
  value?: string;
  delta?: string;
}

export interface Implication {
  field: string;
  text: string;
}

export interface Decision {
  when: string;
  pick: string;
  rank: number | "—";
  l3: number | "—";
  diff: DiffEntry[];
  implications: Implication[];
}

export interface ActiveRule {
  kind: "refine" | "blacklist";
  label: string;
  since: string;           // "D1" / "D2 拒×2"
  ttl?: number;            // 单 round 时 undefined,不显示倒计时
  conflict?: string;
  reason?: string;         // blacklist
}

// Codex Q3 iter2 修正: 不带 color. APanel 用 TS map TASTE_COLOR_TOKENS (在
// components/panels/APanel.tsx 内) 把 name → className, 与 styles.css 的
// .taste-color-{slug} 对齐. mock 也不出 color.
export interface TasteEntry {
  name: string;
  v: number;
  delta: number;
}

export interface KeywordEntry {
  tag: string;
  isNew: boolean;
}

export interface FatigueEntry {
  name: string;
  count: number;
  hot: boolean;
}

export interface BannerEntry {
  id: string;
  level: "warn" | "danger" | "review" | "info";
  title: string;
  detail: string;
  dismissable: boolean;
}

export interface Clock {
  idx: number;
  day: number;
  slot: "午" | "晚";
  total: number;
}

export interface Tweaks {
  timelineVariant: "bars" | "calendar";
  rightDensity: 0 | 1;     // 0=compact, 1=standard
  showDebugLayer: boolean;
  accent: Accent;
  theme: Theme;
}

// Codex Q2 修正: spec §1 提到 FullSnapshot, S-02 本身不用 (S-06a 才用), 但 types 先 stub 防 S-08 漂移
export interface FullSnapshot {
  meta: SessionMeta;
  clock: Clock;
  history: Meal[];
  currentRecs: Rec[];
  lastDecision: Decision | null;
  activeRules: { refine: ActiveRule[]; blacklist: ActiveRule[] };
  taste: TasteEntry[];
  keywords: KeywordEntry[];
  recent: string[];
  fatigue: FatigueEntry[];
}
```

## 3. mocks/sbxMocks.ts

把 data.js 1:1 改 TS,各 const export 出来。

**Codex Q8 修正 (B panel refine TTL)**:
- 原 `ACTIVE_RULES = [{ kind: "refine", label: "戒辣", since: "D1", ttl: 5, conflict: "推荐 #1 川味小炒" }]`
- 改:**完全删 `ttl` 字段**(types 仍 optional 保留给后端,但 mock 不给死值),避免后续代码漂移误渲染倒计时
- DOM 渲染: "当前 round 已应用 \"{label}\""(B panel 模板) — 没有时间维度数据可漏

## 4. styles.css 搬运

策略:**整个文件 cp 过去,不动**,只在头部加个注释 `/* 1:1 ported from chidiansha-sandbox/project/sandbox-lab/styles.css, D-088 S-02 */`。
S-01 已经搬了部分(:root + .theme-dark + demo 区);S-02 整体覆盖,demo 区类(`.demo-shell` `.demo-card` `.swatch[data-accent=...]` 等)删掉,只保留 `:root` + `.theme-dark` + `.density-compact` + 原型完整业务类。

**风险**:原型用了 `var(--font-mono)` 但 :root 定义里 `Inter`/`Noto Sans SC`/`JetBrains Mono` 必须保留。S-01 已加载,检查 index.html font links 已含全。✓

## 5. 组件分册关键决策

### TopBar.tsx
- Props: `{ sessionName, profile, seed, clock: Clock, onOpenSummary, onOpenTweaks }`
- session 下拉本任务只渲一个静态打开/关闭态,**点击切换 session 是 S-03**
- onNewSession 本任务 stub (alert),S-03 接 useSandbox
- mono 字体应用 (clock pill 的 idx/total 数字)

### Timeline.tsx + OpBar (合一文件)
- 内部 `Cell` 子组件
- 14 格 mock: history 喂 idx=0..3, currentIdx=4 (D3午), rest future
- variant: bars (默认) / calendar (从 tweaks 透)
- onSelect: **Codex Q1 修正** — 不在 S-02 引入 selectedCellIdx state. 改用 props inert handler `onSelect: (idx: number) => void`,App.tsx 传 no-op 函数。视觉上 cells 仍可 hover/active 样式 (CSS :hover),但点击不切 ReviewCard
- OpBar 单独 visual-only 渲染,**默认 hidden 状态**(spec 里 visual 静态布局所需即可,不需要点击展开)
- 演示 OpBar 形态: App.tsx 暴露一个 dev-only state `__demoOpBarOpen` (?dev=1 启) 给 OpBar 一个默认 mock meal,纯静态展示视觉
- onTrace 按钮: inert(`onClick={() => {}}`),不调 alert,S-09 接

### DecisionArea.tsx
- Props: `{ recs: Rec[], selectedRank, onSelectRank, onEat, onSwap, onRefine, onSkip, showDebug, clock }`
- 本任务 onEat/onSwap/onRefine/onSkip 全 inert (S-03 接 useSandbox)
- 6 preset chips 写死数组 (handoff §refine: 想吃辣的/换日料/来份烧烤/想吃牛肉/来盖饭/换粤菜)
- refine 输入 controlled state (本组件局部 useState 接 input value,**没有 onSubmit 逻辑**,仅显示输入框)
- Enter / "换一组" / preset chip 点击 — **Codex Q1 修正** — 全部 `onClick={() => {}}` inert,纯视觉演示
- selectedRank: App.tsx 把它 hard-code 喂 `2` (第 2 张卡 selected 高亮),不上 state

### RecCard.tsx
- Props: `{ rec: Rec, selected, onSelect, onEat, showDebug }`
- showDebug=true 显 L3 chip + L1/L2/L3 数字行 + conflict pill
- showDebug=false 显 user-friendly why
- "就这个 →" + "查看详情" 双按钮; e.stopPropagation 防止冒泡触发卡选中
- **Codex RecCard parity 修正**: 在实现前先 Read apps/web/src/components/RecCard.tsx 全部, 核对骨架 (article + rank chip + tags row + title/summary/price + reasoning + meta row + 双 action),确保结构 1:1 一致; 沙箱版只在视觉骨架上**叠加** showDebug 调试层 (L3 chip / 数字行 / conflict pill / 探索 tag) — 不改骨架,不"美化"
- 对 `rec.conflict`: 兼容 `null` (不显示) / object (显示),不假定 undefined-only — Codex Q7

### ReviewCard.tsx
- skip 模式: 极简卡 + "回到决策模式" 按钮
- eat 模式: ReviewHeader + callout (实际选了 #N) + altScores 列表 (picked 高亮) + 打开 trace 按钮
- 都包 ReviewHeader (内部 helper component, 不 export)

### TweaksPanel.tsx (简化版)
- 不引入原型的 host protocol (parent window postMessage),纯本地浮层 + localStorage 持久 (S-01 useTweaks 已干了 theme+accent;本任务扩 3 字段同 useEffect)
- 浮层右下角 fixed, ?dev=1 query 控制可见 (URLSearchParams 检测; 否则 hidden)
- 5 控件:
  - 时间轴形态 (segmented: 横向条/日历卡)
  - 右栏密度 (segmented: 紧凑/标准)
  - 显示 L1/L2/L3 调试层 (toggle)
  - 主色 (4 swatch, 复用 S-01 .swatch[data-accent="..."] CSS)
  - 主题 (segmented: 浅色/深色)
- 关闭按钮 + 拖拽留 v2 (本任务固定位置)

### useTweaks.ts 扩
S-01 当前只 export theme+accent,本任务扩成 5 字段 Tweaks (types/sandbox.ts):
- `load()` fallback: timelineVariant="bars", rightDensity=1, showDebugLayer=true, accent="#6366f1", theme="light"
- useEffect 触发 6 个 accent var + body.theme-dark/light + body.density-compact (rightDensity===0)
- localStorage key 不变 (chisha:sandboxTweaks),内含 5 字段 JSON
- 提供 ACCENTS 数组 export 给 TweaksPanel
- **Codex hidden gotcha #6 修正 (migration)**: load() 读到旧 S-01 2-field JSON (只有 theme/accent) 时, **合并默认值** 而非整个 fallback. 用 `{ ...DEFAULTS, ...validKeys(parsed) }` 模式. 测试覆盖: 注入 S-01 老 localStorage `{"theme":"dark","accent":"#059669"}` → load 应返 `{theme:"dark",accent:"#059669",timelineVariant:"bars",rightDensity:1,showDebugLayer:true}`

### panels/DPanel.tsx
- Props: `{ decision: Decision, density: 0|1|2, onDensityChange: (d:0|1|2) => void, onOpenTrace?: () => void }`
- 紧凑: 压成"Δ N 字段 · N 条影响 [展开 ›]"
- 标准: 三段全显 (动作摘要 + diff list + implications)
- 详细: + trace_id + diff/impl meta 行 + path=D.{field} 等元数据
- DiffRow 子组件 (kind add/up/dn/ttl 4 路样式; "rm" 不实现, types 已限到 4 路)
- **Codex hidden gotcha #6 修正**: 不用 `window.__sbxSetDensity` 全局,密度切换 button 直接调 `onDensityChange` prop
- 没有 onOpenTrace prop 调用时本任务 inert (no-op),S-09 接

### panels/APanel.tsx / BPanel.tsx / CPanel.tsx
- 都有 open/closed 折叠,closed 时 panel-summary 显聚合摘要
- APanel: taste 条 (color 是数据维度不是 accent,遵守 no-inline-color)
  - **Codex Q3 修正**: 不用 data-attribute (每加一个 taste 名要扩 CSS,scale 差)。改用**纯 TS map** `TASTE_COLOR_TOKENS: Record<string, string>` (映射 taste name → CSS variable name 或 className),DOM 渲染走 `className={`taste-bar taste-color-${tokenKey(t.name)}`}` (slug-safe)
  - mock data **不带 color 字段** (Codex Q3 推荐: View 决定)。types/sandbox.ts TasteEntry 也去 color 字段
  - styles.css 内定义 fallback `.taste-color-default { background: linear-gradient(...) }` + 5 个具体 `.taste-color-{slug}` 类
  - 同一 mapping 也用于 keyword 关键词标签 (如果有)
- BPanel: refine 块文案改 "当前 round 已应用 \"{label}\"" (不显示 ttl)
- CPanel: recent 4 顿 + fatigue 表格

### modals/SummaryDrawer.tsx + ConfirmModal.tsx + RefineModal.tsx
- 全部 visual-only,onApply/onConfirm 接 props stub
- SummaryDrawer filter chips 内部 useState
- ConfirmModal kind=rollback/branch 两态,branch 时 controlled input (本组件 useState)
- RefineModal 5 preset chips 写死

## 6. App.tsx 拼装

**Codex Q1 修正**: App 只持有"视觉演示需要"的状态,不持有 selectedCellIdx 等"业务"切换状态(S-03 才引入)。

```tsx
import { useState } from "react";
import { useTweaks } from "./hooks/useTweaks";
// ... imports

export function App() {
  const { tweaks, setTweak } = useTweaks();
  // 视觉演示用 (?dev=1 可控开关; 默认 dev mode 全打开):
  const isDev = new URLSearchParams(window.location.search).has("dev");
  const [tweaksPanelOpen, setTweaksPanelOpen] = useState(isDev);
  const [density, setDensity] = useState<0|1|2>(1); // DPanel 三档密度,纯本地
  // 注: summaryOpen / confirmKind / refineModalOpen 等 modal 状态留 S-03 引入

  const recs = MOCK.CURRENT_RECS;
  const history = MOCK.HISTORY;
  const clock = { idx: MOCK.CURRENT_IDX, day: ..., slot: ..., total: MOCK.TOTAL_MEALS };

  return (
    <div className="app">
      <TopBar sessionName="session-新打分" profile="profile@v2" seed={42}
              clock={clock} onOpenSummary={() => {}} onNewSession={() => {}} />
      <Banners banners={[{id:"demo-c", level:"warn", title:"Refine 冲突",
                          detail:"推荐 #1 川味小炒 违反 'refine 戒辣'",
                          dismissable:true}]} onDismiss={() => {}} />
      <Timeline history={history} currentIdx={clock.idx} total={clock.total}
                selected={null} onSelect={() => {}} variant={tweaks.timelineVariant} />
      <OpBar selected={null} history={history}
             onTrace={() => {}} onRollback={() => {}} onBranch={() => {}} onDismiss={() => {}} />
      <div className="main">
        <DecisionArea recs={recs} selectedRank={2}
                      onSelectRank={() => {}} onEat={() => {}} onSwap={() => {}}
                      onRefine={() => {}} onSkip={() => {}}
                      showDebug={tweaks.showDebugLayer} clock={clock} />
        <RightCol decision={MOCK.LAST_DECISION} density={density}
                  onDensityChange={setDensity} onOpenTrace={() => {}} />
      </div>
      {/* TweaksPanel: ?dev=1 时可见, modal 现 hidden 状态 */}
      <TweaksPanel open={tweaksPanelOpen} onClose={() => setTweaksPanelOpen(false)}
                   tweaks={tweaks} setTweak={setTweak} />
      {/* modals: S-02 mount 但永远 open=false; S-03 才接 state */}
      <SummaryDrawer open={false} history={history} onClose={() => {}} onOpenTrace={() => {}} />
      <ConfirmModal open={false} kind={null} mealRef={null} onClose={() => {}} onConfirm={() => {}} />
      <RefineModal open={false} onClose={() => {}} onApply={() => {}} />
    </div>
  );
}
```

mock 喂全:no fetch / no useEffect 副作用 (除了 useTweaks 已有)。**density (DPanel 3 档) 是唯一允许的本地交互 state**,因为它是"该 panel 本身的视觉模式",不是业务动作。

**ReviewCard 视觉展示**: 本任务不让用户切到 ReviewCard。要展示 ReviewCard 视觉,通过 `?demo-review=1` query 触发 hard-code 渲 ReviewCard 替代 DecisionArea (mock 第 1 顿 D1 午 川味小炒 eat 状态)。chrome-devtools-mcp 用 `?demo-review=1` query 跑一次截图验证 ReviewCard 视觉。

**modals 视觉展示**: 同理用 `?demo-modal=summary|confirm-rollback|confirm-branch|refine` query 强开对应 modal,纯视觉验证。验证完关掉。

## 7. 验证策略

### 7.1 本任务范围内

1. `npm install` 应已稳 (S-01 已装)
2. `npm run typecheck` 全绿
3. `npm run build` 全绿
4. dev server `VITE_PORT=5180 npm run dev` 起来
5. **chrome-devtools-mcp 验证 — 本任务的核心** (chrome 解锁后必须跑):
   - golden path:
     - navigate http://127.0.0.1:5180/
     - take_snapshot:验 TopBar + 14 timeline cells + 5 RecCard + 4 panel + Tweaks 浮层 (?dev=1 url) 都渲染
     - take_screenshot 默认浅色:留照
     - click "深色" pill → snapshot:无硬编码色遗漏 (用 evaluate_script 取若干元素 backgroundColor 验证)
     - take_screenshot 深色:留照
     - click 第 2 accent swatch → 验 --accent / --accent-soft 等 6 个 var 全变
     - click 第 1 个 timeline cell (D1 午) → ReviewCard 出现, eat 模式
     - click 第 3 个 timeline cell (D2 午 skip) → ReviewCard skip 极简模式
     - click "回到决策模式" → 回到 DecisionArea
     - click 摘要按钮 → SummaryDrawer 开
     - click filter chips → 列表过滤
     - click drawer 外 mask → 关闭
   - edge case:
     - showDebugLayer=false → RecCard 不显示 L3 chip / 数字行,显 user why
     - rightDensity=0 → body.density-compact 上去,panel/RecCard 真的变紧 (用 evaluate_script 取 .rec-card padding 验证)
     - timelineVariant=calendar → timeline 切日历卡布局
   - console / network 无 error / 4xx-5xx

6. pytest `uv run --with pytest pytest tests/ -q` 全绿 (本任务不动 py,但跑一遍稳)

### 7.2 视觉对照

`chidiansha-sandbox/project/sandbox-lab/index.html` 打开 (原型 inline Babel 可以直接 file:// 看),对照截图 → 视觉应等同 (字距 / 圆角 / 颜色 / 间距)。chrome-devtools-mcp screenshot 与原型截图肉眼对比。

### 7.3 chrome profile lock 兜底 (Codex Q9)

**Codex Q9 修正**: 如果 chrome profile 持续被并行 session 锁,**不阻断 approval**。改为:
- 每个 chrome-devtools-mcp 步骤生成的 take_screenshot 落盘到 `.tmp_screenshots/S-02-{step}.png` 路径,作为人工 review 留照
- 若 chrome 真完全不可用 (单次重试失败),在 plan-review 报告里明记 "MCP unavailable; saved n/a — visual verification deferred",但允许 S-02 进入 commit
- 留照路径在 .gitignore 已配置 (`.tmp_screenshots/`)
- 仅当 build/typecheck/pytest 失败 → 真正 block

## 8. 回归风险

- **CSS 全量搬运,极小概率有 class 命名冲突** — 原型 styles.css 是自包含的,S-01 demo class 命名 (`.demo-shell` 等) 也跟原型业务 class 不冲突。
- **CSS 硬编码颜色** — Codex Q5 iter2 修正,**bounded stop criterion**:
  - **不做** 全量 hex → var refactor (会无限发散,且原型已经 1:1 验证过视觉)
  - **只做** 两类 hex 替换:
    (a) 与 dark theme 冲突的 hex (在 `.theme-dark` 覆盖时不能传递的固定色 — 通过 chrome screenshot 对比 light/dark 发现的)
    (b) 与 accent 切换有联动需求的 hex (`--accent`/`--accent-soft`/`--accent-ring` 已经覆盖原型的所有 indigo hex,只在 chrome screenshot 切 4 个 accent 时看到没换的紫色 hex 才修)
  - 其他 hardcoded hex (`.cell-badge.swap` 的紫色等数据维度色)**完全不动**,1:1 port
  - 验收: chrome screenshot light/dark + 4 个 accent 切换时无视觉异常 → 完成
  - 实施时**先全量 cp**,再按截图对比反向修订需要替换的 hex
- **prototype 内联 style** — Codex hidden gotcha #4: 原型 app.jsx/panels.jsx/timeline.jsx 多处 `style={{ ... }}` 内联. 本任务在 port 时全部转 className + CSS rule (新增到 styles.css 末尾,统一区块)
- **types/sandbox.ts 严格** — Rec.conflict 是 ConflictInfo | null,mock 用 null 不用 undefined;types 用 `| null`
- **dist/ 上次 build artifact 留着** — gitignore 已配,不进 commit
- **chrome-devtools-mcp 被锁** — 见 §7.3 兜底

## 9. 文档

- 不动 CLAUDE.md / docs/decisions.md / CONTRACTS.md (S-10 统一)
- 不更新 README.md (S-01 README 已说"S-01 阶段",S-02 完成后改一句"已落 S-02 完整视觉")—— 还是改一句,**README.md 改一行**

## 10. 不做

- 交互逻辑 (eat/skip/swap/refine/select/rollback/branch 全部 stub,S-03)
- 后端 fetch (S-08)
- trace 跳转 (S-09)
- "host protocol" 复杂版 Tweaks (简化版即可)
- 任何 Python / chisha/ 改动

## 11. 子文件创建顺序 (实施时)

1. **Read apps/web/src/components/RecCard.tsx 全部** (Codex RecCard parity 修正前置检查)
2. types/sandbox.ts (基础, 含 FullSnapshot stub)
3. mocks/sbxMocks.ts (依 types,删 active_rules.ttl,删 taste.color)
4. styles.css 全量替换 (一边搬一边修 hardcoded hex / 内联 style)
5. useTweaks.ts 扩 5 字段 (含 S-01 → S-02 老数据 migration)
6. RecCard.tsx (1:1 对照 apps/web 骨架)
7. panels/{D,A,B,C}.tsx (独立; APanel 用 TS map 决定 taste 颜色)
8. components/RightCol.tsx (包 4 panel)
9. components/{TopBar, Banners, Timeline (含 OpBar/Cell), DecisionArea, ReviewCard, TweaksPanel}.tsx
10. modals/{SummaryDrawer, ConfirmModal, RefineModal}.tsx
11. App.tsx 重写 (?dev / ?demo-review / ?demo-modal query 演示开关)
12. README.md 改 1 行 (S-02 状态)
