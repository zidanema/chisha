# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.pytest_cache/

# Claude Code
.claude/

# Logs (本地, 私人数据)
logs/

# 临时输出
*.tmp
*.failed.json
# chrome-devtools-mcp 本地自测截图 (debug-ui 改动验证产物)
.tmp_screenshots/
# D-072.1 L2 trace 回归基线 (本地工具产物, scripts/baseline_l2_snapshot.py 可重生成)
tmp/

# 系统
.DS_Store

# 私密 (api keys 等)
.env
.env.*
!.env.example
# apps/web/.env.production 入库 (只声明 VITE_USE_MOCK=0, 无敏感信息, D-069)
!apps/web/.env.production
eval/dish_tagging_eval/.cache/

# D-043 P3 反馈历史 (用户运行时状态, 同 logs/, 不进版本库)
data/feedback_history.jsonl
# D-099 迁移快照 + 未发布 staged + ingest 锁 + schema 失败 staged tagged (本地运行时产物)
data/_migration_snapshot_*/
data/*/_staged/
data/*/.ingest_lock
data/*/dishes_tagged.staged.json

# Web 前端 (apps/web)
apps/web/node_modules/
apps/web/dist/
apps/web/.vite/
apps/web/tsconfig.tsbuildinfo

# T-DIST-01 B.6: hatch wheel build 产物 (chisha_meal-*.whl + .tar.gz + .sha256).
# 不进版本库 — release 走 git tag + GitHub Release 附件 (本期不做, 见 ROADMAP).
dist/

# claude.ai/design 原型上传包 (D-051 输入材料, 不进版本库)
chisha-user.zip

# debug-ui 高保真设计稿 source-of-truth (D-075 输入材料, 不进版本库)
design/

# prompts/ 本地阅读用 PDF (从 .md 导出, 不进版本库)
prompts/*.pdf

# Keys / credentials (public repo hygiene)
*.pem
*.key
id_rsa
id_ed25519
credentials
secrets
*.token

# 备份 (A.1 history rewrite 留下的, 不进版本库)
*.preA1.bak
